# Contacte Duplicate - plan de implementare detaliat (1000 commit-uri)

## Obiectiv

Construirea unei aplicații mobile Android și iOS care găsește, verifică și unește contactele duplicate local pe dispozitiv, fără upload în cloud și cu flux complet de backup și confirmare.

## Reguli obligatorii pentru dezvoltare

1. **Numerotare**: Toate commit-urile folosesc formatul `<WORK_CODE><NUMAR> - <DESCRIERE>`. La finalizarea unui task, acesta se marchează cu codul commitului între paranteze. Taskurile din acest plan nu conțin prefixul `<WORK_CODE>ID`.
2. **Granularitate**: Fiecare commit trebuie să fie atomic. Nu se amestecă refactorizarea cu funcționalități noi.
3. **Paralelizare**: Se folosesc interfețe (`abstract class`) pentru servicii pentru a permite mock-uirea și lucrul în paralel.
4. **Testare**: Fiecare funcționalitate critică (algoritmi, servicii de date) are un commit dedicat pentru teste.
5. **Fără PII**: Nu se loghează date personale (nume, telefoane, email-uri).

---

## FAZA 0: Infrastructură, Configurare și Design System (Commit-uri 0001 - 0150)

### Infrastructură Proiect (0001 - 0030)
- ~~Inițializare repository și adăugare documentație proiect.~~ (JNI0001)
- ~~Adăugare materiale vizuale pentru design.~~ (CHT0002)
- ~~Adăugare mockup overview pentru design.~~ (CHT0003)
- ~~Adăugare ecran dashboard pentru design.~~ (CHT0004)
- ~~Refacere logo-ul după direcția inițială.~~ (CHT0005)
- ~~Încărcare preview app.~~ (CRS00006)
- ~~Actualizare logo-ul pentru aplicație.~~ (CHT0007)
- ~~Adăugare ghidul temei vizuale.~~ (CHT0008)
- ~~Adăugare planul tehnic Android și iOS.~~ (CHT0009)
- ~~Adăugare planul de implementare.~~ (CHT0010)
- ~~Adăugare checklist pentru publicare în store.~~ (CHT0011)
- ~~Actualizare planul proiectului.~~ (CHT0012)
- ~~Adăugare foreground pentru icon adaptiv.~~ (CHT0013)
- ~~Adăugare background pentru icon adaptiv.~~ (CHT0014)
- ~~Refacere logo.svg conform preview-logo.png.~~ (JNI0015)
- ~~Actualizare logo (fără fundal) și creare variante iconițe Android/iOS.~~ (JNI0016)
- ~~Extindere plan implementare la 1000 commit-uri și structurare pentru lucru în paralel.~~ (JNI0017)
- ~~Inițializare proiect Flutter brut.~~ (CHT0018)
- ~~Configurare gitignore pentru Flutter și mobil.~~ (CHT0019)
- ~~Configurare regulile de analiză Dart.~~ (CHT0020)
- ~~Adăugare logo-ul aplicației în assets.~~ (CHT0021)
- ~~Crearea punctului de intrare Flutter.~~ (CHT0022)
- ~~Configurare aplicație principală Flutter.~~ (CHT0023)
- ~~Implementare paletă de culori.~~ (CHT0024)
- ~~Adăugare stiluri de text globale.~~ (CHT0025)
- ~~Configurare teme light și dark.~~ (CHT0026)
- ~~Implementare provider pentru temă.~~ (CHT0027)
- ~~Configurare localizare de bază.~~ (CHT0028)
- ~~Adăugare logger fără PII.~~ (CHT0029)
- ~~Configurare rute principale.~~ (CHT0030)

### UI Components & Layout (0031 - 0100)
- ~~Adăugare buton primar reutilizabil.~~ (CHT0031)
- ~~Adăugare buton secundar reutilizabil.~~ (CHT0032)
- ~~Adăugare card UI reutilizabil.~~ (CHT0033)
- ~~Implementare layout principal brut.~~ (CHT0034)
- ~~Adăugare splash screen brut.~~ (CHT0035)
- ~~Adăugare dashboard brut.~~ (CHT0036)
- ~~Adăugare ecran brut de setări.~~ (CHT0037)
- ~~Adăugare structură Android de bază.~~ (CHT0038)
- ~~Configurare build Android rădăcină.~~ (CHT0039)
- ~~Configurare modul Android app.~~ (CHT0040)
- ~~Adăugare manifest Android cu permisiuni contacte.~~ (CHT0041)
- ~~Adăugare MainActivity Android.~~ (CHT0042)
- ~~Adăugare stil de lansare Android.~~ (CHT0043)
- ~~Adăugare Info plist pentru iOS.~~ (CHT0044)
- ~~Adăugare AppDelegate pentru iOS.~~ (CHT0045)
- ~~Marcare taskuri brute finalizate în plan.~~ (CHT0046)
- ~~Clarificare numerotare reală în plan.~~ (CHT0047)
- ~~Restaurare format work code în plan.~~ (CHT0048)
- ~~Renumerotare plan implementare.~~ (CHT0049)
- ~~Refacere plan de implementare detaliat (1000 commit-uri) și mapare istoric.~~ (JNI0050)
- ~~Creare widget `AppLogo` cu suport pentru diferite dimensiuni.~~ (JNI0051)
- ~~Implementare widget `StatusBadge` pentru scorul de încredere.~~ (JNI0052)
- ~~Creare widget `ContactAvatar`: implementare completă (imagini, inițiale, culori hash).~~ (JNI0053)
- ~~Implementare widget-uri `AppDivider` și `AppLoadingIndicator` (stilizare, animații).~~ (JNI0054)
- ~~Detaliere completă plan implementare (1000 commit-uri) cu specificații tehnice.~~ (JNI0055)
- ~~Extindere completă plan implementare (1000 pași detaliați).~~ (JNI0056)
- ~~Corectie: Reparare numerotare plan si actualizare reguli commit.~~ (JNI0057)
- ~~Sincronizare plan de implementare cu istoricul real Git.~~ (JNI0058)
- ~~Actualizare format taskuri finalizate in plan si reguli commit.~~ (JNI0059)
- ~~Restaurare consecutivitate plan si detaliere task-uri pana la 1000+.~~ (JNI0059)

### UI Components & Layout (1002 - 1100)
- ~~Implementare AppEmptyState: Arhitectură și Layout de Bază.~~ (JNI0060)

- **Implementare `AppEmptyState`: Integrare Ilustrații Vectoriale SVG.** În acest pas, extindem widget-ul creat anterior pentru a suporta ilustrații complexe în locul iconițelor simple. Se va integra pachetul `flutter_svg` pentru randarea fișierelor din `assets/images/illustrations/`. Widget-ul va primi un parametru opțional `String? illustrationPath`. Dacă acest parametru este prezent, se va afișa `SvgPicture.asset` cu o înălțime fixă de 180dp, păstrând aspect ratio-ul original. Ilustrațiile trebuie să fie optimizate (fără metadata inutilă) și să folosească culorile din paleta brandului pentru a se integra natural. Se va implementa o logică de "fallback" care afișează iconița implicită dacă fișierul SVG nu este găsit sau dacă drumul este null. Este esențial ca ilustrația să fie plasată deasupra titlului, cu o distanță de 32dp față de acesta, oferind un impact vizual mai puternic decât o simplă iconiță. Se va testa performanța randării pe dispozitive low-end pentru a asigura un scroll fluid în paginile care ar putea conține acest widget (ex: în interiorul unor tab-uri).

- **Implementare `AppEmptyState`: Integrare Buton de Acțiune Contextual.** Pentru a ghida utilizatorul către următoarea acțiune logică, widget-ul `AppEmptyState` trebuie să includă un buton de acțiune opțional (ex: "Scanează acum" sau "Adaugă contact"). Se va adăuga un parametru `AppPrimaryButton? actionButton` în constructor. Acest buton va fi plasat la baza coloanei, sub descriere, cu o distanță de 24dp. Utilizarea widget-ului `AppPrimaryButton` deja existent asigură consistența stilului (rotunjire 12dp, culori brand). Se va implementa o proprietate booleană `isFullWidthButton` pentru a decide dacă butonul ocupă toată lățimea disponibilă (minus padding-ul lateral) sau se ajustează la conținut. În faza de testare, se va verifica dacă callback-ul `onPressed` al butonului funcționează corect și dacă stările de "hover" sau "splash" sunt vizibile. Acest pas este critic pentru UX, deoarece transformă o pagină informativă într-un punct de conversie în fluxul aplicației, reducând abandonul utilizatorului în zonele fără date.

- **Creare `AppErrorState`: Design și Structură Vizuală pentru Erori.** Acest widget este destinat informării utilizatorului atunci când o operațiune eșuează (ex: lipsă conexiune la baza de date locală sau eroare la citirea contactelor). Va fi creat în `lib/shared/widgets/app_error_state.dart`. Vizual, va folosi culori de avertizare: iconița `Icons.error_outline_rounded` cu dimensiunea de 56dp și culoarea `AppColors.error`. Titlul va fi stilizat cu `AppTextStyles.h3`, iar mesajul de eroare va fi afișat într-un container cu fundal discret `AppColors.error.withOpacity(0.05)` și margini rotunjite de 8dp. Acest container va avea un padding intern de 12dp pentru a separa clar detaliile tehnice ale erorii (dacă sunt furnizate) de restul interfeței. Se va acorda o atenție deosebită accesibilității, asigurându-se că textul erorii poate fi citit de screen-readere (TalkBack/VoiceOver) cu prioritate mare. Widget-ul trebuie să fie suficient de flexibil pentru a fi afișat atât pe tot ecranul, cât și în interiorul unui card mic.

- **Creare `AppErrorState`: Implementare Logică de Reîncercare (Retry).** O eroare fără o cale de rezolvare este frustrantă pentru utilizator. În acest commit, adăugăm un buton "Reîncearcă" (Retry) widget-ului `AppErrorState`. Se va primi un parametru `VoidCallback? onRetry`. Dacă acesta nu este null, sub mesajul de eroare va apărea un `AppSecondaryButton` cu eticheta localizată (folosind `AppLocalizations`). Butonul va avea o animație subtilă de tip "puls" sau o schimbare de culoare la apăsare pentru a confirma interacțiunea. În timpul execuției callback-ului de retry, butonul poate intra opțional într-o stare de `loading` (dacă logică de apelare suportă asta), prevenind apăsările multiple. Se va scrie un widget test care simulează eșecul unei operațiuni, afișarea `AppErrorState` și declanșarea funcției de reîncercare la apăsarea butonului, verificând dacă starea aplicației se actualizează corect ulterior. Acest mecanism de "self-healing" al UI-ului este vital pentru o aplicație robustă.

- **Implementare `AppBottomBar`: Arhitectură și Navigare Principală.** Se va crea componenta de navigare de bază a aplicației în `lib/shared/widgets/app_bottom_bar.dart`. Aceasta va extinde `StatelessWidget` și va folosi `BottomNavigationBar` sau un `Row` customizat pentru un control total asupra design-ului. Structura va include 4 secțiuni: Dashboard (Acasă), Duplicate (Listă), Backup (Istoric) și Setări. Fiecare item va fi reprezentat de un `NavigationItem` model care conține iconița (activă/inactivă), eticheta localizată și ruta destinație. Se va defini o înălțime fixă de 64dp pentru a asigura o zonă de atingere (touch target) confortabilă. Logica de schimbare a indexului va fi gestionată extern (printr-un provider de navigare precum `GoRouter` sau `Riverpod`), astfel încât componenta să rămână un "dumb widget" axat pe prezentare. Se va asigura că bara de navigare respectă zonele sigure ale dispozitivului (SafeArea) pe iOS (unde există "home indicator") și Android.

- **Implementare `AppBottomBar`: Stil vizual avansat și Glassmorphism.** În acest commit, aplicăm finisajele estetice conform `theme-guide.md`. Fundalul barei de navigare va folosi un efect de "Glassmorphism" implementat cu `BackdropFilter` (sigmaX: 10, sigmaY: 10) și o culoare de fundal semi-transparentă (`AppColors.surface.withOpacity(0.8)`). Item-ul selectat va fi evidențiat printr-un gradient subtil (`brandGradient`) aplicat doar iconiței sau printr-o linie orizontală de 3dp plasată deasupra iconiței. Se va adăuga o umbră superioară foarte fină (`AppBoxShadows.soft`) pentru a separa vizual bara de conținutul scrollabil de dedesubt. Animația de schimbare a item-ului selectat va fi una de tip "scale" (mărire ușoară a iconiței la 1.1x). Se va testa vizibilitatea pe diverse fundaluri (imagini, liste colorate) pentru a garanta că textul etichetelor rămâne lizibil. Această componentă este crucială pentru identitatea vizuală a aplicației, oferind acel aspect "premium" și modern cerut în specificații.

- **Rafinare `PrimaryButton`: Implementare Stare de Încărcare (Loading).** Pentru a îmbunătăți feedback-ul vizual în timpul operațiunilor asincrone (ex: pornirea unei scanări), widget-ul `AppPrimaryButton` va primi o proprietate `isLoading` de tip boolean. Când `isLoading` este `true`, textul butonului va fi ascuns printr-o animație de tip `FadeOut`, iar în locul lui va apărea central widget-ul `AppLoadingIndicator.small()` (culoare albă). Dimensiunea butonului trebuie să rămână constantă pentru a evita "layout shift". În această stare, butonul va fi automat dezactivat pentru interacțiuni (`onPressed` nu va fi apelat), prevenind trimiterea multiplă a aceleiași comenzi. Se va utiliza un `AnimatedSwitcher` pentru a asigura o tranziție fluidă între text și indicatorul de progres. Acest detaliu tehnic previne confuzia utilizatorului și oferă o senzație de fluiditate aplicației, confirmând că sistemul procesează cererea.

- **Rafinare `PrimaryButton`: Implementare Stare Dezactivată (Disabled).** Butoanele trebuie să reflecte starea logică a formularului sau a acțiunii (ex: butonul "Merge" este inactiv până când nu se selectează cel puțin două contacte). Se va implementa o stare de `disabled` care se activează fie când `onPressed` este null, fie când o nouă proprietate `isEnabled` este `false`. Vizual, butonul va avea o opacitate redusă la 0.5 și culoarea de fundal va fi gri neutru (`AppColors.surfaceMuted`), eliminând umbrele sau gradienții activi. Din punct de vedere tehnic, se va folosi `IgnorePointer` pentru a bloca orice eveniment de atingere. Se va verifica dacă starea dezactivată este clar distinctă de cea activă în ambele teme (Light/Dark). Această logică de validare vizuală este esențială pentru a preveni erorile de utilizare și pentru a menține un flux de lucru coerent și predictibil.

- **Rafinare `PrimaryButton`: Suport pentru Iconițe și Spasțieri.** Pentru a face butoanele mai descriptive, adăugăm suport pentru iconițe poziționate la stânga sau la dreapta textului. Constructorul va primi `Widget? leadingIcon` și `Widget? trailingIcon`. Spațierea dintre iconiță și text va fi fixă, de 8dp, implementată printr-un `SizedBox`. Iconițele vor moșteni automat culoarea textului butonului (alb pe fundal albastru) pentru a menține contrastul. Se va asigura că textul este centrat corect în interiorul butonului chiar și atunci când este prezentă o singură iconiță pe o parte (folosind un widget "spacer" invizibil de aceeași dimensiune pe partea opusă, dacă este necesar pentru echilibru vizual). Acest pas permite utilizarea butoanelor în contexte variate, cum ar fi "Continuă ->" sau "<- Înapoi", îmbunătățind navigația intuitivă. Toate aceste modificări vor fi documentate în fișierul de widget-uri și testate prin unit teste dedicate pentru a preveni regresia stilistică.
- **Creare `IconButton` Personalizat: Geometrie și Dimensiuni.** Vom dezvolta un widget `AppIconButton` în `lib/shared/widgets/app_icon_button.dart` pentru a standardiza toate butoanele de tip iconiță din interfață (ex: butonul de închidere dialog, butonul de setări). Widget-ul va suporta două forme geometrice principale prin intermediul unui enum `AppIconButtonShape`: `circle` (pentru acțiuni rapide, flotante) și `roundedRect` (cu un `borderRadius` de 12dp, pentru a se alinia cu restul limbajului vizual al butoanelor). Dimensiunea implicită va fi de 40dp pentru a respecta zona minimă de touch, dar va fi configurabilă prin parametrul `size`. Se va folosi un `Container` cu un `BoxDecoration` pentru a defini forma și fundalul. Este esențial ca iconița din interior să fie centrată perfect folosind `Center`. Se va testa randarea pe diverse densități de pixeli pentru a asigura că marginile rotunjite sunt netede și nu prezintă artefacte vizuale.

- **Creare `IconButton` Personalizat: Interacțiune și Efecte Tactile.** Pentru a oferi utilizatorului confirmarea imediată a atingerii, vom integra efecte de "splash" și "highlight" folosind widget-ul `InkWell` în interiorul unui `Material` transparent. Culoarea efectului de splash va fi extrasă din temă, folosind `AppColors.blue500.withOpacity(0.1)` pentru a menține un aspect subtil. Se va implementa suport pentru starea de `disabled`, unde butonul devine semi-transparent și interacțiunile sunt blocate. De asemenea, se va adăuga un parametru `tooltip` care va folosi widget-ul nativ `Tooltip` pentru a afișa o descriere textuală la apăsarea lungă, îmbunătățind accesibilitatea pentru utilizatorii cu deficiențe de vedere. Se va verifica dacă sunetul de sistem pentru click (Haptic Feedback) este declanșat corect la fiecare interacțiune reușită.

- **Implementare Widget `SectionHeader`: Structură și Tipografie.** Acest widget este destinat separării vizuale a secțiunilor în liste lungi (ex: în Dashboard sau în ecranul de Duplicate). Va fi situat în `lib/shared/widgets/section_header.dart`. Layout-ul va fi un `Row` care conține un titlu în stânga (`Text` cu stilul `AppTextStyles.h3`) și, opțional, un buton de tip "text" în dreapta (ex: "Vezi tot"). Butonul din dreapta va folosi stilul `AppTextStyles.label` și culoarea `AppColors.blue500`. Spațierea dintre titlu și buton va fi gestionată automat folosind `MainAxisAlignment.spaceBetween`. Se va implementa o logică de "overflow" pentru titlu pentru a preveni împingerea butonului în afara ecranului pe dispozitive mici. Widget-ul va fie testat pentru a se asigura că titlul este aliniat la baza liniei de text (baseline) cu butonul de acțiune.

- **Implementare Widget `SectionHeader`: Padding și Margini Standard.** Consistența spațierii este vitală pentru un design profesional. `SectionHeader` va include un padding orizontal fix de 16dp (aliniat cu grid-ul global al aplicației) și un padding vertical de 8dp. Se va adăuga un parametru opțional `hasBottomPadding` (implicit `true`) care adaugă o distanță de 12dp sub header pentru a-l separa de elementele listei care urmează. În acest commit, se vor defini constante de spațiere reutilizabile în `lib/core/theme/app_spacing.dart` (dacă nu există deja) pentru a fi folosite în acest widget. Se va verifica dacă widget-ul se integrează corect în interiorul unui `SliverList` sau `ListView`, fără a genera erori de layout sau scroll nedorit.

- **Creare Widget `SettingsTile`: Arhitectură Rând de Setări.** Vom implementa piesa de bază pentru ecranul de setări în `lib/shared/widgets/settings_tile.dart`. Un `SettingsTile` este un rând interactiv care conține: o iconiță în partea stângă (dimensiune 24dp, culoare `AppColors.blue500`), un `Column` central pentru Titlu (`Body1`) și Subtitlu (`Body2`, opțional, culoare `textMuted`). Înălțimea minimă a rândului va fi de 56dp pentru a respecta ghidul de design Android/iOS. Întregul rând va fi înfășurat într-un `InkWell` pentru a permite navigarea sau declanșarea unor acțiuni la apăsare. Se va folosi un `Padding` de 16dp pentru a menține alinierea cu restul interfeței. Acest widget trebuie să fie extrem de flexibil pentru a acomoda diverse lungimi de text fără a rupe layout-ul.

- **Creare Widget `SettingsTile`: Integrare Elemente Trailing (Comutatoare/Săgeți).** Extindem `SettingsTile` prin adăugarea unui parametru `Widget? trailing`. Acesta va permite inserarea unor elemente de control în partea dreaptă a rândului, cum ar fi un `Switch` (pentru activare/dezactivare funcții), o săgeată `Icons.chevron_right` (pentru a indica navigarea spre un alt ecran) sau un widget de tip `Text` informativ. Se va implementa o logică care asigură că elementul `trailing` este mereu aliniat la dreapta și centrat vertical. De asemenea, vom adăuga un separator discret (`AppDivider`) opțional la baza fiecărui tile, controlat prin proprietatea `showDivider`. Se va testa interacțiunea tactilă pentru a confirma că butonul trailing (cum e Switch-ul) primește evenimentele de touch înaintea rândului părinte, evitând declanșarea accidentală a navigării.

- **Adăugare `AppSwitch`: Personalizare și Branding.** Deși Flutter oferă un `Switch.adaptive` excelent, acesta trebuie personalizat pentru a reflecta paleta de culori a brandului. Vom crea un wrapper în `lib/shared/widgets/app_switch.dart`. Când este activat, "thumb-ul" și "track-ul" vor folosi nuanțe de `AppColors.blue500`. Când este dezactivat, culorile vor trece în tonuri de gri neutru (`AppColors.surfaceMuted`). Vom ajusta dimensiunea switch-ului pentru a nu părea disproporționat față de textul din `SettingsTile`. Se va implementa o micro-animație la schimbarea stării (on/off) pentru a adăuga un plus de rafinament. Testarea se va concentra pe verificarea accesibilității: starea switch-ului trebuie să fie clară vizual și raportată corect de serviciile de accesibilitate ale sistemului de operare.

- **Implementare Widget `AppTextField`: Design și Borduri Rotunjite.** Vom crea un câmp de introducere text robust în `lib/shared/widgets/app_text_field.dart`. Acesta va folosi `InputDecoration` pentru a defini un border permanent de 12dp (`borderRadius`). Culoarea border-ului se va schimba din `AppColors.lightBorder` în `AppColors.blue500` atunci când câmpul este focalizat (focused). Vom adăuga suport pentru un `prefixIcon` (ex: lupă pentru căutare) și un padding intern de 16dp (`contentPadding`). Textul de sugestie (hintText) va fi stilizat pentru a fi vizibil dar neintruziv. Acest widget va fi folosit atât pentru căutarea contactelor, cât și pentru editarea manuală a câmpurilor, necesitând o logică solidă de gestionare a `FocusNode`.

- **Implementare Widget `AppTextField`: Logică "Clear" și Validări.** Pentru o experiență de utilizare fluidă, `AppTextField` va include un buton "X" (clear button) în partea dreaptă (`suffixIcon`), care va apărea doar atunci când există text în câmp. La apăsarea acestuia, textul va fi șters și tastatura va rămâne deschisă. Se va implementa suport pentru mesaje de eroare afișate sub border, folosind culoarea `AppColors.error`. De asemenea, vom adăuga proprietatea `keyboardType` pentru a permite optimizarea tastaturii în funcție de conținut (ex: tastatură numerică pentru telefon). Se va scrie un test de integrare care verifică ciclul de viață al focalizării și apariția/dispariția butonului de ștergere în funcție de input-ul utilizatorului.

- **Creare `BadgeCount`: Notificări Vizuale în UI.** Vom implementa un widget mic, circular, în `lib/shared/widgets/badge_count.dart`, folosit pentru a atrage atenția asupra numărului de duplicate găsite sau a acțiunilor pendinte. Acesta va avea un fundal roșu (`AppColors.error`) și un text alb, foarte mic (`AppTextStyles.caption` îngroșat). Dimensiunea va fi fixă (20dp diametru), dar se va auto-ajusta (va deveni o formă de pastilă) dacă numărul depășește două cifre (ex: "99+"). Badge-ul va fi poziționat de obicei în colțul din dreapta sus al unei iconițe dintr-un meniu sau buton. Se va implementa o mică animație de "zoom-in" la prima apariție pentru a fi mai ușor de observat de către utilizator. Acest element este vital pentru a motiva utilizatorul să finalizeze procesul de curățare a contactelor.
- **Implementare `AnimatedCard`: Animații de Intrare în Listă.** Acest task se concentrează pe adăugarea unui plus de fluiditate listelor de contacte. Vom crea un wrapper `AnimatedCard` în `lib/shared/widgets/animated_card.dart` care înfășoară un widget `child`. Folosind `AnimatedOpacity` și `SlideTransition`, vom implementa o animație de "fade-in" și "slide-up" (pornind de la un offset de 20px jos) care se declanșează atunci când cardul este montat în UI. Durata animației va fi de 300ms, folosind curba `Curves.easeOut`. Pentru liste lungi, se va introduce un "delay" calculat în funcție de indexul item-ului (ex: `index * 50ms`), creând un efect de cascadă (staggered animation). Se va asigura că animația rulează o singură dată la prima apariție, pentru a nu deveni obositoare în timpul scroll-ului rapid.

- **Creare Widget `ProgressRing`: Desenare Custom cu `CustomPainter`.** Pentru afișarea scorului de curățenie în Dashboard, avem nevoie de un indicator de progres circular personalizat. Acesta va fi implementat în `lib/shared/widgets/progress_ring.dart` folosind un `CustomPainter`. Vom desena două arcuri concentrice: unul de fundal (gri deschis, cerc complet) și unul de progres (colorat, lungime variabilă în funcție de procent). Grosimea liniei (strokeWidth) va fi de 8dp, iar capetele arcului de progres vor fi rotunjite (`StrokeCap.round`). Dimensiunea widget-ului va fi controlată prin `BoxConstraints`, cu o dimensiune implicită de 80dp. Se va folosi un gradient pe arcul de progres (trecere de la `brandBlue500` la `brandViolet500`) pentru a se alinia cu identitatea vizuală.

- **Creare Widget `ProgressRing`: Animație și Text Central.** Extindem `ProgressRing` prin integrarea unui `AnimationController` care să animeze trecerea de la valoarea curentă la cea nouă a procentului. Vom folosi un `Tween<double>` pentru a calcula valoarea intermediară în fiecare cadru al animației. În centrul cercului, vom afișa textul procentual (ex: "85%") folosind stilul `AppTextStyles.h2`. Culoarea textului se va schimba din roșu în verde pe măsură ce scorul crește, oferind un feedback vizual semantic despre starea contactelor. Se va verifica dacă textul este centrat matematic în interiorul arcului, indiferent de grosimea acestuia, și dacă cifrele nu "tremură" în timpul animației.

- **Adăugare Suport pentru Gradienți în `AppCard`.** Pentru a permite crearea unor elemente de UI mai dinamice (cum ar fi cardul principal de scanare), vom actualiza widget-ul `AppCard` din `lib/shared/widgets/app_card.dart`. Vom adăuga un parametru opțional `Gradient? gradient`. În metoda `build`, dacă gradientul este prezent, acesta va fi aplicat proprietății `gradient` din `BoxDecoration`, suprascriind culoarea solidă `color`. Se va asigura că umbrele și rotunjirea marginilor (16dp) sunt menținute corect și în cazul folosirii gradienților. De asemenea, vom implementa o proprietate `isTransparent` care, dacă e setată pe `true`, va elimina fundalul cardului dar va păstra structura de padding și margini, utilă pentru overlay-uri.

- **Implementare Sistem de Umbre: `AppBoxShadows`.** Design-ul aplicației se bazează pe o ierarhie vizuală clară obținută prin umbre (depth). În `lib/core/theme/app_shadows.dart`, vom defini o clasă statică `AppBoxShadows` care conține trei tipuri de umbre: `soft` (blur: 8, spread: 0, opacity: 0.05, yOffset: 2), `medium` (pentru carduri active) și `hard` (pentru dialoguri). Toate umbrele vor folosi nuanțe foarte subtile de gri-albăstrui pentru a nu părea "murdare" pe tema light. Se va implementa o logică de adaptare pentru tema dark, unde umbrele vor fi înlocuite de o ușoară elevație a culorii de fundal (elevation overlay), conform standardelor Material Design.

- **Creare Widget `InfoBox`: Mesaje Informative de Tip Callout.** Vom implementa un widget utilitar în `lib/shared/widgets/info_box.dart` pentru a afișa sfaturi sau avertismente non-critice utilizatorului (ex: "Scanarea poate dura până la 1 minut"). Layout-ul va consta într-un rând cu o iconiță `Icons.info_outline` în stânga și un text explicativ în dreapta. Fundalul va fi o nuanță foarte pală de albastru (`AppColors.blue500.withOpacity(0.08)`), iar marginile vor fi rotunjite la 12dp. Padding-ul intern va fi de 12dp pe toate laturile. Se va adăuga un buton mic de "Închide" (X) opțional, care permite utilizatorului să ignore mesajul. Widget-ul va fi testat pentru a se asigura că textul lung face wrap corect fără a depăși marginile containerului.

- **Rafinare Splash Screen: Animații Secvențiale Complexe.** Vom transforma ecranul de pornire într-o experiență vizuală memorabilă. În `lib/features/splash/splash_screen.dart`, vom implementa o secvență de animații folosind `Interval`: 1. Logo-ul aplicației apare printr-un "scale-in" (de la 0.5 la 1.0) și "fade-in" în primele 800ms. 2. Numele aplicației glisează ușor de jos în sus sub logo în următoarele 500ms. 3. Un indicator de încărcare discret apare la bază după 1.5 secunde. Vom folosi un `SingleTickerProviderStateMixin` pentru a gestiona controller-ul principal. Se va asigura că tranziția către ecranul următor (Onboarding sau Dashboard) are loc doar după finalizarea animațiilor și a verificărilor de sistem, oferind o senzație de stabilitate.

- **Implementare `AppDialog`: Fundație pentru Ferestre Modale.** Vom crea un widget de bază pentru dialoguri în `lib/shared/widgets/app_dialog.dart`. Acesta va folosi `BackdropFilter` pentru a blura conținutul din spatele ferestrei (`sigmaX: 5, sigmaY: 5`), creând un efect de focus. Containerul dialogului va avea un fundal alb (sau gri închis în tema dark), margini rotunjite la 24dp (`radius.xl`) și un padding intern generos de 24dp. Se va include o zonă pentru titlu, o zonă pentru conținut scrollabil (în caz de texte lungi) și o zonă inferioară pentru butoane de acțiune aliniate la dreapta. Se va implementa o animație de deschidere tip "Grow" (mărire de la centru) pentru a atrage atenția utilizatorului într-un mod natural.

- **Creare `ConfirmationDialog`: Dialoguri de Decizie Standardizate.** Bazându-ne pe `AppDialog`, vom crea o variantă specializată pentru confirmări (ex: "Ești sigur că vrei să ștergi acest backup?"). Constructorul va primi `title`, `message`, `confirmText` și `cancelText`. Butonul de confirmare va putea fi stilizat ca "danger" (fundal roșu) dacă acțiunea este distructivă. Vom implementa o logică care returnează o valoare booleană (`true`/`false`) prin `Navigator.pop`, simplificând apelarea dialogului din codul de business. Se va testa închiderea dialogului atât prin butoane, cât și prin apăsarea în afara acestuia (barrier dismiss), asigurându-se că feedback-ul către apelant este corect.

- **Implementare `AppBottomSheet`: Panouri Glisante de Jos.** Pentru acțiuni secundare sau selecții rapide, vom crea un widget în `lib/shared/widgets/app_bottom_sheet.dart`. Acesta va folosi `showModalBottomSheet` din Flutter, dar cu o personalizare profundă a stilului: margini rotunjite doar sus (20dp), un mic "handle" (bară orizontală) în partea superioară pentru a indica glisarea și un fundal adaptat temei. Conținutul va fi înfășurat într-un `SafeArea` pentru a evita suprapunerea cu butoanele de sistem. Se va implementa suport pentru starea "expandată" (full screen) și cea "compactă" (ajustată la dimensiunea conținutului). Testarea se va concentra pe fluiditatea gestului de glisare (drag to dismiss) și pe viteza de reacție a animației de închidere.
- **Creare `SelectionListTile`: Selecție Granulară în Liste.** Acest widget va fi piesa centrală pentru procesul de fuziune (merge), unde utilizatorul decide ce câmpuri păstrează dintr-un contact duplicat. Îl vom implementa în `lib/shared/widgets/selection_list_tile.dart`. Structura va fi un `Row` care conține un `Checkbox` personalizat (folosind culorile brandului) în stânga și o coloană cu informații în dreapta. Coloana va afișa valoarea câmpului (ex: numărul de telefon) și o etichetă (ex: "Mobil"). Întregul rând va fi reactiv: apăsarea oriunde pe suprafața acestuia va schimba starea checkbox-ului. Se va implementa o logică de evidențiere (highlight) a rândului selectat printr-un fundal foarte discret, oferind un feedback vizual clar.

- **Adăugare Suport pentru "Haptic Feedback": Răspuns Tactil.** O aplicație modernă trebuie să se "simtă" bine la utilizare. În acest commit, vom integra apeluri către `HapticFeedback` din Flutter în toate widget-urile de tip buton (`AppPrimaryButton`, `AppIconButton`, etc.). Vom folosi `HapticFeedback.mediumImpact()` pentru acțiuni importante (cum ar fi pornirea scanării) și `lightImpact()` pentru selecții simple. Această adăugire tehnică îmbunătățește percepția calității aplicației și confirmă acțiunile utilizatorului chiar și atunci când dispozitivul este pe silențios. Se va asigura că feedback-ul haptic este declanșat doar pe dispozitivele care îl suportă, fără a genera erori pe restul.

- **Creare Widget `MergeFieldRow`: Compararea Datelor.** În ecranul de detalii fuziune, utilizatorul trebuie să compare valorile între două contacte. Widget-ul `MergeFieldRow`, situat în `lib/shared/widgets/merge_field_row.dart`, va afișa cele două valori una lângă alta sau una sub alta (în funcție de spațiul disponibil). Vom folosi `RichText` și `TextSpan` pentru a evidenția automat diferențele (ex: dacă un număr de telefon are un prefix diferit, acel prefix va fi colorat în roșu). Această logică de "diffing" vizual este esențială pentru a ajuta utilizatorul să ia decizii rapide. Widget-ul va fi testat cu diverse tipuri de date: nume, email-uri lungi și numere de telefon formatate diferit.

- **Implementare `SearchEmptyState`: Rezultate de Căutare Vide.** Aceasta este o specializare a widget-ului `AppEmptyState` creat anterior. În `lib/shared/widgets/search_empty_state.dart`, vom configura un mesaj specific pentru căutări fără rezultat: iconița `Icons.search_off_rounded`, titlul "Niciun rezultat găsit" și o descriere care sugerează utilizatorului să verifice ortografia sau să încerce alți termeni. Vom adăuga și un buton de "Resetează căutarea" care golește câmpul `AppTextField`. Această abordare reduce frustrarea utilizatorului și îi oferă o cale clară de a continua interacțiunea cu aplicația.

- **Creare Widget `SummaryCard`: Statistici Rapide.** Pentru Dashboard, avem nevoie de carduri compacte care afișează cifre cheie (ex: "45 Duplicate", "340 MB eliberați"). Widget-ul `SummaryCard` va fi un card de 100x100dp cu o iconiță în colțul stânga-sus și o valoare mare, centrală, folosind stilul `AppTextStyles.h1`. Sub valoare, va exista o etichetă mică descriptivă. Fundalul va fi alb cu o umbră `AppBoxShadows.soft`. Vom implementa o animație de tip "numărător" (count-up) pentru cifre, care rulează la deschiderea ecranului, adăugând o notă de dinamism Dashboard-ului.

- **Implementare `AppSkeletonLoader`: Placeholders de Încărcare.** Pentru a evita ecranele complet albe în timpul procesării datelor, vom crea un sistem de "Skeleton Loading" (shimmer effect). Widget-ul `AppSkeletonLoader` va genera forme geometrice (cercuri și dreptunghiuri cu margini rotunjite) care pulsează ușor în opacitate (de la 0.3 la 0.7). Vom folosi un `AnimationController` cu `RepeatMode.reverse` pentru a controla pulsația. Acest widget va fi folosit drept bază pentru a crea placeholder-e care mimează structura Dashboard-ului, oferind utilizatorului o idee despre layout-ul final înainte ca datele reale să fie disponibile.

- **Creare `SkeletonLoader` pentru Listă de Contacte.** Folosind tehnica de la task-ul anterior, vom crea o componentă specifică pentru listele de contacte. Aceasta va afișa un rând placeholder cu un cerc în stânga (avatar) și două linii gri de lungimi diferite în dreapta (nume și telefon). Vom expune un parametru `itemCount` pentru a randa mai multe rânduri simulate. Această tehnică de "perceived performance" este vitală pentru aplicațiile care lucrează cu baze de date mari, menținând utilizatorul angajat în timp ce aplicația lucrează în fundal.

- **Adăugare `AppBlur`: Widget Utilitar pentru Efecte Vizuale.** Design-ul modern folosește mult blur pentru a crea ierarhie. Vom crea widget-ul `AppBlur` în `lib/shared/widgets/app_blur.dart` care înfășoară un `child` și aplică un `ImageFiltered` sau `BackdropFilter`. Parametrii vor permite controlul intensității (sigmaX, sigmaY). Acest widget va fi folosit intens în dialoguri, bare de navigare și overlay-uri de onboarding pentru a concentra atenția utilizatorului pe elementele din prim-plan, estompând contextul din spate.

- **Implementare Widget `LanguageSelector`: Alegerea Limbii.** Pentru ecranul de setări, avem nevoie de o modalitate intuitivă de a schimba limba. Widget-ul `LanguageSelector` va afișa o listă (de obicei într-un BottomSheet) cu opțiunile disponibile (Română, Engleză). Fiecare opțiune va include numele limbii și, opțional, un steag. Vom folosi `RadioListTile` pentru a indica selecția curentă. Schimbarea selecției va declanșa automat actualizarea întregii aplicații prin intermediul `AppLanguageService`. Se va testa persistența alegerii după repornirea aplicației.

- **Creare `ThemeSelector`: Controlul Aspectului (Light/Dark).** Ultimul widget major din această fază este selectorul de temă. Acesta va oferi trei opțiuni: "Luminoasă", "Întunecată" și "Sistem" (care urmărește setările dispozitivului). Vizual, poate fi implementat sub formă de butoane segmentate sau o listă de selecție. Vom folosi iconițe sugestive (Soare, Lună, Monitor). Schimbarea temei va folosi un `provider` (Riverpod) pentru a aplica noile culori instantaneu în toată aplicația, fără a necesita repornirea. Se va verifica dacă toate culorile de brand (gradienți, umbre) se adaptează corect în ambele moduri.

- **Audit UI și Curățenie: Alinierea la 8dp Grid.** Înainte de a trece la faza următoare, vom efectua un audit tehnic al tuturor widget-urilor create (0031-0100). Vom folosi un instrument de debug pentru a verifica dacă toate paddings-urile, marginile și dimensiunile sunt multipli de 8, conform specificațiilor. Vom elimina orice cod redundant sau stiluri hardcodate, mutându-le în `app_colors.dart` sau `app_text_styles.dart`. Se vor rula toate widget testele create până acum pentru a asigura o bază solidă. Acest pas de consolidare garantează că interfața este nu doar frumoasă, ci și construită corect din punct de vedere tehnic.

### Localization & Asset Management (0102 - 0150)
- **Configurare `Intl` pentru Suport Plural.** Această etapă presupune configurarea pachetului `intl` în proiect pentru a gestiona corect formele de plural în limbile Română și Engleză. În Română, avem trei forme (ex: "1 contact", "2 contacte", "20 de contacte"), spre deosebire de cele două forme din Engleză ("1 contact", "2 contacts"). Vom defini regulile în fișierele `.arb` folosind sintaxa `{count, plural, =1{...} other{...}}`. Se va crea un serviciu utilitar `AppPluralFormatter` care facilitează apelarea acestor reguli din cod. Acest pas este esențial pentru a oferi o experiență de utilizator naturală și corectă gramatical în toate secțiunile aplicației care afișează numere (duplicate, fișiere backup, contacte selectate).

- **Adăugare Traduceri RO: Flux Onboarding.** Vom popula fișierul `lib/core/l10n/app_ro.arb` cu toate textele necesare pentru primele ecrane ale aplicației. Aceasta include titluri convingătoare (ex: "Curăță-ți agenda în siguranță"), descrieri detaliate pentru fiecare pas (Privacy, Backup, Start) și etichetele butoanelor ("Continuă", "Sari peste"). Textele vor fi scrise într-un ton prietenos și profesionist, explicând beneficiile aplicației. Se va acorda atenție lungimii string-urilor pentru a preveni suprapunerile pe ecranele mici. Toate cheile vor fi denumite folosind formatul `onboardingTitleStep1`, `onboardingDescStep1`, asigurând o organizare clară.

- **Adăugare Traduceri EN: Flux Onboarding.** Similar cu pasul anterior, vom completa fișierul `lib/core/l10n/app_en.arb`. Traducerile nu vor fi mot-a-mot, ci adaptate cultural pentru a suna natural pentru un vorbitor de limba Engleză. Se va folosi terminologia standard de iOS/Android pentru permisiuni și setări. Acest fișier va servi drept "template" de bază pentru proiect. Vom verifica dacă toate cheile create în varianta RO au un corespondent în varianta EN pentru a evita erorile de compilare ale generatorului `intl`. Această bilingvitate pregătește aplicația pentru o audiență internațională încă din primele faze ale dezvoltării.

- **Adăugare Traduceri RO: Dashboard și Statistici.** Vom introduce textele specifice ecranului principal în fișierul de localizare Română. Aceasta include etichetele pentru scorul de "sănătate" a contactelor (ex: "Scor de încredere"), rezumatul scanării ("Am găsit 45 de duplicate") și denumirile categoriilor (Nume identice, Numere identice, Email-uri). De asemenea, vom localiza mesajele de încurajare care apar atunci când agenda este curată ("Felicitări! Agenda ta este organizată"). Se va folosi un vocabular tehnic simplificat pentru a nu intimida utilizatorii mai puțin experimentați.

- **Adăugare Traduceri EN: Dashboard și Statistici.** Completăm varianta Engleză pentru Dashboard. Vom folosi termeni precum "Health Score", "Duplicates Found" și "Clean". Se va verifica dacă textele în Engleză (care sunt de obicei mai scurte decât cele în Română) nu lasă spații vide inestetice în layout-ul cardurilor `SummaryCard`. Acest proces de "layout-testing" prin localizare este vital pentru a asigura un design adaptiv (responsive).

- **Adăugare Traduceri RO: Listă Duplicate și Filtrare.** Populam textele pentru ecranul de gestionare a duplicatelor. Aceasta include opțiunile de sortare (După nume, După număr), filtrele (Grupate, Individuale) și butoanele de acțiune în masă ("Unește toate", "Ignoră"). De asemenea, vom adăuga traducerile pentru mesajele de confirmare care apar înainte de o fuziune. Textele vor sublinia siguranța procesului, folosind termeni precum "Previzualizare" și "Confirmare".

- **Adăugare Traduceri EN: Listă Duplicate și Filtrare.** Varianta Engleză pentru lista de duplicate va folosi terminologie precum "Merge All", "Ignore", "Sort by Name". Se va asigura consistența între denumirile din interfață și cele din documentația de ajutor a aplicației. Vom testa afișarea pe dispozitive cu fonturi mari (accessibility fonts) pentru a vedea cum se comportă textul în ambele limbi.

- **Adăugare Traduceri RO: Detalii Merge și Selecție Câmpuri.** Localizăm ecranul cel mai complex din punct de vedere al informațiilor: cel în care utilizatorul alege ce date păstrează. Traducerile vor include nume de câmpuri (Telefon, Adresă, Organizație, Note) și avertismente specifice (ex: "Aceste numere sunt diferite. Alege unul singur pentru contactul final"). Se va folosi un limbaj precis pentru a evita orice confuzie care ar putea duce la pierderea accidentală a datelor de către utilizator.

- **Adăugare Traduceri EN: Detalii Merge și Selecție Câmpuri.** Varianta Engleză va folosi "Phone", "Address", "Job Title". Vom implementa și textele pentru stările de succes (ex: "Contacts successfully merged") și eroare. Se va verifica dacă termenii lungi (ex: "Organization") nu sunt tăiați (truncated) în lista de selecție. Acest pas încheie localizarea interfeței de bază pentru procesul de fuziune.
- **Adăugare Traduceri RO: Backup & Restaurare.** În acest pas, localizăm toate textele legate de siguranța datelor. Vom adăuga traducerile pentru procesul de export vCard (ex: "Creare copie de rezervă", "Fișierul a fost salvat cu succes"), pentru istoricul de restaurare ("Restaurări anterioare") și pentru avertismentele de integritate a fișierelor. Mesajele vor sublinia că datele sunt salvate doar local, folosind un limbaj care să inspire încredere (ex: "Contactele tale nu părăsesc niciodată acest dispozitiv"). Se va verifica dacă textele sunt clare și oferă instrucțiuni precise despre unde poate fi găsit fișierul exportat.

- **Adăugare Traduceri EN: Backup & Restaurare.** Varianta Engleză pentru backup va folosi termeni precum "Backup & Restore", "Export vCard", "Save to Device". Vom asigura consistența cu terminologia sistemului de operare pentru "Share" și "Files". Se va scrie un test de localizare care verifică dacă textele se schimbă corect atunci când utilizatorul schimbă limba în timpul procesului de export, prevenind orice eroare de context.

- **Adăugare Traduceri RO: Setări și Administrare.** Vom popula ecranul de setări cu toate etichetele necesare: "Preferințe", "Aspect", "Limbă aplicație", "Despre", "Contactează echipa de suport" și "Resetare setări". De asemenea, vom localiza textele pentru versiunea aplicației și drepturile de autor. Un element critic este localizarea opțiunii de "Resetare", unde mesajul de confirmare trebuie să fie foarte clar pentru a nu fi confundat cu ștergerea contactelor reale.

- **Adăugare Traduceri EN: Setări și Administrare.** Completăm varianta Engleză pentru setări ("Settings", "General", "Appearance", "Language"). Se va verifica dacă layout-ul `SettingsTile` creat anterior acomodează textele mai lungi în ambele limbi fără a împinge elementele de tip `trailing` în afara ecranului. Acest pas finalizează localizarea statică a tuturor ecranelor principale ale aplicației.

- **Adăugare Mesaje de Eroare Localizate (RO/EN).** Erorile trebuie să fie pe înțelesul utilizatorului, nu doar coduri tehnice. Vom crea un set de mesaje localizate pentru situații precum: "Lipsă permisiune contacte", "Memorie plină", "Eroare la scrierea bazei de date", "Fuziune eșuată" și "Fișier backup corupt". Pentru fiecare eroare, vom furniza atât un titlu scurt, cât și o descriere care sugerează o soluție (ex: "Te rugăm să verifici setările de sistem pentru a acorda acces"). Această abordare human-centric reduce anxietatea utilizatorului în fața problemelor tehnice.

- **Implementare `AppLanguageService`: Persistență și Logică.** Vom crea serviciul responsabil de gestionarea limbii în `lib/core/l10n/app_language_service.dart`. Acesta va folosi pachetul `shared_preferences` pentru a salva alegerea utilizatorului. Serviciul va expune un `Locale` curent și o metodă `changeLanguage(Locale locale)`. La schimbarea limbii, serviciul va notifica ascultătorii (folosind un `ChangeNotifier` sau un `StateProvider` din Riverpod), declanșând re-randarea întregului arbore de widget-uri. Se va implementa o logică de detectare automată a limbii sistemului la prima pornire, oferind o experiență personalizată "out-of-the-box".

- **Adăugare Asset-uri SVG: Iconițe de Acțiune.** Vom integra în proiect iconițele vectoriale necesare pentru interacțiunile de bază, situate în `assets/icons/actions/`. Acestea includ: `merge.svg` (simbol de fuziune), `scan.svg` (lupă peste contacte), `delete.svg` (coș de gunoi) și `backup.svg`. Toate iconițele trebuie să urmeze stilul vizual definit în `theme-guide.md` (linii de 2dp, margini rotunjite). Vom folosi `flutter_svg` pentru a le randa, asigurându-ne că suportă schimbarea culorii prin parametrul `colorFilter`.

- **Adăugare Asset-uri SVG: Categorii de Contacte.** Pentru a vizualiza mai bine datele, adăugăm iconițe pentru tipurile de câmpuri în `assets/icons/contact/`: `phone.svg`, `email.svg`, `work.svg` (serviciu), `home.svg` (acasă) și `social.svg`. Acestea vor fi folosite în listele de detalii pentru a ajuta utilizatorul să identifice rapid informația. Se va verifica dacă iconițele au aceleași dimensiuni de bază (viewBox 24x24) pentru a asigura o aliniere perfectă în liste.

- **Optimizare Asset-uri SVG cu `SVGO`.** Dimensiunea aplicației contează. Vom rula utilitarul `svgo` pe toate fișierele SVG adăugate pentru a elimina metadata inutilă (creat de editori precum Illustrator sau Figma), comentariile și ID-urile redundante. Acest proces poate reduce dimensiunea fișierelor vectoriale cu până la 60%, fără a afecta calitatea vizuală. Se va crea un mic script de tip "pre-commit hook" care să automatizeze această optimizare pe măsură ce se adaugă noi imagini în viitor.

- **Script de Validare Localizare (CI/CD).** Pentru a preveni erorile de tip "missing translation", vom scrie un script în Dart situat în `scripts/validate_l10n.dart`. Acesta va citi recursiv fișierele `.arb` și va verifica dacă toate cheile prezente în fișierul de referință (EN) există și în fișierul RO. Script-ul va raporta orice discrepanță și va opri procesul de build dacă se găsesc chei lipsă. Această automatizare este vitală pentru menținerea calității pe măsură ce proiectul crește și se adaugă noi funcționalități.

- **Integrare Asset-uri Audio și Feedback Sonor.** Vom adăuga câteva sunete discrete (sub 100ms latență) în `assets/audio/` pentru a confirma acțiunile reușite (un sunet de "success" la finalizarea fuziunii) sau interacțiunile tactile (un mic "click" la apăsarea butoanelor principale). Vom folosi pachetul `audioplayers` sau `soundpool` pentru o execuție rapidă. Feedback-ul sonor va fi opțional, putând fi dezactivat din ecranul de setări, respectând astfel preferințele fiecărui utilizator.

- **Configurare Fonturi de Sistem și Personalizate.** Vom definitiva configurarea tipografică în `pubspec.yaml` și `lib/core/theme/app_text_styles.dart`. Se va seta fontul `Inter` ca font principal pentru întregul proiect, asigurându-ne că toate variațiile de greutate (Regular, Medium, Bold) sunt incluse corect în pachetul aplicației. Se va implementa o logică de fallback către fonturile de sistem (Roboto pe Android, San Francisco pe iOS) în cazul în care încărcarea fontului personalizat eșuează, garantând lizibilitatea textului în orice condiții.

- **Implementare Clasă `AppAssets`: Acces Tipizat.** Pentru a evita erorile de tip "typo" în căile către fișiere, vom crea o clasă cu constante statice în `lib/core/constants/app_assets.dart`. Aceasta va conține toate rutele către imagini, iconițe și animații (ex: `static const String logo = 'assets/brand/logo.svg'`). De asemenea, vom adăuga metode utilitare care returnează un `SvgPicture` sau un `Image` gata configurat, reducând codul duplicat din UI.

- **Creare Imagini Placeholder pentru Contacte.** Vom genera sau integra un set de imagini placeholder pentru contactele care nu au o poză de profil. Acestea vor fi simple (inițialele numelui pe un fundal colorat discret) sau iconițe generice de tip "avatar". Vom implementa widget-ul `AppContactImage` care încearcă să încarce poza reală a contactului, iar în caz de eșec (eroare sau poză lipsă), afișează automat placeholder-ul corect.

- **Ilustrații Empty State: Design Final.** Vom definitiva design-ul ilustrațiilor SVG folosite în `AppEmptyState`. Acestea vor include scene precum: "Niciun duplicat găsit" (un personaj care privește o listă curată), "Sunteți gata de scanare" (un radar stilizat) și "Backup finalizat". Ilustrațiile vor folosi culorile brandului și un stil minimalist pentru a nu încărca vizual interfața. Se va verifica randarea lor corectă pe tablete și ecrane cu densitate mare.

- **Ilustrații Onboarding: Conexiune Emoțională.** Vom adăuga ultimele ilustrații pentru fluxul de introducere. Acestea au rolul de a explica concepte abstracte precum "Criptarea datelor local" sau "Fuziunea inteligentă" prin metafore vizuale simple. Ilustrațiile vor fi animate discret (ex: un efect de plutire) folosind biblieteci precum `lottie` (dacă sunt fișiere JSON) sau simple animații de rotație/translație din Flutter.

- **Configurare Generator Localizare: `l10n.yaml`.** Vom definitiva setările pentru generarea automată a codului de localizare. Vom specifica folderul de output, fișierul template și activarea opțiunii de "nullable-getter: false" pentru a simplifica utilizarea traducerilor în cod (ex: `context.l10n.title` în loc de `context.l10n?.title`). Această configurare robustă asigură că fluxul de lucru pentru localizare este rapid și lipsit de erori.

- **Unit Test: `AppLanguageService`.** Vom scrie teste pentru serviciul de limbă, verificând: 1. Dacă limba se schimbă corect la apelul metodei `changeLanguage`. 2. Dacă alegerea este salvată în persistent storage. 3. Dacă la repornire se încarcă limba corectă. 4. Dacă detectarea automată a limbii sistemului funcționează conform așteptărilor. Aceste teste garantează că fundamentul bilingv al aplicației rămâne intact pe tot parcursul dezvoltării.

- **Audit Vizual Localizare: Overflow și Spasțieri.** Ultima verificare înainte de finalizarea Fazei 0 presupune scanarea tuturor ecranelor cu textele în Română (limba mai lungă). Vom căuta zone unde textul depășește marginile containerelor (overflow) sau unde butoanele devin prea late. Vom ajusta stilurile de text (reducerea dimensiunii fontului sau creșterea înălțimii containerelor) pentru a acomoda orice lungime de string rezonabilă. Acest pas este crucial pentru a evita un aspect "nefinisat" al aplicației.

- **Normalizarea Căutării: Suport pentru Diacritice.** Utilizatorii români pot căuta contacte atât cu diacritice (ex: "Ștefan"), cât și fără (ex: "Stefan"). Vom implementa o funcție de normalizare care transformă toate caracterele speciale în echivalentele lor simple. Această logică va fi integrată direct în serviciul de căutare, asigurând că utilizatorul găsește rezultatul dorit indiferent de modul în care tastează. Se va scrie un unit test care verifică mapping-ul corect pentru toate caracterele specifice limbii române (ș, ț, ă, î, â).

- **Audit de Cod: Eliminarea String-urilor Hardcodate.** Vom rula un instrument de analiză statică (sau un script custom) pentru a identifica orice text afișat în UI care nu este extras din fișierele de localizare. Toate aceste string-uri vor fi mutate în `.arb` și apelate prin `context.l10n`. Acest proces de curățenie asigură că aplicația este 100% pregătită pentru localizare și că nu vor apărea cuvinte netraduse în versiunea finală.

- <WORK_CODE>0132-0150 - **Pregătire Faza 1: Verificare Infrastructură pe Dispozitive Reale.** Acest bloc de commit-uri este rezervat pentru teste de compatibilitate finală a Fazei 0 pe dispozitive fizice Android și iOS (nu doar simulatoare). Se va verifica randarea corectă a iconițelor adaptive, viteza de încărcare a asset-urilor și stabilitatea sistemului de teme. Orice mică problemă de performanță sau discrepanță vizuală va fi corectată acum, asigurând o fundație perfectă pentru implementarea logică a funcționalităților de Onboarding și Permisiuni care urmează în Faza 1.

---

## FAZA 1: Onboarding, Permisiuni și Securitate (Commit-uri 0151 - 0300)

### Onboarding Flow (0151 - 0200)
- **Creare `OnboardingModel`: Structura de Date.** Începem Faza 1 prin definirea modelului de date pentru ecranul de introducere în `lib/features/onboarding/models/onboarding_model.dart`. Acest model va fi o clasă imuabilă care conține: `title`, `description`, `assetPath` (pentru ilustrația SVG) și un `backgroundGradient` (care va folosi culorile din `AppColors`). Folosirea unui model dedicat ne permite să adăugăm sau să modificăm pașii de onboarding prin simpla actualizare a unei liste statice de obiecte, fără a modifica structura widget-ului. Se va implementa și o metodă `factory` pentru a crea obiecte din fișiere de localizare, asigurând suportul bilingv.

- **Implementare `OnboardingController`: Gestiunea Stării.** Pentru a controla fluxul de onboarding (schimbarea paginilor, finalizarea), vom crea `OnboardingController` folosind Riverpod (`StateNotifierProvider`). Starea va fi reprezentată de indexul paginii curente (0, 1 sau 2). Controller-ul va expune metode precum `nextPage()`, `previousPage()` și `completeOnboarding()`. Vom implementa o logică de validare care previne depășirea indexului maxim. Acest controller va fi inima interactivă a ecranului, permițând o separare clară între logica de business și prezentarea UI.

- **Creare `OnboardingScreen`: Structura Visuală de Bază.** Vom implementa ecranul principal în `lib/features/onboarding/screens/onboarding_screen.dart`. Acesta va folosi un `Scaffold` cu un `Stack` pentru a suprapune elementele: un fundal animat cu gradient, un `PageView.builder` central pentru conținut și o zonă fixă la bază pentru butoane și indicatorul de progres (`OnboardingProgressDots`). Vom asigura o tranziție fluidă a culorilor de fundal pe măsură ce utilizatorul glisează între pagini. Layout-ul va fi optimizat pentru a evita zonele cu "notches" pe iPhone și ecranele cu perforații pe Android.

- **Implementare `OnboardingStep1`: Privacy First.** Primul pas de onboarding se va concentra pe siguranța datelor. Vom afișa ilustrația `local_privacy.svg` și textele localizate care explică faptul că scanarea contactelor se face 100% local, fără a trimite date pe server. Elementele vizuale vor apărea folosind o animație de tip `FadeIn` pentru a nu obosi utilizatorul. Se va verifica dacă textul are un contrast bun pe gradient-ul de fundal ales pentru acest pas (nuanțe de albastru închis).

- **Implementare `OnboardingStep2`: Backup și Siguranță.** Pasul al doilea prezintă funcționalitatea de backup vCard. Vom folosi ilustrația `secure_backup.svg` și o listă cu bullet points (stilizate cu iconițe de check mici) pentru a enumera beneficiile: istoricul restaurărilor, exportul ușor și protecția împotriva fuziunilor accidentale. Design-ul va fi curat, cu mult spațiu negativ pentru a scoate în evidență punctele cheie.

- **Implementare `OnboardingStep3`: Call to Action și Permisiuni.** Ultimul pas încurajează utilizatorul să înceapă utilizarea aplicației. Ilustrația va fi una optimistă, de tip "all systems go". În locul butoanelor de navigare anterioare, va apărea un `AppPrimaryButton` mare cu textul "Să începem". Apăsarea acestui buton va declanșa prima cerere de permisiune pentru contacte. Se va implementa o micro-animație pe buton pentru a atrage atenția utilizatorului spre acest punct de conversie.

- **Animații de Tranziție între Pagini.** Vom personaliza comportamentul `PageView`-ului folosind `CurvedAnimation` cu o curbă de tip `Curves.easeInOutCubic`. Această tranziție oferă o senzație de "premium" aplicației. Vom implementa și un efect de "parallax" discret: ilustrațiile se vor mișca cu o viteză diferită față de text în timpul glisării, adăugând adâncime vizuală. Se va testa performanța animațiilor pe dispozitive de gamă medie pentru a asigura un framerate constant de 60fps.

- **Implementare Buton "Skip" (Sari Peste).** Pentru utilizatorii care doresc să ajungă rapid la funcționalitate, vom adăuga un buton "Skip" în colțul din dreapta sus. Acesta va fi vizibil doar pe primele două pagini. Apăsarea butonului va trimite utilizatorul direct la ultimul pas sau la Dashboard (după cererea de permisiuni). Butonul va folosi stilul `AppSecondaryButton` dar fără fundal (transparent), pentru a nu concura vizual cu conținutul principal.

- **Creare Widget `OnboardingProgressDots`.** Vom implementa indicatorul de pagină în partea de jos a ecranului. Acesta va consta în 3 puncte mici. Punctul activ va fi mai lung (tip pastilă, 16dp) și va avea culoarea albă, în timp ce punctele inactive vor fi cercuri de 8dp cu opacitate redusă. Schimbarea dimensiunii punctului activ se va face printr-o animație `AnimatedContainer`, oferind un feedback vizual plăcut la schimbarea paginii.

- **Persistență Status Onboarding: `SharedPreferences`.** La finalizarea cu succes a onboarding-ului, vom salva un flag boolean `has_seen_onboarding` în memoria persistentă a dispozitivului folosind `SharedPreferences`. La următoarea deschidere a aplicației, serviciul de rutare va verifica acest flag pentru a decide dacă afișează din nou onboarding-ul sau trimite utilizatorul direct în Dashboard. Acest pas este crucial pentru o experiență de utilizare corectă.

- **Creare `PrivacyPolicyScreen`: Aspect Legal.** Deși este o aplicație locală, transparența este cheia încrederii. Vom crea un ecran dedicat unde utilizatorul poate citi politica de confidențialitate completă. Acesta va fi un `Scaffold` simplu cu un `SingleChildScrollView` care conține textul legal formatat clar. Vom folosi un `AppBar` cu buton de înapoi. Acest ecran va fi accesibil atât din Onboarding (print-un link), cât și din ecranul de Setări.

- **Widget `PrivacyAcceptance`: Checkbox de Confirmare.** Înainte de a intra în aplicație, utilizatorul trebuie să accepte termenii. Vom crea un widget care conține un `Checkbox` și un `RichText`. Textul va include link-uri "clicabile" (folosind `TapGestureRecognizer`) către Politica de Confidențialitate și Termenii de Utilizare. Butonul de "Să începem" va fi activat doar după ce checkbox-ul este bifat, asigurând conformitatea legală (GDPR/CCPA).

- **Integrare `url_launcher`: Link-uri Externe.** Pentru a deschide site-ul oficial al aplicației sau documentația de suport, vom integra pachetul `url_launcher`. Vom scrie un serviciu utilitar `ExternalNavigationService` care gestionează deschiderea în siguranță a link-urilor externe, verificând dacă dispozitivul are un browser instalat. Se va implementa și un dialog de avertizare înainte de a părăsi aplicația ("Ești pe cale să deschizi o pagină web externă").

- **Logica de Redirecționare Automată (Splash -> Target).** Vom actualiza logica din `main.dart` sau de la finalul Splash Screen-ului. Folosind flag-ul de persistență, aplicația va decide instantaneu ruta inițială. Această tranziție trebuie să fie imperceptibilă pentru utilizator. Vom scrie un unit test care simulează ambele stări (onboarding văzut / nevăzut) și verifică dacă rutele generate de sistemul de navigare sunt corecte.

- **Widget Test: `OnboardingScreen`.** Vom scrie teste automate care verifică: 1. Dacă toate paginile sunt afișate corect la swipe. 2. Dacă butoanele "Next" și "Skip" funcționează. 3. Dacă textele se schimbă conform modelului de date. 4. Dacă indicatorul de progres își schimbă poziția. Aceste teste asigură că experiența de "prima impresie" a utilizatorului nu se va strica în urma unor modificări ulterioare de cod.

- **Unit Test: `OnboardingController`.** Testăm logica de business din spatele ecranului: schimbarea stării la apelul metodelor, salvarea flag-ului în `SharedPreferences` și interacțiunea cu `ExternalNavigationService`. Vom folosi `mocktail` pentru a simula dependențele și a verifica dacă metodele sunt apelate cu parametrii corecți.

- **Suport pentru Re-onboarding din Setări.** Adăugăm o opțiune în ecranul de setări care permite utilizatorului să revadă prezentarea aplicației. Aceasta va reseta flag-ul `has_seen_onboarding` și va naviga înapoi la `OnboardingScreen`. Este o funcționalitate utilă pentru utilizatorii care doresc să își reamintească cum funcționează anumite module de siguranță ale aplicației.

- **Rafinare Design: Efect de Parallax.** Vom adăuga ultimele finisaje vizuale ecranului de onboarding. Vom implementa un efect de parallax mai avansat unde elementele de fundal (gradienții sau formele decorative) se mișcă invers față de direcția de scroll. Acest detaliu, deși subtil, ridică standardul vizual al aplicației la nivelul celor mai bune produse de pe piață.

- **Feedback Vizual: Schimbare Gradient.** Vom implementa o logică de interpolare a culorilor (Color Interpolation) între pagini. Pe măsură ce utilizatorul glisează, gradientul de fundal va trece lin de la o nuanță la alta (ex: de la albastru închis la mov intens). Vom folosi `Color.lerp` în interiorul unui `AnimatedBuilder` pentru a obține o tranziție perfectă punct cu punct.

- **Analytics (Anonim): Logare Progres.** Fără a colecta date personale, vom implementa un sistem de logare anonimă pentru a înțelege unde se opresc utilizatorii în procesul de onboarding. Vom folosi un serviciu de analytics minimalist (ex: Firebase Analytics sau o soluție custom) pentru a trimite evenimente precum `onboarding_start`, `onboarding_skip` și `onboarding_complete`. Aceste date ne vor ajuta să optimizăm fluxul în viitor.

- <WORK_CODE>0171-0200 - **Buffer: Optimizare pentru Ecrane Atipice.** Acest bloc de commit-uri este dedicat rezolvării problemelor de layout pe ecrane foarte mici (ex: iPhone SE, Android-uri vechi) sau tablete. Vom asigura că ilustrațiile se redimensionează corect (Responsive Design) și că textele nu sunt tăiate. Vom efectua teste de regresie vizuală pentru a garanta o experiență uniformă pe toată gama de dispozitive suportate.

### Permissions Management (0201 - 0250)
- **Enum `AppPermissionStatus`: Definirea Stărilor de Acces.** Pentru a gestiona fluxul complex de permisiuni (care diferă semnificativ între Android și iOS), vom crea un enum dedicat în `lib/core/permissions/permission_status.dart`. Stările incluse sunt: `initial` (prima rulare), `granted` (acces total), `denied` (utilizatorul a refuzat o dată) și `permanentlyDenied` (utilizatorul a bifat "Nu mai întreba" sau a refuzat de mai multe ori). Această abstractizare ne permite să scriem o logică de UI care reacționează diferit: de la afișarea unui simplu buton, până la un dialog complex care explică cum se activează permisiunea din setările sistemului de operare.

- **Interfață `IPermissionService`: Arhitectură Decuplată.** Respectând principiile SOLID și necesitatea paralelizării, vom defini o interfață abstractă pentru serviciul de permisiuni. Aceasta va conține metode precum `Future<AppPermissionStatus> requestContactsPermission()` și `Future<AppPermissionStatus> checkContactsStatus()`. Prin folosirea unei interfețe, restul aplicației (ex: DashboardController) nu va depinde de pachete externe, facilitând scrierea de teste unitare folosind mock-uri. Această abordare garantează stabilitatea codului chiar dacă decidem să schimbăm biblioteca de gestionare a permisiunilor în viitor.

- **Implementare `ContactsPermissionService`: Integrare `permission_handler`.** Vom crea implementarea concretă a serviciului folosind pachetul lider de piață `permission_handler`. Serviciul va mapa rezultatele brute ale pachetului către enum-ul nostru personalizat `AppPermissionStatus`. Vom implementa o logică specială pentru a detecta corect starea de `permanentlyDenied`, care necesită adesea o verificare suplimentară a istoricului cererilor. Serviciul va fi înregistrat ca un provider singleton în Riverpod pentru a fi accesibil global.

- **Logica Android: Manifest și Cereri Specifice.** Acest task se concentrează pe configurarea specifică platformei Android. Vom adăuga permisiunile `READ_CONTACTS` și `WRITE_CONTACTS` în `AndroidManifest.xml`. Vom implementa și logica de "Permission Rationale" (explicația nevoii de acces) care, pe Android, trebuie afișată înainte ca sistemul să arate prompt-ul nativ pentru a doua oară. Se va asigura compatibilitatea cu noile cerințe de securitate ale Android 13+ legate de permisiunile granulare pentru media (dacă contactele includ poze stocate separat).

- **Logica iOS: `Info.plist` și Mesaje de Utilizare.** Pentru iOS, este obligatoriu să furnizăm un text explicativ clar (Usage Description) în fișierul `Info.plist` pentru cheia `NSContactsUsageDescription`. Vom adăuga acest text în Română și Engleză, explicând utilizatorului exact de ce aplicația are nevoie de acces (ex: "Avem nevoie de acces pentru a identifica și uni contactele duplicate direct pe telefonul tău"). Se va verifica dacă aplicația respectă ghidul Apple "Human Interface Guidelines" pentru cererea permisiunilor, evitând respingerea aplicației din App Store.

- **Tratarea Stării `Permanently Denied`: Dialog către Setări.** Când un utilizator refuză definitiv accesul, aplicația nu mai poate afișa prompt-ul nativ. Vom implementa un dialog de tip "Educational Overlay" care explică situația și oferă un buton "Deschide Setările". Acest buton va apela API-ul nativ `openAppSettings()` pentru a trimite utilizatorul direct la pagina aplicației din setările dispozitivului. Este un pas critic de UX pentru a recupera utilizatorii care au blocat accesul accidental.

- **Permisiunea de Scriere: Cerere Contextuală.** Deși avem nevoie de citire pentru scanare, permisiunea de scriere (`WRITE_CONTACTS` pe Android) este necesară doar în momentul fuziunii efective. Vom implementa o logică de "Just-in-Time Permission" care cere accesul de scriere doar atunci când utilizatorul apasă pentru prima dată pe "Confirmă Fuziunea". Această strategie crește rata de încredere a utilizatorului, cerând permisiuni sensibile doar atunci când beneficiul este imediat și clar.

- **Verificare Startup: Sincronizarea Stării.** La fiecare pornire a aplicației, serviciul de permisiuni va verifica automat statusul curent. Dacă utilizatorul a revocat permisiunea din setările sistemului în timp ce aplicația era închisă, sistemul nostru de navigare va detecta acest lucru și va direcționa utilizatorul către un ecran de "Acces Necesar" în loc de Dashboard. Această verificare continuă previne erorile de tip "crash" la apelarea API-urilor de contacte fără autorizare.

- **`PermissionExplanationDialog`: Design și Argumentare.** Vom crea un dialog personalizat care apare înainte de prompt-ul nativ. Acesta va folosi iconița de brand și va enumera principalele avantaje ale acordării accesului: 1. Scanare rapidă. 2. Identificare duplicate ascunse. 3. Curățare automată. Design-ul va fi unul neintruziv, cu butoane clare: "Înțeleg" și "Mai târziu". Studiile de UX arată că acest tip de "Pre-prompt" crește rata de acceptare a permisiunilor cu peste 30%.

- **Widget `PermissionGuard`: Protecția Interfeței.** Vom implementa un widget de tip wrapper situat în `lib/shared/widgets/permission_guard.dart`. Orice ecran care necesită contacte (Dashboard, Listă, Backup) va fi înfășurat în acest widget. Dacă permisiunea nu este acordată, widget-ul va intercepta randarea și va afișa în schimb un ecran de tip `AppEmptyState` configurat pentru eroare de permisiune, blocând accesul la funcționalitățile indisponibile.

- **Suport pentru `READ_EXTERNAL_STORAGE` (Android Legacy).** Pe versiunile vechi de Android (sub 10), exportul fișierelor vCard în foldere publice necesită permisiunea de stocare. Vom implementa logica de cerere condiționată a acestei permisiuni doar pentru dispozitivele vizate. Pentru versiunile noi, vom folosi `Scoped Storage` (MediaStore API) care nu necesită permisiuni suplimentare pentru fișierele create de aplicația proprie.

- **Error Handling: Gestiunea Cazurilor de Excepție.** Sistemele de operare pot refuza cererea de permisiune din motive externe (mod "Parental Control", restricții de sistem, erori de kernel). Vom implementa un bloc `try-catch` robust în jurul apelurilor de permisiuni, capturând aceste excepții și afișând un mesaj de eroare prietenos utilizatorului. Vom loga aceste erori (fără date personale) pentru a identifica eventuale probleme de compatibilitate pe anumite modele de telefoane.

- **Serviciu Utilitar `SettingsOpener`.** Vom extrage logica de deschidere a setărilor într-un serviciu separat pentru a fi refolosită și în alte contexte (ex: setări de baterie, setări de accesibilitate). Serviciul va detecta platforma și va folosi schemele de URL corecte (`app-settings:` pe iOS). Se va scrie un test manual pentru a verifica dacă funcționează corect pe diverse versiuni de OS.

- **Unit Test: `PermissionService` cu `Mocktail`.** Vom scrie teste unitare exhaustive pentru serviciul de permisiuni. Vom simula toate cele 4 stări ale enum-ului și vom verifica dacă maparea din rezultatele pachetului `permission_handler` se face corect. De asemenea, vom testa dacă serviciul persistă corect informația că a fost făcută deja o primă cerere, esențială pentru logica de `permanentlyDenied`.

- **Integrare Status Permisiuni în Dashboard.** DashboardController va asculta (listen) la schimbările de stare ale serviciului de permisiuni. Dacă accesul este revocat în timp ce aplicația este în fundal, Dashboard-ul se va actualiza instantaneu la revenire, afișând un "Warning Banner" în partea de sus. Acest banner va conține un buton rapid de re-activare, minimizând frecarea în utilizare.

- **Privacy Log: Monitorizarea Accesului.** Vom implementa o logică de logare internă care înregistrează data și ora când utilizatorul a acordat sau revocat permisiunile. Aceste loguri vor fi stocate criptat în `SecureStorage` și vor servi ca dovadă de conformitate cu politicile de confidențialitate. Log-ul nu va conține niciun identificator de utilizator sau locație, respectând principiul "Privacy by Design".

- <WORK_CODE>0217-0250 - **Buffer: Cazuri Particulare și Edge Cases.** Acest interval este rezervat pentru gestionarea unor situații rare, cum ar fi: 1. Dispozitive fără aplicație de contacte instalată. 2. Modul "Guest" pe Android. 3. Permisiuni temporare (One-time permission) introduse în versiunile recente de Android/iOS. Vom asigura că aplicația rămâne stabilă și informativă chiar și în aceste scenarii neobișnuite, oferind mesaje de eroare specifice și utile.

### Data Privacy & Security (0251 - 0300)
- **`SecurityService`: Integritatea Pachetului și Protecție împotriva Piratării.** În acest task, vom implementa `SecurityService` în `lib/core/security/security_service.dart`. Obiectivul principal este verificarea semnăturii pachetului la runtime pe platforma Android. Vom folosi canale native (MethodChannel) pentru a interoga sistemul despre `Signature` sau `SigningInfo`. Această valoare va fi comparată cu o cheie hash (SHA-256) predefinită a certificatului oficial de release. Dacă hash-urile nu coincid, aplicația va intra într-un mod de "blocare" sau va afișa un avertisment critic, prevenind rularea pe versiuni modificate sau repachetate de terți. Această măsură este vitală pentru protejarea proprietății intelectuale și a siguranței datelor utilizatorului, asigurându-ne că nicio persoană nu a injectat cod malițios în aplicație.

- **Detectare Root/Jailbreak: Avertisment de Securitate.** Securitatea contactelor este prioritară. Vom folosi pachetul `flutter_jailbreak_detection` pentru a verifica dacă dispozitivul a fost modificat (Rooted pe Android sau Jailbroken pe iOS). La startup, dacă se detectează un astfel de mediu nesigur, aplicația va afișa un dialog informativ (dar neblocant, conform politicii noastre de utilizare) care explică riscurile: posibilitatea ca alte aplicații cu drepturi de administrator să poată citi datele criptate din memoria securizată a aplicației. Utilizatorul va trebui să confirme că înțelege riscurile înainte de a continua. Se va implementa un flag în `SecurityService` pentru a stoca rezultatul acestei verificări, evitând re-scanarea la fiecare schimbare de ecran.

- **`DataEncryptionService`: Criptarea Bazei de Date Locale.** Pentru a asigura confidențialitatea totală, toate datele cache-uite (duplicatele găsite, istoricul scanărilor) trebuie stocate criptat. Vom folosi pachetul `encrypt` cu algoritmul `AES-256` în mod `CBC` sau `GCM`. Cheia de criptare (32 caractere) va fi generată unic pentru fiecare dispozitiv la prima pornire și va fi stocată în `SecureStorage`. Vom scrie metode de tip "wrapper" în `DataEncryptionService` pentru a cripta string-uri și obiecte JSON înainte de a fi salvate în baza de date locală (Drift/SQLite). Acest nivel de protecție garantează că, chiar dacă un atacator extrage fișierul bazei de date de pe telefon, informațiile vor fi complet ilizibile fără cheia stocată în hardware-ul securizat al dispozitivului.

- **`SecureStorage`: Gestiunea Cheilor Sensibile.** Vom integra pachetul `flutter_secure_storage` pentru a salva flag-uri și chei critice care nu trebuie să fie accesibile prin `SharedPreferences` (care stochează datele în plain text). Aceasta include cheia de criptare a bazei de date, token-urile de sesiune (dacă vor exista) și flag-ul de acceptare a politicilor de securitate. Vom implementa o logică de "fallback" pe Android pentru a folosi `EncryptedSharedPreferences` prin configurarea corectă a `AndroidOptions`. Se va scrie un test de integrare care verifică persistența datelor în `SecureStorage` chiar și după ștergerea cache-ului aplicației, asigurându-ne că utilizatorul nu își pierde accesul la datele criptate anterior.

- **Auto-backup Exclusion și Curățenie la Dezinstalare.** Pe Android, sistemul face automat backup la `SharedPreferences` în Google Cloud. Pentru datele sensibile, acest comportament trebuie dezactivat. Vom configura `android:allowBackup="false"` sau vom folosi un `backup_rules.xml` pentru a exclude explicit folderele care conțin datele utilizatorului. De asemenea, vom implementa o logică care marchează fișierele temporare pentru ștergere imediat ce nu mai sunt necesare (ex: vCard-uri generate pentru share). Această atenție la detalii asigură conformitatea cu standardele de securitate și previne scurgerea accidentală a datelor personale în afara dispozitivului prin mecanismele automate ale sistemului de operare.

- **Audit de Cod: Eliminarea Scurgerilor de Date în Loguri.** Un risc major de securitate îl reprezintă logurile de debug care conțin date reale ale utilizatorului (nume, telefoane). Vom implementa un script de analiză statică (sau un test unitar custom) care scanează codul sursă pentru orice apel de `print()` sau `debugPrint()` care folosește variabile dinamice. Toate logurile de producție vor fi obligatoriu trecute prin `AppLogger`, care are implementat un filtru automat pentru PII (Personally Identifiable Information). Se va efectua o verificare manuală a tuturor claselor de date (Model) pentru a ne asigura că metoda `toString()` nu expune câmpuri sensibile, ci doar un rezumat tehnic al obiectului.

- **Obfuscation Build: Protecția Codului Sursă.** În faza finală de build (Android), vom configura `shrinkResources true` și `minifyEnabled true` în `android/app/build.gradle`. Vom adăuga reguli de `proguard-rules.pro` pentru a exclude de la minificare clasele critice (ex: cele folosite pentru reflexie sau modelele JSON). Această procedură de "obfuscare" face ca ingineria inversă (reverse engineering) să fie mult mai dificilă, transformând numele claselor și metodelor în caractere ilizibile. Vom verifica dacă build-ul obfuscate funcționează corect, testând în special zonele care folosesc serializare JSON (unde minificarea poate rupe maparea câmpurilor dacă nu este configurată corect).

- **Detectarea Capturilor de Ecran (Android/iOS).** Deși nu este un blocaj critic, aplicația va implementa o logică de avertizare sau de blurare a ecranului atunci când utilizatorul face o captură de ecran în zonele sensibile (ex: Merge Detail). Pe Android, vom folosi `WindowManager.LayoutParams.FLAG_SECURE` pentru a preveni total capturile de ecran pe anumite ecrane. Pe iOS, unde acest lucru nu este permis nativ, vom asculta evenimentul `userDidTakeScreenshotNotification` și vom afișa un mesaj de informare despre bunele practici de siguranță a datelor. Acest detaliu tehnic adaugă un strat suplimentar de protecție a intimității utilizatorului.

- **Unit Test: `SecurityService` și Integritate.** Vom scrie teste unitare care simulează eșecul verificării semnăturii și detectarea unui dispozitiv root-at. Vom verifica dacă `SecurityService` raportează corect aceste stări și dacă restul aplicației reacționează conform specificațiilor (afișarea dialogurilor de avertizare). De asemenea, vom testa dacă criptarea și decriptarea datelor prin `DataEncryptionService` este consistentă: datele decriptate trebuie să fie identice cu cele originale la nivel de bit (byte-perfect).

- **Audit de Securitate Final: Faza 1.** Ultima etapă a acestei faze presupune o revizuire completă a tuturor măsurilor de securitate implementate (0151-0259). Vom folosi instrumente de tip "linter" specifice pentru securitate (ex: `dart_code_metrics` cu reguli de security) și vom verifica dacă nicio permisiune inutilă nu a fost adăugată în manifest. Se va genera un raport tehnic intern care confirmă că toate datele PII sunt manipulate exclusiv în memorie sau stocate criptat, pregătind aplicația pentru auditul tehnic necesar publicării în magazinele oficiale de aplicații.
- **GDPR Compliance: Transparență și Control Local.** Deși aplicația nu trimite date în cloud, conformitatea cu GDPR este esențială pentru încrederea utilizatorului. Vom implementa un ecran "Datele Tale" în setări, care explică detaliat principiul stocării locale. Vom folosi un limbaj non-tehnic pentru a descrie cum sunt salvate duplicatele temporare și unde se află fișierele de backup. Acest ecran va include și un buton de "Export Audit Log", care generează un fișier text cu toate acțiunile de sistem efectuate de aplicație (fără PII), oferind utilizatorului control total și transparență asupra modului în care aplicația îi procesează agenda.

- **Unit Test: `DataEncryptionService` - Cazuri Limită.** Vom scrie teste unitare riguroase pentru serviciul de criptare. Vom verifica: 1. Comportamentul sistemului la încercarea de decriptare cu o cheie greșită (trebuie să arunce o excepție specifică `EncryptionException`). 2. Criptarea unor string-uri vide sau extrem de lungi (peste 1MB). 3. Rezistența la caractere speciale și seturi de caractere non-UTF8. Aceste teste asigură că motorul de securitate al aplicației este robust și nu va corupe datele utilizatorului în scenarii neprevăzute.

- **`LocalWipeService`: Ștergerea Completă a Datelor.** Pentru utilizatorii care doresc să curețe orice urmă a aplicației de pe dispozitiv, vom implementa funcția "Wipe All Data". Aceasta va șterge recursiv folderul `ApplicationDocumentsDirectory`, va goli toate bazele de date SQLite și va apela `storage.deleteAll()` pentru `SecureStorage`. Această acțiune va fi protejată de un dialog de confirmare de tip "Hard Confirm" (utilizatorul trebuie să introducă cuvântul "STERG" pentru a confirma). Este o funcționalitate obligatorie pentru respectarea "dreptului de a fi uitat" (Right to be Forgotten).

- **Securizarea Canalelor Native (MethodChannel Security).** Toate comunicările între Flutter și codul nativ (Android/iOS) pentru servicii sensibile (permisiuni, securitate) vor fi securizate. Vom implementa o logică de validare a originii apelului, asigurându-ne că nicio altă aplicație malițioasă instalată pe dispozitiv nu poate invoca metodele noastre native prin canalele Flutter. Vom folosi identificatori unici de sesiune pentru fiecare apel și vom valida semnătura procesului apelant acolo unde sistemul de operare permite acest lucru.

- **Audit Permisiuni: Eliminarea `READ_PHONE_STATE`.** Un audit atent al permisiunilor va fi efectuat pentru a ne asigura că aplicația nu cere permisiuni inutile care ar putea speria utilizatorul. Vom verifica în special dacă nu s-a infiltrat permisiunea `READ_PHONE_STATE` (adesea adăugată automat de unele biblioteci de analytics), care permite citirea ID-ului unic al telefonului (IMEI). Vom folosi `tools:node="remove"` în `AndroidManifest.xml` pentru a exclude explicit orice permisiune nedorită adusă de dependențele third-party.

- **Implementare `PrivacyManager`: Dashboard-ul Intimității.** Vom crea un widget care afișează utilizatorului "Amprenta de Date" a aplicației: cât spațiu ocupă baza de date criptată, câte fișiere de backup există și data ultimei curățări de cache. Acest widget va fi plasat în ecranul de Setări sub secțiunea "Securitate". Vizualizarea clară a faptului că datele ocupă doar câțiva KB și sunt stocate local întărește promisiunea de privacy a brandului nostru.

- **Criptarea Fișierelor de Log (Internal Audit).** Chiar și logurile tehnice (erori de sistem, timpi de procesare) vor fi stocate criptat pe disc. Vom folosi un `RotatingLogStrategy` care păstrează doar ultimele 7 zile de activitate și șterge automat restul. Această măsură previne situația în care un atacator cu acces fizic la telefon ar putea deduce structura agendei utilizatorului analizând mesajele de eroare din logurile aplicației.

- **Verificarea Surselor de Instalare (Installer Store Validation).** Pe Android, vom implementa o verificare care detectează dacă aplicația a fost instalată din Google Play Store sau a fost "side-loaded" (instalat dintr-un APK descărcat de pe net). În cazul instalărilor side-load, aplicația va afișa un avertisment de securitate sporit, deoarece integritatea pachetului nu poate fi garantată de magazinul oficial. Vom folosi `getInstallerPackageName()` din API-ul de Package Manager pentru această verificare.

- **Optimizarea `SecureStorage` pentru iOS (Keychain Access Groups).** Vom configura corect accesul la Keychain pe iOS pentru a ne asigura că datele sensibile sunt protejate de nivelul de securitate `kSecAttrAccessibleAfterFirstUnlock`. Această setare permite aplicației să acceseze cheile de criptare în fundal (necesar pentru scanări periodice sau backup-uri automate) doar după ce utilizatorul a deblocat telefonul cel puțin o dată după restart, oferind un echilibru perfect între securitate și utilitate.

- **Consolidare Faza 1: Raport Final de Securitate.** Înainte de a trece la Faza 2 (Modele de Date și Servicii Native), vom genera un document de audit intern care mapează fiecare cerință de securitate din `technical-plan.md` la implementarea efectivă din cod. Vom rula o suită completă de teste de integrare (UI Tests) pe un dispozitiv real pentru a confirma că fluxul Onboarding -> Permisiuni -> Dashboard este impecabil și că nicio scurgere de date nu este detectabilă prin instrumentele standard de monitorizare a traficului de rețea. Acest raport va servi drept fundament pentru orice viitoare certificare de securitate a aplicației.
- **Modelarea Entității `AppContact`: Definirea Atributelor de Bază.** Vom crea clasa `AppContact` în `lib/core/models/app_contact.dart` folosind pachetul `freezed` pentru imuabilitate și generare automată de cod (`copyWith`, `toMap`, `fromMap`). Această entitate va abstractiza contactul nativ și va include: `id` (unic, de tip String), `displayName`, `firstName`, `lastName`, `initials` și o listă de numere de telefon (`AppPhone`). Vom implementa o logică de normalizare a numelui la nivel de model, asigurându-ne că spațiile de prisos sunt eliminate. Această clasă va fi baza pentru toți algoritmii de comparare, necesitând o structură extrem de curată și performantă pentru a fi procesată rapid în liste de mii de elemente. Se va acorda atenție specială modului în care `firstName` și `lastName` sunt extrase din `displayName` în cazul în care acestea lipsesc individual de la nivel nativ.

- **Modelarea Entității `AppPhone` și `AppEmail`: Structură Granulară.** Contactele au multiple puncte de contact. Vom defini clasele `AppPhone` și `AppEmail` în fișiere separate sub folderul `lib/core/models/`. Acestea vor conține valoarea (telefon/email) și o etichetă (`label`) care poate fi una predefinită (Home, Work, Mobile) sau una custom. Pentru numerele de telefon, vom implementa o metodă `normalizedValue` care elimină toate caracterele non-numerice (ex: "+40 722-123.456" devine "40722123456"). Această normalizare este critică pentru algoritmul de potrivire exactă a duplicatelor. Se va asigura că modelele suportă serializarea JSON pentru a putea fi salvate în cache-ul local criptat. Vom adăuga și un flag `isPrimary` pentru a marca metoda preferată de contact a utilizatorului.

- **Modelarea Entității `DuplicateGroup`: Structurarea Rezultatelor Detecției.** Grupurile de duplicate vor fi reprezentate de clasa `DuplicateGroup`. Aceasta va conține un ID unic al grupului, o listă de `AppContact` identificate ca duplicate, un scor de încredere (0.0 - 1.0) și o etichetă pentru motivul grupării (ex: "Nume și Telefon identice"). Vom implementa o metodă `primaryContact` care determină automat cel mai complet contact din grup (cel cu cele mai multe câmpuri populate), propunându-l ca bază pentru fuziune. Această structură facilitează randarea UI-ului în liste grupate. Se va include și un câmp `ignoredAt` pentru a marca grupurile pe care utilizatorul a decis să le păstreze separate.

- **Definirea `ConfidenceScore` și Tipuri de Potrivire (Matching Types).** Vom crea un enum `MatchType` (Exact, Partial, Fuzzy) și o logică de calcul a scorului de încredere în `lib/core/algorithms/scoring_logic.dart`. Un grup cu `MatchType.Exact` va avea un scor de 1.0, în timp ce unul bazat pe algoritmul Levenshtein va avea un scor proporțional cu distanța dintre string-uri. Vom implementa constante de prag (thresholds) în `lib/core/constants/matching_constants.dart` pentru a decide când un contact este considerat "Sigur", "Probabil" sau "Necesită verificare manuală". Această logică va fi documentată tehnic pentru a permite ajustarea ușoară a sensibilității motorului de match.

- **Interfața `IContactService`: Abstractizarea Accesului Nativ pentru Testabilitate.** Pentru a permite lucrul în paralel și testarea fără un dispozitiv real, vom defini `IContactService` în `lib/core/services/contact_service.dart`. Interfața va include metode asincrone precum `fetchContacts()`, `updateContact(AppContact contact)`, `deleteContact(String id)` și `getContactById(String id)`. Această abordare ne permite să creăm un `MockContactService` pentru mediul de dezvoltare, care returnează o listă predefinită de 50 de contacte cu diverse tipuri de duplicate, accelerând procesul de implementare a UI-ului fără a depinde de performanța bridge-ului nativ în faza de design.

- **Implementare `NativeContactService`: Integrare cu Pachetul `flutter_contacts`.** Vom crea implementarea reală a serviciului folosind pachetul `flutter_contacts`. Serviciul va gestiona conversia complexă între obiectele native ale sistemului de operare (Android/iOS) și modelele noastre `AppContact`. Vom acorda o atenție deosebită performanței, folosind parametrul `withProperties: true` doar atunci când este strict necesar, pentru a reduce timpul de citire a agendei. Se va implementa un mecanism de "paging" sau "batching" pentru a nu bloca firul principal de execuție la procesarea a mii de contacte, folosind `Isolates` dacă este necesar.

- **Normalizarea Datelor la Citire: Curățarea Agendei la Intrare.** În momentul în care datele sunt extrase din sistemul de operare, serviciul va aplica o serie de reguli de curățare: eliminarea diacriticelor, transformarea în lowercase pentru comparare (stocând totuși varianta originală pentru UI) și formatarea unitară a numerelor de telefon. Această etapă de "sanitization" este fundamentul pe care se construiește acuratețea algoritmului de detecție duplicate. Se vor elimina caracterele invizibile (ZWS) sau simbolurile de control care pot apărea la sincronizări defectuoase între conturile de Google și iCloud.

- **Gestiunea Pozei de Profil (Avatars) Nativ cu Lazy Loading.** Citirea pozelor de profil de la contacte este o operațiune costisitoare din punct de vedere al memoriei. `NativeContactService` va implementa o metodă separată `fetchContactPhoto(String id)` care încarcă imaginea (Uint8List) la cerere. Vom folosi un sistem de cache în memorie (eviction policy: LRU) pentru a stoca ultimele imagini afișate, asigurând un scroll fluid în listă fără a re-interoga sistemul de operare pentru fiecare cadru. Imaginile vor fi re-dimensionate la dimensiunea de 150x150dp la citire pentru a optimiza consumul de RAM.

- **Unit Test: `AppContact` Mapping, Normalizare și Serializare.** Vom scrie teste unitare care verifică dacă transformarea din obiectul nativ `Contact` în `AppContact` se face fără pierderi de date. Vom testa în special cazurile cu nume foarte lungi, caractere speciale din diverse limbi și formate de telefon exotice. Se va verifica dacă metoda `normalizedValue` produce același rezultat pentru formatele "+40722..." și "0722...", esențial pentru succesul fuziunii. De asemenea, vom testa dacă obiectele pot fi convertite în JSON și invers fără a pierde tipurile de date complexe.

- **Consolidare Modele: Faza 2 Startup și Verificarea Performanței.** Această etapă finalizează definirea structurilor de date. Toate modelele vor fi verificate pentru a suporta `Serialization` și `Deserialization` corect, pregătind integrarea cu baza de date locală. Vom rula o suită de teste de performanță (benchmark) pentru a vedea cât timp durează crearea a 5000 de obiecte `AppContact` în memorie, asigurându-ne că ne încadrăm în limitele de memorie ale dispozitivelor low-end. Se va monitoriza în special consumul de memorie al listelor de numere de telefon și email-uri asociate fiecărui contact.

- <WORK_CODE>0281-0300 - **Buffer: Adaptare la Schemele Native Specifice Android/iOS.** Fiecare sistem de operare are câmpuri proprii (ex: "Phonetic Name" pe iOS sau "Starring/Favorite" pe Android). În acest interval de commit-uri, vom extinde modelele noastre pentru a suporta aceste câmpuri "vendor-specific" fără a rupe compatibilitatea cross-platform. Obiectivul este ca aplicația să nu piardă nicio informație fină în timpul procesului de fuziune, păstrând toate metadatele pe care utilizatorul le-a adăugat de-a lungul anilor în agenda sa, inclusiv grupările native sau notele ascunse.

---

## FAZA 2: Modele de Date, Servicii Native și Baza de Date (Commit-uri 0301 - 0450)

### Data Models & Entities (0301 - 0350)
- **Modelarea Entității `AppContact`: Definirea Atributelor de Bază.** Vom crea clasa `AppContact` în `lib/core/models/app_contact.dart` folosind pachetul `freezed` pentru imuabilitate și generare automată de cod (`copyWith`, `toMap`, `fromMap`). Această entitate va abstractiza contactul nativ și va include: `id` (unic, de tip String), `displayName`, `firstName`, `lastName`, `initials` și o listă de numere de telefon (`AppPhone`). Vom implementa o logică de normalizare a numelui la nivel de model, asigurându-ne că spațiile de prisos sunt eliminate. Această clasă va fi baza pentru toți algoritmii de comparare, necesitând o structură extrem de curată și performantă pentru a fi procesată rapid în liste de mii de elemente. Se va acorda o atenție deosebită modului în care `firstName` și `lastName` sunt extrase din `displayName` în cazul în care acestea lipsesc individual de la nivel nativ.

- **Modelarea Entității `AppPhone`: Valorile și Etichetele Numerelor.** Vom defini clasa `AppPhone` care conține valoarea numerică a telefonului și o etichetă (Home, Work, Mobile). Pentru numerele de telefon, vom implementa o metodă `normalizedValue` care elimină toate caracterele non-numerice (ex: "+40 722-123.456" devine "40722123456"). Această normalizare este critică pentru algoritmul de potrivire exactă a duplicatelor. Se va asigura că modelele suportă serializarea JSON pentru a putea fi salvate în cache-ul local criptat. Vom adăuga și un flag `isPrimary` pentru a marca metoda preferată de contact a utilizatorului, util în procesul de fuziune automată.

- **Modelarea Entității `AppEmail`: Validarea și Categorizarea.** Similar cu telefoanele, clasa `AppEmail` va stoca adresa de email și tipul acesteia. Vom include o metodă de validare de bază (`isValid`) bazată pe un regex simplu, pentru a avertiza utilizatorul în legătură cu datele posibil greșite din agenda sa. Adresele de email vor fi normalizate intern prin trecerea în lowercase și eliminarea spațiilor de la început și final. Această structură unitară facilitează identificarea duplicatelor bazate pe identitatea digitală a contactului, completând informațiile oferite de numărul de telefon.

- **Modelarea Entității `AppAddress`: Structura Adreselor Fizice.** Vom implementa clasa `AppAddress` care sparge o adresă în componentele sale standard: `street`, `city`, `state`, `postalCode` și `country`. Această abordare granulară este esențială deoarece contactele sincronizate din surse diferite (ex: Outlook vs Gmail) stochează adesea adresele în formate incompatibile. Vom adăuga o metodă `toSingleLine()` care returnează o variantă formatată a adresei pentru a fi afișată ușor în listele de previzualizare.

- **Modelarea Entității `AppPhoto`: Gestiunea Datelor Binare.** Pozele de contact vor fi gestionate prin clasa `AppPhoto`. Aceasta va conține un `Uint8List` cu bytes-ii imaginii (pentru afișare rapidă) și un `localPath` către folderul de cache al aplicației (unde imaginea este salvată permanent). Vom implementa o logică de "lazy initialization", unde imaginea este încărcată de pe disc doar atunci când widget-ul avatar este vizibil pe ecran, minimizând amprenta de memorie a aplicației în timpul procesării listelor lungi de duplicate.

- **Modelarea Entității `DuplicateGroup`: Agregarea Rezultatelor.** Grupurile de duplicate vor fi reprezentate de clasa `DuplicateGroup`. Aceasta va conține un ID unic al grupului, o listă de `AppContact` identificate ca duplicate, un scor de încredere (0.0 - 1.0) și o etichetă pentru motivul grupării (ex: "Nume și Telefon identice"). Vom implementa o metodă `primaryContact` care determină automat cel mai complet contact din grup (cel cu cele mai multe câmpuri populate), propunându-l ca bază pentru fuziune. Această structură facilitează randarea UI-ului în liste grupate și permite utilizatorului să ignore întreg grupul cu o singură acțiune.

- **Implementare `ContactMapper`: Conversia din Formate Native.** Vom crea o clasă utilitară situată în `lib/core/mappers/contact_mapper.dart` responsabilă de transformarea hărților brute (`Map<String, dynamic>`) primite prin `MethodChannel` în obiectele noastre `AppContact`. Mapper-ul va gestiona diferențele de denumire a câmpurilor între Android și iOS, asigurând un model de date unic în interiorul Flutter. Se va folosi un bloc `try-catch` pentru fiecare câmp pentru a asigura că o eroare de conversie la un singur contact nu blochează procesarea întregii agende.

- **Implementare `ContactMapper`: Conversia spre Formate Native.** În oglindă cu pasul anterior, mapper-ul va implementa metoda `toNativeJson`. Aceasta va pregăti datele pentru a fi trimise către sistemul de operare în vederea actualizării sau creării de contacte noi. Se va acorda o atenție deosebită tipurilor de date cerute de API-urile native (ex: `Long` pentru ID-uri pe Android sau `String` pe iOS), prevenind erorile de tip "invalid argument" la nivel de bridge.

- **Logica `isReadOnly`: Detectarea Contactelor Protejate.** Nu toate contactele pot fi modificate sau șterse (ex: contacte din WhatsApp, LinkedIn sau directoare corporative pe Android). Vom implementa un flag calculat `isReadOnly` care detectează sursa contactului. Dacă un contact este marcat ca read-only, UI-ul va dezactiva opțiunea de fuziune pentru acel grup sau va avertiza utilizatorul că poate păstra informația dar nu poate șterge sursa originală. Această logică este vitală pentru stabilitatea aplicației și respectarea restricțiilor de sistem.

- **Identificarea `accountType`: Sursa Datelor.** Vom adăuga suport pentru detectarea tipului de cont asociat fiecărui contact (Google: 'com.google', iCloud: 'com.apple.contacts', SIM, local). Această informație va fi afișată în UI sub forma unei mici iconițe lângă numele contactului, ajutând utilizatorul să înțeleagă de unde provin duplicatele (ex: de ce are același om salvat pe SIM și în Google). Maparea acestor string-uri native către un enum intern va simplifica logica de filtrare în Dashboard.

- **Enum `DuplicateReason`: Clasificarea Motivelor de Match.** Vom defini motivele pentru care două contacte sunt considerate duplicate (ex: `exactPhone`, `fuzzyName`, `sameEmail`, `sharedAddress`). Aceste motive vor fi populate de motorul de detecție și folosite în UI pentru a explica utilizatorului decizia aplicației. Fiecare motiv va avea o prioritate internă care contribuie la calculul scorului de încredere final al grupului.

- **Modelul `MatchResult`: Rezultatul Comparației Individuale.** Vom crea o clasă care stochează rezultatul comparării a două contacte specifice. Aceasta va conține scorul numeric (0-100) și o listă de obiecte `DuplicateReason`. Folosirea acestui model intermediar permite depanarea ușoară a algoritmilor de match, oferind o urmă clară a modului în care s-a ajuns la o anumită concluzie tehnică.

- **Generarea Automată a Metodelor Imutabile cu `Freezed`.** Toate entitățile create vor fi adnotate cu `@freezed`. Vom rula `build_runner` pentru a genera fișierele `.freezed.dart` și `.g.dart`. Această tehnică asigură că datele noastre sunt imuabile pe tot parcursul ciclului de viață, prevenind erorile de tip "side-effect" în UI și simplificând integrarea cu providerii de stare (Riverpod/Bloc) care se bazează pe compararea referințelor obiectelor.

- **Validări de Model: Integritatea Datelor.** Vom implementa logici de validare în constructorii modelelor (folosind `assert` sau validări personalizate). Ne vom asigura că un `AppContact` nu poate fi instanțiat fără un ID valid și că are cel puțin un nume de afișat. Aceste constrângeri programatice la nivel de model previn propagarea obiectelor incomplete ("zombie objects") către baza de date sau către ecranele de UI, reducând numărul de crash-uri neașteptate.

- **Utilizatorul de Comparare: `ContactComparisonService`.** Vom crea un utilitar care returnează o hartă a diferențelor între două versiuni ale aceluiași contact. Acest serviciu va fi folosit în ecranul de "Merge Detail" pentru a sublinia câmpurile care urmează să fie actualizate. Se va implementa o logică de comparare profundă (deep equality) pentru liste (telefoane, email-uri), identificând exact ce elemente sunt noi, ce elemente dispar și ce s-a modificat.

- **Unit Test: `ContactMapper` - Stabilitatea Conversiei.** Vom scrie teste unitare care hrănesc mapper-ul cu diverse seturi de date native: hărți perfecte, hărți cu chei lipsă, hărți cu valori null sau tipuri de date greșite (ex: un String unde se aștepta un Int). Vom verifica dacă mapper-ul oferă valori default sensibile (ex: listă goală în loc de null pentru telefoane), asigurând o fundație de date robustă pentru restul aplicației.

- **Unit Test: `AppContact` - Egalitate și HashCode.** Vom verifica dacă logica de egalitate generată automat funcționează corect. Două obiecte `AppContact` create separat dar cu aceleași date trebuie să fie considerate egale (`operator ==`). Acest test este esențial pentru funcționarea corectă a seturilor (`Set<AppContact>`) și a mapărilor folosite intens în algoritmii de detecție a duplicatelor.

- <WORK_CODE>0318-0350 - **Buffer: Extinderea Câmpurilor Sociale și Note.** Acest interval este rezervat pentru a adăuga suport pentru câmpuri de date mai puțin comune, dar prezente în agendele utilizatorilor: ID-uri de rețele sociale (Twitter, Facebook), porecle (nicknames), site-uri web și note text lungi. Vom asigura că toate aceste informații pot fi capturate, comparate și unite fără pierderi în timpul procesului de curățare, oferind o soluție completă de management al contactelor.

### Native Services (Android & iOS) (0351 - 0400)
- **MethodChannel Setup: Puntea între Flutter și Nativ.** Vom stabili canalul principal de comunicare în `lib/core/services/native_bridge.dart` sub numele `ro.contacteduplicate.app/contacts`. Acest task include definirea listei de metode permise: `fetchContacts`, `updateContact`, `deleteContact`, `getContactPhoto`. Vom asigura utilizarea unui `BinaryCodec` sau a unei serializări JSON eficiente pentru a minimiza latența la transferul unor volume mari de date. Este primul pas tehnic pentru a părăsi mediul izolat Flutter și a interacționa cu sistemele de operare Android și iOS.

- **Android Implementation: Integrare `ContactsContract`.** Vom trece în mediul Kotlin (`android/app/src/main/kotlin/...`) pentru a implementa apelurile către `ContactsContract` de la Google. Vom folosi un `ContentResolver` pentru a interoga baza de date a contactelor. Logica va include citirea ID-urilor, numelor de afișare și flag-urilor de tip "favorite". Se va implementa o metodă de interogare optimizată (Projection) pentru a citi doar coloanele necesare, evitând consumul inutil de memorie și timp de procesare pe dispozitive cu mii de contacte.

- **Android Implementation: Citirea Numerelor și Email-urilor.** Extindem logica Kotlin pentru a citi detaliile secundare (telefoane, email-uri) folosind un al doilea cursor sau o interogare de tip `JOIN` pe `ContactsContract.CommonDataKinds`. Vom mapa etichetele native (ex: `TYPE_MOBILE`, `TYPE_WORK`) către string-uri pe care Flutter le poate interpreta. Vom gestiona cu atenție permisiunile de runtime la nivel nativ, returnând erori specifice către Flutter dacă accesul este refuzat în mijlocul operațiunii.

- **iOS Implementation: Integrare `Contacts.framework`.** Vom implementa echivalentul în Swift (`ios/Runner/...`) folosind framework-ul modern `Contacts`. Vom crea un `CNContactStore` și vom folosi `CNContactFetchRequest` pentru a extrage contactele. Vom defini una listă de chei (keys to fetch) care include: `givenName`, `familyName`, `phoneNumbers`, `emailAddresses`. Swift oferă o API mai curată decât Android, dar necesită o gestionare atentă a thread-urilor pentru a nu bloca interfața utilizatorului (Main Thread).

- **iOS Implementation: Conversia la Formate Native Swift.** Vom implementa logica de transformare a obiectelor `CNContact` în dicționare (`[String: Any]`) care pot fi trimise înapoi prin `MethodChannel`. Vom mapa corect etichetele iOS (ex: `_$!<Home>!$_`) către formate lizibile. O provocare majoră va fi gestionarea contactelor sincronizate prin iCloud, asigurându-ne că ID-ul persistent al contactului este capturat corect pentru a fi folosit ulterior la operațiunile de fuziune.

- **Android Implementation: Actualizarea Contactelor (Write).** Revenim la Kotlin pentru a implementa logica de scriere. Vom folosi `ContentProviderOperation` pentru a asigura tranzacționalitatea operațiunilor de update. Dacă o fuziune presupune adăugarea unui număr de telefon nou la un contact existent, aceasta se va face într-o singură tranzacție atomica. Acest pas este critic: o eroare aici poate corupe agenda utilizatorului, deci vom implementa validări riguroase înainte de a executa `applyBatch`.

- **Android Implementation: Ștergerea Contactelor.** Vom implementa metoda de ștergere la nivel nativ. Aceasta va primi ID-ul contactului și va apela `resolver.delete`. Vom adăuga o verificare de siguranță: aplicația nu va putea șterge contacte marcate ca "ReadOnly" (din surse precum WhatsApp). Rezultatul operațiunii (succes/eșec) va fi raportat către Flutter împreună cu un cod de eroare detaliat în caz de insucces (ex: permisiuni insuficiente sau disc plin).

- **iOS Implementation: Actualizarea și Ștergerea (CNContactStore).** Vom implementa `CNSaveRequest` în Swift pentru a trimite modificările către agenda Apple. iOS necesită o instanță de `CNMutableContact` pentru editare. Vom gestiona conflictele de scriere (concurrency) care pot apărea dacă utilizatorul modifică manual un contact în aplicația nativă în timp ce noi procesăm fuziunea în fundal. Se va asigura că utilizatorul este notificat dacă este necesară re-autentificarea iCloud pentru anumite acțiuni.

- **Gestiunea Imaginilor (Thumbnail Fetching).** Vom implementa o metodă optimizată pentru a citi doar miniaturile pozelor de contact. Pe Android, vom folosi `ContactsContract.DisplayPhoto.CONTENT_URI`. Pe iOS, vom folosi proprietatea `thumbnailImageData`. Imaginile vor fi returnate ca `Uint8List` către Flutter. Vom implementa un sistem de limitare a dimensiunii (throttle) pentru a nu trimite prea multe date simultan prin bridge, prevenind blocarea aplicației.

- **Native Error Handling: Coduri de Eroare Standardizate.** Vom defini o clasă de excepții în Flutter care mapează erorile native. Pe Android, capturăm `SQLException` sau `SecurityException`. Pe iOS, capturăm `CNError`. Toate acestea vor fi traduse în erori inteligibile pentru utilizator în UI (ex: "Memorie insuficientă pe dispozitiv" în loc de "OS error 13"). Această abstractizare crește calitatea experienței de depanare și ușurează implementarea dialogurilor de eroare.

- **Thread Management: Background execution pe Nativ.** Este esențial ca toate operațiunile pe agendă să ruleze pe thread-uri secundare (Worker Thread în Android, Global Queue în iOS). Vom implementa logica de asincronism la nivel nativ pentru a ne asigura că `MethodChannel` returnează controlul către Flutter imediat, iar rezultatul este trimis doar după finalizarea procesării. Acest detaliu tehnic elimină micro-întreruperile (jank) din interfață în timpul scanărilor masive.

- **Detectarea Schimbărilor (Change Listeners).** Vom implementa un mecanism de ascultare a modificărilor în agenda nativă. Pe Android, vom folosi un `ContentObserver`. Pe iOS, vom asculta notificarea `CNContactStoreDidChange`. Atunci când agenda se schimbă extern (utilizatorul adaugă un contact nou), aplicația va fi notificată și va declanșa o scanare parțială de actualizare, asigurând că Dashboard-ul reflectă mereu realitatea agendei.

- **iOS Configuration: Permisiuni de Scriere Avansate.** Apple necesită permisiuni suplimentare pentru a modifica contactele. Vom configura `Info.plist` pentru a include descrierile corecte pentru toate acțiunile. Vom verifica dacă aplicația are acces la `CNEntityTypeContacts` cu drepturi de scriere. Orice refuz al utilizatorului va fi capturat și va activa automat starea de "Read Only" în aplicația noastră, dezactivând funcțiile de merge dar păstrând cele de scanare.

- **Android Configuration: `contacts.xml` și Integrarea în Sistem.** Vom configura aplicația pentru a fi recunoscută de Android ca o utilitate de management al contactelor. Vom adăuga metadate în `AndroidManifest.xml` pentru a permite utilizatorului să lanseze aplicația direct din ecranul de detalii al unui contact nativ (opțional). Această integrare profundă face ca aplicația să se simtă ca o extensie naturală a sistemului de operare, nu ca un instrument izolat.

- **Unit Test Nativ (Kotlin/Swift).** Deși majoritatea testelor sunt în Flutter, vom scrie câteva teste unitare specifice limbajelor native pentru a verifica logicile de conversie a datelor. Vom testa în Kotlin funcția de curățare a proiecțiilor SQL și în Swift logica de extragere a etichetelor de telefon. Această teste asigură că fundamentul nativ al aplicației este stabil înainte de a fi apelat de către Flutter.

- **Test de Stabilitate a Bridge-ului: Date Masive.** Vom rula un test de stres care trimite 5000 de hărți de contacte prin `MethodChannel`. Vom monitorizarea consumul de RAM în Xcode Profiler și Android Studio Profiler pentru a ne asigura că nu există pierderi de memorie (Memory Leaks) la nivelul bridge-ului nativ. Orice vârf de memorie va fi adresat prin optimizarea serializării sau prin trimiterea datelor în bucăți (chunks).

- **Unit Test: `NativeContactsService` - Simulare Bridge.** Vom folosi `setMockMethodCallHandler` din Flutter pentru a simula comportamentul sistemelor native în mediul de testare Dart. Vom crea scenarii de succes, scenarii cu erori de permisiuni și scenarii cu date corupte. Acest test ne permite să verificăm dacă wrapper-ul nostru Flutter tratează corect toate răspunsurile posibile de la codul nativ, fără a avea nevoie de un emulator pornit.

- <WORK_CODE>0368-0400 - **Buffer: Compatibilitate cu Producători (Samsung, Huawei).** Acest interval este rezervat pentru ajustări specifice producătorilor de Android care folosesc implementări non-standard ale `ContactsContract`. Vom testa aplicația pe dispozitive Samsung (care au câmpuri proprii de grupuri) și Huawei (fără Google Services), adaptând bridge-ul nativ pentru a funcționa uniform pe orice variantă de Android, garantând o experiență stabilă pentru toți utilizatorii.

### Local Database (0401 - 0450)
- **Drift Setup: Arhitectura Bazei de Date.** Vom începe implementarea stratului de persistență prin configurarea pachetului `drift` în proiect. Acest task presupune adăugarea dependențelor în `pubspec.yaml` (`drift`, `sqlite3_flutter_libs`) și a uneltelor de generare cod în `dev_dependencies` (`drift_dev`, `build_runner`). Vom crea structura de foldere `lib/core/database/` și fișierul `app_database.dart`. Vom defini clasa `AppDatabase` care extinde `$AppDatabase` (generată ulterior) și vom configura constructorul pentru a folosi `LazyDatabase`. Alegerea Drift este motivată de suportul excelent pentru `Streams`, permițând UI-ului să se actualizeze automat când datele din baza de date se schimbă, fără a re-interoga manual tabelele.

- **Schema `ContactsCache`: Definirea Tabelei de Oglindire.** Vom defini tabelul `ContactsCache` în Drift. Această tabelă are rolul de a stoca o copie locală ("mirror") a contactelor native pentru a permite procesarea lor rapidă și offline. Coloanele vor include: `id` (Text, Primary Key), `rawJson` (Text - stochează obiectul `AppContact` serializat), `displayNameNormalized` (Text, pentru căutare rapidă), `phoneNumberNormalized` (Text, indexat), și `lastScannedAt` (DateTime). Vom folosi adnotări pentru a asigura că `normalized` fields sunt mereu la zi, facilitând algoritmi de detecție duplicate ultra-rapizi care nu necesită decodarea JSON-ului pentru fiecare comparație.

- **Schema `DuplicateGroups`: Gestiunea Rezultatelor Scanării.** Vom crea tabelul `DuplicateGroups` pentru a stoca grupurile de contacte identificate ca fiind similare. Coloanele esențiale: `id` (Int, AutoIncrement), `groupHash` (Text, unic - pentru a evita duplicarea grupurilor la scanări repetate), `matchType` (Enum/Text: Exact, Fuzzy, etc.), `confidenceScore` (Real), și `status` (Enum: Pending, Ignored, Merged). De asemenea, vom avea o tabelă de legătură `DuplicateGroupMembers` care asociază `groupId` cu `contactId` din cache. Această structură relațională permite interogări complexe, cum ar fi "afișează toate grupurile care conțin contacte cu numere de telefon diferite".

- **Schema `BackupHistory`: Urmărirea Copiilor de Rezervă.** Pentru a oferi utilizatorului siguranța că poate reveni la o stare anterioară, vom implementa tabelul `BackupHistory`. Acesta va stoca metadatele fiecărui fișier vCard exportat: `id` (Int, AutoIncrement), `filePath` (Text), `fileName` (Text), `contactCount` (Int), `fileSize` (Int, în bytes), și `createdAt` (DateTime). Vom adăuga o coloană `isAutoBackup` pentru a diferenția între backup-urile făcute manual de utilizator și cele create automat de aplicație înainte de o fuziune. Se va implementa o logică de "self-cleaning" care păstrează doar ultimele 20 de înregistrări pentru a economisi spațiu.

- **Schema `AppSettings`: Persistența Configurărilor.** Vom crea o tabelă simplă de tip `key-value` numită `AppSettings` pentru a stoca preferințele utilizatorului care trebuie să fie mai sigure decât în `SharedPreferences`. Coloanele: `key` (Text, Unique) și `value` (Text/Blob). Aici vom salva setări precum: `last_scan_date`, `auto_scan_enabled`, `preferred_language` și `theme_mode`. Folosirea bazei de date pentru setări permite tranzacții atomice (ex: actualizarea setărilor odată cu finalizarea unei scanări), asigurând că starea aplicației este mereu consistentă.

- **`DatabaseService`: Implementarea Accesului Singleton.** Vom crea un serviciu wrapper în `lib/core/services/database_service.dart` care gestionează ciclul de viață al bazei de date. Acesta va asigura că există o singură instanță de `AppDatabase` deschisă (Singleton pattern) pentru a preveni erorile de tip "Database is locked" în SQLite. Serviciul va include metode pentru deschiderea securizată a conexiunii, gestionarea erorilor de I/O și închiderea corectă a bazei de date la închiderea aplicației. Vom integra aici și logica de criptare a bazei de date (folosind `SQLCipher` dacă cerințele de securitate o impun).

- **`ContactDao`: Implementarea Logică a Operațiunilor pe Contacte.** Vom dezvolta primul Data Access Object (DAO) dedicat cache-ului de contacte. Metodele incluse: `upsertContacts(List<ContactCacheCompanion> contacts)` (folosind `onConflict: DoUpdate`), `deleteContact(String id)`, `watchAllContacts()` (pentru UI reactiv) și `findContactByPhone(String phone)`. Vom implementa interogările folosind fluent API-ul Drift, asigurându-ne că toate operațiunile masive de scriere sunt executate în interiorul unei tranzacții pentru a maximiza viteza (până la 10x mai rapid decât inserările individuale).

- **`GroupDao`: Gestiunea Grupurilor de Duplicate.** Acest DAO va conține logica de manipulare a grupurilor de duplicate. Metode critice: `createGroupWithMembers(DuplicateGroup group, List<String> contactIds)`, `getPendingGroups()`, `markGroupAsMerged(int groupId)` și `ignoreGroup(int groupId)`. Vom implementa o metodă de interogare complexă care face `join` între `DuplicateGroups`, `GroupMembers` și `ContactsCache` pentru a returna obiecte `DuplicateGroup` complete (cu toate datele contactelor membre) direct către UI.

- **`BackupDao`: Interogări pentru Istoricul de Backup.** Vom implementa logica de acces pentru tabelul de backup. Metode: `addBackupEntry(BackupHistoryCompanion entry)`, `getBackupsSortedByDate()`, și `findBackupByPath(String path)`. Vom adăuga o metodă specială `deleteOldBackups(int keepCount)` care rulează periodic și elimină înregistrările vechi atât din baza de date, cât și fișierele fizice de pe disc asociate acestora, prevenind umplerea memoriei telefonului.

- **Schema Migration: Strategia de Upgrade.** Este vital să putem actualiza schema bazei de date în viitor fără a șterge datele utilizatorului. Vom implementa `MigrationStrategy` în Drift. Vom defini versiunea 1 a schemei și vom pregăti funcția `onUpgrade`. Aceasta va conține un bloc `switch` pe versiuni, unde vom adăuga logică pentru `addColumn` sau `createTable` pentru versiunile viitoare (2, 3, etc.). Vom activa și `beforeOpen: (details) async { await customStatement('PRAGMA foreign_keys = ON'); }` pentru a asigura integritatea legăturilor între tabele.

- **Wipe Service: Resetarea Completă a Bazei de Date.** Vom crea o metodă utilitară în `DatabaseService` care permite ștergerea tuturor datelor din toate tabelele. Această acțiune este necesară pentru funcționalitatea de "Wipe All Data" din setări sau pentru a curăța cache-ul la cererea utilizatorului. Vom folosi comenzi SQL de tip `DELETE FROM table` urmate de `VACUUM` pentru a elibera spațiul de stocare la nivelul sistemului de operare. Se va asigura că această operațiune se face într-o tranzacție sigură.

- **SQL Indexes: Optimizarea Performanței de Căutare.** SQLite poate deveni lent în tabele cu mii de rânduri dacă nu are indexurile corecte. Vom adăuga explicit indexuri (`CREATE INDEX`) pe coloanele: `ContactsCache.displayNameNormalized`, `ContactsCache.phoneNumberNormalized` și `DuplicateGroups.status`. Această optimizare tehnică va reduce timpul de căutare a unui duplicat de la O(n) la O(log n), asigurând o interfață rapidă chiar și pentru utilizatorii cu peste 5000 de contacte în agendă.

- **Transactions: Asigurarea Atomicității Datelor.** Orice operațiune care modifică mai multe tabele (ex: crearea unui grup de duplicate și actualizarea statusului contactelor membre) trebuie să fie atomică. Vom implementa utilizarea `transaction(() async { ... })` din Drift. Dacă orice pas eșuează (ex: eroare de scriere pe disc), întreaga tranzacție va fi anulată (`rollback`), prevenind stările de date inconsistente (ex: un grup care referă contacte care nu mai există).

- **Unit Test: `ContactDao` - Testarea Persistenței.** Vom scrie teste unitare exhaustive pentru DAO-ul de contacte. Vom testa: 1. Inserarea unei liste mari de contacte (batch insert). 2. Actualizarea unui contact existent (upsert). 3. Verificarea fluxului de date prin `watchAllContacts` (folosind `expectLater` pe stream). 4. Ștergerea unui contact și verificarea dispariției lui din baza de date. Aceste teste garantează că stratul de date este robust și nu produce pierderi de informații.

- **Integration Test: Verificarea Migrării Schemei.** Vom scrie un test de integrare complex care: 1. Creează o bază de date cu schema versiunii 1. 2. Inserează date de test. 3. Rulează codul de migrare către versiunea 2 (simulată). 4. Verifică dacă datele originale sunt încă prezente și corecte în noile tabele/coloane. Acest test este asigurarea noastră că utilizatorii nu își vor pierde istoricul de scanări sau setările la viitoarele actualizări ale aplicației în Store.

- **DB Stress Test: Benchmark de Performanță.** Vom scrie un script de benchmark care generează 10.000 de contacte random și le inserează în baza de date. Vom măsura timpul total de inserare (țintă sub 500ms) și timpul de interogare a duplicatelor (țintă sub 50ms). Dacă rezultatele depășesc aceste praguri, vom ajusta `PRAGMA` settings ale SQLite (ex: `journal_mode = WAL`, `synchronous = NORMAL`) pentru a optimiza viteza de scriere pe dispozitivele mobile.

- **Auto-backup DB: Siguranță Suplimentară.** (Opțional dar recomandat) Vom implementa o logică care, o dată la 7 zile, face o copie a fișierului `.sqlite` într-un folder secundar (internal storage). În cazul în care baza de date principală se corupe (ex: din cauza unei opriri bruște a telefonului în timpul scrierii), aplicația va încerca să restaureze datele din acest backup intern, asigurând continuitatea serviciului fără a frustra utilizatorul.
- <WORK_CODE>0418-0450 - Buffer: optimizare query-uri de tip `JOIN` pentru statistici complexe pe dashboard.

---

## FAZA 3: Algoritmi de Normalizare și Detecție Duplicate (Commit-uri 0451 - 0600)

### Normalization Service (0451 - 0500)
- **`NormalizationService`: Arhitectură și Interfață.** În această etapă, definim serviciul care va asigura consistența datelor pe tot parcursul procesului de detecție. În `lib/core/normalization/normalization_service.dart`, vom crea o interfață cu metode abstracte pentru normalizarea numerelor de telefon, adreselor de email și numelor persoanelor. Această abordare decuplată permite testarea unitară riguroasă a fiecărui algoritm fără a depinde de restul aplicației. Serviciul va fi baza pe care se sprijină motorul de detecție duplicate, garantând că "ion popescu" și "Ion Popescu" sunt văzuți ca identici.

- **Normalizarea Numerelor de Telefon: Curățarea Regex.** Vom implementa logica de eliminare a zgomotului din numerele de telefon folosind un regex robust `r'[^0-9+]'`. Această funcție va elimina parantezele, spațiile, cratimele și punctele, păstrând doar cifrele și simbolul `+`. Este primul pas crucial pentru a aduce toate numerele într-un format comparabil, indiferent de modul în care utilizatorul le-a tastat inițial în agenda sa.

- **Normalizarea Prefixelor RO (+40).** Pentru utilizatorii din România, este comun să salveze numerele începând cu `07`. Vom implementa o logică inteligentă care detectează aceste numere și le prefixează intern cu `+40` pentru a asigura compatibilitatea cu numerele salvate în format internațional. Această transformare se va face doar în scopul comparării, păstrând varianta originală pentru afișarea în UI, evitând astfel confuzia utilizatorului.

- **Conversia Prefixului 00 în Formatul Internațional.** Vom adăuga o regulă de normalizare care transformă prefixul `00` de la începutul numerelor în simbolul `+`. Această standardizare este esențială deoarece ambele formate sunt folosite interschimbabil pe Android și iOS, iar motorul nostru de match trebuie să le recunoască drept identice pentru a nu rata duplicatele salvate diferit.

- **Gestiunea Numerelor Scurte și de Urgență.** Nu toate numerele trebuie normalizate agresiv. Vom implementa o excepție pentru numerele cu mai puțin de 5 cifre (ex: 112, 955, numere scurte de servicii). Acestea vor fi păstrate exact așa cum sunt, prevenind adăugarea eronată a prefixelor de țară care ar putea face numărul inutilizabil în timpul procesului de merge sau apelare.

- **Normalizarea Email-urilor: lowercase și Gmail logic.** Pentru adresele de email, vom folosi `toLowerCase().trim()`. Vom implementa și o opțiune avansată pentru a ignora punctele din interiorul numelui de utilizator pentru domeniile gmail.com (ex: `ion.popescu@gmail.com` este identic cu `ionpopescu@gmail.com`), oferind o detecție de duplicate superioară oricărei aplicații native de contacte.

- **Normalizarea Numelor: Gestiunea Spațiilor Multiple.** Utilizatorii pot introduce accidental mai multe spații între nume și prenume. Vom folosi regex-ul `r'\s+'` pentru a înlocui orice succesiune de spații cu unul singur. Această curățenie de bază asigură că algoritmul de `Exact Match` nu eșuează din cauza unor greșeli invizibile de tastare.

- **Compararea Case Insensitive a Numelor.** Vom implementa o logică care transformă toate string-urile de nume în `toLowerCase()` înainte de orice operațiune de comparare. Această măsură elimină necesitatea ca utilizatorul să fie consecvent în folosirea majusculelor, asigurând că "Marius" și "marius" sunt identificate corect ca fiind aceeași persoană.

- **Eliminarea Diacriticelor din Nume.** Folosind `String.replaceAllMapped` și un dicționar de mapare, vom implementa funcția care transformă caracterele românești (ă, î, ș, ț, â) în echivalentele lor simple (a, i, s, t, a). Această normalizare este vitală pentru utilizatorii din România, deoarece agenda lor conține adesea un amestec de contacte scrise cu și fără diacritice.

- **Detecția Inversării Nume/Prenume (Swap Detection).** Un duplicat comun este acela în care numele și prenumele sunt inversate (ex: "Popescu Ion" vs "Ion Popescu"). Vom implementa un algoritm care sparge numele în cuvinte, le sortează alfabetic și le reunește înainte de comparare. Această tehnică identifică duplicatele pe care o căutare textuală simplă le-ar trece cu vederea.

- **Curățarea Caracterelor Speciale din Nume.** Vom implementa o regulă de eliminare a simbolurilor care nu au ce căuta într-un nume de contact (ex: `*`, `&`, `_`, `#`). Adesea, aceste simboluri sunt folosite de utilizatori pentru a "marca" contactele, dar ele împiedică detecția automată. Prin eliminarea lor în faza de normalizare, creștem acuratețea motorului de scoring.

- **Algoritmul Soundex pentru Limba Română.** Vom implementa o variantă personalizată a algoritmului Soundex care codifică numele bazat pe pronunție, adaptat pentru particularitățile fonetice ale limbii române. Aceasta va permite găsirea duplicatelor scrise cu greșeli de ortografie comune, oferind o sugestie de tip "Fuzzy Match" cu un grad ridicat de precizie.

- **Unit Test: Matricea de Test pentru Numere de Telefon.** Vom scrie un test unitar care rulează 50 de variante diferite ale aceluiași număr (cu spații, puncte, prefixe variate) prin serviciul de normalizare și verifică dacă toate produc același rezultat unic. Acesta este testul suprem de consistență pentru motorul nostru de date.

- **Unit Test: Verificarea Cazurilor Speciale de Email.** Vom testa normalizarea email-urilor cu seturi de date care includ caractere non-ascii, puncte multiple și domenii de top-level neobișnuite, asigurându-ne că motorul de curățare este robust și nu distruge adresele valide.

- **Unit Test: Nume Compuse și Diacritice.** Vom verifica dacă numele compuse cu cratimă (ex: "Popa-Anghel") sau cele cu diacritice multiple sunt normalizate corect în ambele limbi (RO/EN). Testul va confirma că logica de transformare este reversibilă și consistentă în orice condiții.

- **Test de Performanță: Normalizare în Masă.** Vom măsura timpul necesar pentru normalizarea a 10.000 de intrări de contacte. Obiectivul tehnic este ca această operațiune să dureze sub 200ms pe un dispozitiv mediu, pentru a asigura o experiență de utilizare fluidă și o pornire rapidă a procesului de scanare.

- **Maparea Prefixelor Internaționale Comune.** Vom extinde serviciul de normalizare cu un tabel de prefixe pentru țările vecine României (HU, BG, RS, MD). Această logică va ajuta utilizatorii care au contacte internaționale în agendă să beneficieze de același nivel de precizie în detecția duplicatelor ca și pentru numerele locale.

- **Smart Trim: Eliminarea Titlurilor Profesionale.** Vom implementa o regulă care detectează și elimină opțional titlurile precum "Dr.", "Ing.", "Prof." de la începutul câmpului de nume în timpul comparării. Aceste prefixe onorifice sunt adesea cauza pentru care contactele identice nu sunt văzute ca duplicate de către algoritmii simpli.

- <WORK_CODE>0469-0500 - **Buffer: Gestiunea Formatului "LastName, FirstName".** Acest interval de commit-uri este rezervat pentru a gestiona formatele de nume inversate prin virgulă, comune în agendele exportate din Outlook sau Gmail, asigurându-ne că motorul nostru de normalizare le aduce la un format standardizat înainte de fuziune.

### Duplicate Detection Logic (0501 - 0550)
- **`DuplicateDetectorService`: Arhitectură și Strategie.** Vom implementa motorul central de detecție în `lib/core/algorithms/duplicate_detector_service.dart`. Acest serviciu va prelua lista de `AppContact` normalizate și va orchestra apelurile către diverși sub-algoritmi (Exact Match, Fuzzy Match, Cross-Field Match). Rezultatul va fi o listă de `DuplicateGroup`, fiecare având un scor de încredere calculat. Vom folosi un sistem de "pondere" (weights) configurabil, permițând ajustarea sensibilității motorului fără a modifica codul sursă. Această arhitectură modulară ne permite să adăugăm noi criterii de potrivire în viitor cu un efort minim.

- **Exact Match Phone: Identificarea Duplicatelor după Număr.** Vom implementa primul algoritm de detecție: compararea numerelor de telefon normalizate. Dacă două contacte au cel puțin un număr de telefon identic în formatul internațional `+40...`, grupul va primi un scor de încredere de 100%. Acest criteriu este considerat cel mai sigur ("Safe Match") deoarece probabilitatea ca două persoane diferite să împartă același număr de telefon privat este extrem de mică. Se va asigura că normalizarea prefixelor este aplicată înainte de această etapă pentru a nu rata potrivirile între formate locale și internaționale.

- **Exact Match Email: Identificarea după Adresa de Email.** Similar cu telefoanele, compararea adreselor de email normalizate oferă o precizie ridicată. Vom atribui un scor de 100% dacă email-urile coincid exact. Vom implementa o logică care ignoră diferențele de tip "Home" vs "Work", concentrându-se doar pe string-ul adresei. Acest algoritm este vital pentru curățarea contactelor de business, unde utilizatorii salvează adesea aceeași persoană cu mai multe email-uri, creând intrări separate în agendă.

- **Exact Match Name: Identificarea după Nume Complet.** Dacă două contacte au numele complet identic (după eliminarea diacriticelor și a spațiilor multiple), ele vor fi grupate cu un scor de 90%. Deși numele pot fi identice pentru persoane diferite (ex: "Ion Popescu"), în contextul unei agende personale private, probabilitatea de duplicat este foarte mare. Vom adăuga o verificare secundară: dacă numele coincid dar au numere de telefon diferite, scorul va fi redus la 70%, marcându-l ca "Necesită verificare manuală".

- **Levenshtein Distance: Detecția Numelor Apropiate.** Pentru a găsi duplicatele cu greșeli de tipar (ex: "Marius Antonecu" vs "Marius Antonescu"), vom implementa algoritmul Levenshtein. Vom calcula distanța de editare între numele normalizate. Dacă distanța este mică (ex: 1 sau 2 caractere pentru nume lungi), vom atribui un scor de încredere bazat pe raportul dintre distanță și lungimea string-ului. Această tehnică "Fuzzy Match" este esențială pentru a atinge obiectivul de scor de curățenie de 95+, găsind duplicatele ascunse pe care nicio căutare alfabetică nu le poate evidenția.

- **Multi-factor Match: Corelarea Metadatelor Suplimentare.** Vom implementa un sistem de bonusuri de scor bazat pe alte câmpuri. De exemplu, dacă două contacte au aceeași companie (`+10 puncte`) sau aceeași adresă fizică (`+5 puncte`), scorul lor de încredere va crește. Această abordare multi-dimensională permite o detecție mult mai fină decât o simplă comparație de nume, ajutând la consolidarea contactelor care au fost adăugate fragmentat de-a lungul timpului.

- **Partial Phone: Match pe Ultimele 9 Cifre.** Utilizatorii salvează adesea numerele fără prefixul de țară sau cu prefixe diferite. Vom implementa o logică care extrage ultimele 9 cifre ale numerelor normalizate. Dacă acestea coincid, vom atribui un scor de 70%. Această detecție parțială este utilă pentru a găsi contacte care sunt probabil identice, dar au fost salvate în formate neconforme (ex: cu `00` sau direct cu `0`).

- **Weight Configuration: Ponderi Ajustabile.** Vom permite configurarea importanței fiecărui criteriu de match printr-un obiect de tip `MatchSettings`. Dezvoltatorii (și ulterior utilizatorii avansați) pot decide dacă numele este mai important decât compania în procesul de calcul al scorului. Această flexibilitate face ca motorul de detecție să fie adaptabil pentru diverse piețe sau tipuri de utilizatori (ex: corporate vs personal).

- **Confidence Levels: Clasificarea Rezultatelor.** Grupurile identificate vor fi mapate pe trei nivele de încredere: `Safe` (95-100%, pot fi unite automat), `Probable` (80-94%, necesită o confirmare rapidă) și `Manual` (60-79%, necesită verificarea fiecărui câmp). Această clasificare va fi reflectată în UI prin culorile widget-ului `StatusBadge`, ghidând utilizatorul spre acțiunile cele mai sigure.

- **Transitive Logic: Închiderea Tranzitivă a Grupurilor.** Vom implementa un algoritm de tip "Union-Find" pentru a consolida legăturile între contacte. Dacă A este duplicat cu B, iar B cu C, sistemul trebuie să le prezinte pe toate trei ca un singur grup de fuziune. Această logică tranzitivă previne fuziunile parțiale și asigură o curățare completă a agendei dintr-o singură mișcare.

- **Memory Indexing: Optimizarea cu `HashMap`.** Pentru a evita o complexitate de O(n^2), vom folosi `HashMap`-uri pentru a indexa contactele după telefon și email. Astfel, în loc să comparăm fiecare contact cu toate celelalte, vom putea găsi instantaneu contactele care au aceleași date de bază. Această optimizare tehnică este vitală pentru a menține viteza de procesare pe agende cu mii de contacte.

- **False Positive Filter: Lista de Excludere.** Există cuvinte comune care pot părea nume dar nu sunt (ex: "Taxi", "Pizza", "Mama", "Service"). Vom implementa un filtru care reduce scorul de match dacă ambele contacte folosesc doar aceste etichete generice fără alte date de identificare. Aceasta previne fuziunea eronată a serviciilor diferite salvate sub nume similare.

- **Whitelist Service: Gestiunea Deciziilor Utilizatorului.** Dacă utilizatorul decide că două contacte marcate ca "Probabile" nu sunt duplicate, sistemul trebuie să țină minte acest lucru. Vom implementa o tabelă `IgnoredMatches` în baza de date locală. Motorul de detecție va verifica această listă, asigurându-se că nu va mai propune aceleași perechi de contacte la scanări ulterioare.

- **Match Reason Generator: Explicații pe Înțelesul Tuturor.** Aplicația trebuie să explice de ce a considerat două contacte ca fiind duplicate. Vom implementa un serviciu care generează string-uri localizate precum "Telefoane identice", "Email identic" sau "Nume foarte similare". Aceste mesaje vor fi afișate în `MergeDetailScreen`, oferind utilizatorului contextul necesar pentru a lua o decizie informată.

- **Algorithm Matrix Test: Matricea de Validare.** Vom scrie un test de integrare masiv care rulează motorul de detecție peste un set de date pre-definit de 100 de perechi (duplicate și non-duplicate). Vom măsura `Precision` (rata de succes) și `Recall` (rata de acoperire), asigurându-ne că orice modificare ulterioară a algoritmilor nu scade calitatea detecției.

- **Confidence Test: Verificarea Limitării Scorului.** Vom scrie unit teste care verifică dacă scorul calculat rămâne mereu în intervalul [0, 100], indiferent de câte bonusuri sau penalizări sunt aplicate. Este un test de siguranță matematică pentru a preveni erori de logică în formula de agregare a scorului de încredere.

- **Complexity O(n log n): Analiza și Optimizarea Scalabilității.** Vom analiza teoretic și practic performanța motorului. Vom implementa strategii de "Early Exit" (oprirea comparației dacă scorul depășește deja 100%) și vom asigura că algoritmul de sortare a grupurilor este eficient. Obiectivul este ca aplicația să nu "înghețe" chiar dacă utilizatorul are o agendă extrem de mare și fragmentată.

- **Large Set Benchmark: Stres Test cu 5000 Contacte.** Vom rula un benchmark automatizat care simulează 5000 de contacte cu 500 de grupuri de duplicate. Vom măsura timpul de execuție și consumul de memorie în `Isolate`, asigurându-ne că aplicația se încadrează în bugetul de resurse al unui telefon de gamă medie (mid-range).

- **Chunk Processing: Procesarea în Blocuri.** Pentru a menține stabilitatea pe iOS (unde procesele de fundal au limite stricte de memorie), vom implementa procesarea listei în blocuri de 100 de contacte. Vom trimite progresul către UI după fiecare bloc procesat, oferind utilizatorului o bară de progres reală și predictibilă.

- **Photo Hash Match: Detecție Vizuală (Opțional).** Ca o funcționalitate premium/avansată, vom explora posibilitatea de a compara pozele de profil folosind un algoritm de tip `perceptual hash`. Dacă două contacte au poze identice sau foarte similare, scorul lor de încredere va crește semnificativ, ajutând la identificarea duplicatelor care nu au nicio altă informație textuala comună.

- <WORK_CODE>0521-0550 - **Buffer: Rafinarea Scenariilor Complexe.** Acest interval este dedicat testării motorului cu agende reale, extrem de dezordonate (ex: contacte importate de 5 ori din surse diferite). Vom ajusta ponderile scorurilor și vom introduce reguli speciale pentru a minimiza intervenția manuală a utilizatorului, tinzând spre automatizarea totală a procesului de curățare.

### Background Processing (0551 - 0600)
- **Configurare `Isolate`: Procesare în Fundal.** Operațiunile de detecție duplicate pot fi extrem de costisitoare pentru procesor, ducând la blocarea interfeței (jank). Vom implementa motorul de scanare întrun `Isolate` separat. Acest task presupune configurarea punctului de intrare pentru Isolate și utilizarea funcției `compute()` din Flutter pentru sarcini scurte sau `Isolate.spawn` pentru procesul principal de scanare a întregii agende. Această separare tehnică garantează că animațiile din UI rămân fluide (60fps) chiar și atunci când algoritmul procesează mii de contacte în spate.

- **`ScanController`: Gestiunea Stărilor de Scanare.** Vom crea un controller de tip `StateNotifier` în `lib/features/dashboard/controllers/scan_controller.dart`. Acesta va gestiona mașina de stări a procesului de scanare: `idle` (pregătit), `scanning` (în lucru), `paused`, `completed` (finalizat) și `error`. Controller-ul va expune o variabilă reactivă `progress` (0.0 la 1.0) care va fi legată direct de `ProgressRing` din Dashboard. Se va implementa o logică de "de-bouncing" pentru a nu emite prea multe actualizări de progres care ar putea suprasolicita UI-ul.

- **Port Communication: Mesageria între Izolate.** Vom folosi `ReceivePort` și `SendPort` pentru a stabili un canal de comunicare bidirecțional între Isolate-ul de scanare și cel de UI. Isolate-ul de scanare va trimite mesaje periodice care conțin numărul de contacte procesate, numărul de duplicate găsite până în acel moment și, la final, lista completă de `DuplicateGroup`. UI-ul va putea trimite mesaje de "stop" sau "pause" către Isolate. Această arhitectură de mesagerie asincronă este modelul recomandat de Flutter pentru performanță maximă pe mobil.

- **Pause/Resume Logic: Controlul Execuției.** Vom implementa un mecanism de control în bucla de scanare a Isolate-ului. La fiecare pas, algoritmul va verifica un flag de tip `AtomicBoolean` sau va asculta un mesaj de control de pe portul de comunicare. Dacă este detectată o cerere de pauză, execuția va fi suspendată până la primirea semnalului de resume. Acest nivel de control este esențial pentru o experiență de utilizator fluidă, permițându-i să prioritizeze alte sarcini pe telefon fără a închide complet procesul de curățare.

- **Android WorkManager: Scanare Periodică Silențioasă.** Vom integra pachetul `workmanager` pentru a programa scanări automate la intervale de 24 de ore. Aceste scanări vor rula doar dacă dispozitivul îndeplinește anumite condiții (Constraints): să fie conectat la o sursă de alimentare și să aibă o conexiune WiFi activă. Rezultatul scanării va fi salvat în baza de date locală, urmând ca utilizatorul să fie notificat dacă au fost găsite duplicate noi, crescând astfel utilitatea aplicației pe termen lung.

- **iOS Background Fetch: `BGTaskScheduler`.** Pentru platforma iOS, vom implementa suportul pentru `Background Tasks`. Vom înregistra un task de tip `App Refresh` care permite aplicației să ruleze scurte sesiuni de scanare în fundal (aproximativ 30 de secunde). Vom optimiza algoritmul pentru a procesa doar contactele modificate de la ultima scanare, respectând limitele stricte de execuție impuse de Apple pentru a proteja viața bateriei.

- **`LocalNotificationService`: Informarea Utilizatorului.** Vom crea un serviciu pentru notificări locale în `lib/core/services/notification_service.dart`. Acesta va folosi pachetul `flutter_local_notifications`. Când o scanare de fundal identifică duplicate, aplicația va afișa o notificare cu un mesaj localizat precum "Agenda ta are nevoie de atenție! Am găsit 5 duplicate noi". Apăsarea notificării va deschide aplicația direct în ecranul de Listă Duplicate.

- **Battery Check: Optimizarea Consumului.** Scanarea contactelor și procesarea fuzzy pot fi mari consumatoare de energie. Vom integra pachetul `battery_plus` pentru a monitoriza starea bateriei. Dacă nivelul scade sub 15% în timpul unei scanări active, aplicația va pune automat procesul pe pauză și va informa utilizatorul printr-un mesaj discret. Această abordare "eco-friendly" este esențială pentru a nu degrada experiența generală de utilizare a dispozitivului.

- **Unit Test: `ScanController` - Verificarea Progresului.** Vom scrie un test unitar care simulează scanarea unei liste de 100 de contacte mock. Vom verifica dacă variabila `progress` trece prin toate etapele (de la 0.01 la 1.0) și dacă starea finală este corect marcată ca `completed`. Acest test garantează că feedback-ul vizual oferit utilizatorului reflectă realitatea procesării din fundal.

- **Dashboard Integration: Feedback Vizual în Timp Real.** Vom implementa o animație de tip "puls" pe widget-ul `ProgressRing` din Dashboard atunci când o scanare este în desfășurare. De asemenea, vom afișa un text dinamic sub cerc, indicând numărul de contacte procesate în timp real (ex: "Procesăm: 1,245 / 5,000"). Acest feedback continuu oferă o senzație de viteză și eficiență, reducând timpul perceput de așteptare.

- <WORK_CODE>0561-0600 - **Buffer: Persistența Stării Parțiale.** Acest bloc de commit-uri este dedicat gestionării cazului în care utilizatorul închide forțat aplicația (Force Close) în timpul unei scanări. Vom implementa o logică de salvare a progresului în baza de date SQLite, astfel încât la următoarea deschidere scanarea să poată fi reluată din punctul în care a rămas, fără a procesa din nou toate contactele.

---

## FAZA 4: Interfață Utilizator - Dashboard și Liste (Commit-uri 0601 - 0800)

### Dashboard Screen (0601 - 0650)
- **`DashboardController`: Agregarea Datelor Reactivă.** Vom implementa controller-ul principal al ecranului Dashboard folosind Riverpod (`StateNotifierProvider`). Acesta va avea rolul de a asculta simultan fluxurile de date (Streams) de la `ContactDao` (pentru numărul total de contacte) și `GroupDao` (pentru numărul de grupuri de duplicate găsite). Starea controller-ului va fi un obiect `DashboardState` care conține: `totalContacts`, `duplicateCount`, `isScanning` și `lastScanDate`. Folosirea stream-urilor asigură că Dashboard-ul se actualizează instantaneu atunci când o scanare de fundal finalizează un nou grup, fără a necesita reîncărcarea manuală a ecranului.

- **UI Header: Design Adaptiv cu `SliverAppBar`.** Dashboard-ul va folosi un header modern implementat cu `SliverAppBar`. Atunci când utilizatorul face scroll, titlul mare "Salut, [Nume]!" se va micșora și se va muta în bara de navigare superioară. Vom configura o înălțime extinsă de 120dp (`expandedHeight`) și vom adăuga o animație discretă de "fade-out" pe avatarul utilizatorului din colț în timpul scroll-ului. Fundalul header-ului va avea un gradient subtil care se aliniază cu identitatea vizuală a aplicației, oferind o senzație de profunzime și rafinament încă de la prima interacțiune.

- **Stats Widget 1: Cardul Total Contacte.** Vom implementa primul widget informativ din Dashboard: un card compact care afișează numărul total de contacte detectate în agendă. Acesta va folosi widget-ul `AppCard` și va include o iconiță `Icons.people_outline` de culoare albastră. Cifra va fi stilizată cu `AppTextStyles.h1` pentru impact maxim. Sub cifră, vom afișa textul localizat "Contacte detectate". Se va implementa o micro-animație la prima încărcare, unde numărul crește rapid de la 0 la valoarea reală (Counter Animation).

- **Stats Widget 2: Cardul Grupuri de Duplicate.** Cel de-al doilea card de statistici se va concentra pe problema principală: duplicatele. Cifra afișată va folosi culoarea `AppColors.error` (roșu) pentru a atrage atenția utilizatorului asupra numărului de grupuri care necesită atenție. Iconița folosită va fi `Icons.copy_all_rounded`. Dacă cifra este 0, cardul va trece automat într-o stare de "Succes" cu o iconiță verde și textul "Agenda este curată", oferind un feedback vizual pozitiv utilizatorului pentru efortul depus.

- **Scan Button: Punctul Central de Conversie.** În centrul Dashboard-ului, vom plasa butonul principal de acțiune: "Începe Scanarea". Acesta va fi un `AppPrimaryButton` cu o dimensiune extinsă și o iconiță de căutare (`Icons.search`). Butonul va fi însoțit de un text descriptiv dedesubt (ex: "Scanarea durează aproximativ 30 de secunde"). Vom implementa o logică care schimbă textul butonului în "Continuă Scanarea" dacă a fost detectată o scanare întreruptă anterior, ghidând utilizatorul să finalizeze procesul început.

- **Recent Groups: Caruselul Grupurilor Găsite.** Sub butoanele de statistici, vom adăuga o secțiune "Găsite Recent" implementată cu un `ListView.horizontal`. Aceasta va afișa sub formă de carduri mici ultimele 5 grupuri de duplicate identificate. Fiecare card va arăta mini-avatarele contactelor implicate și scorul de încredere. Utilizatorul poate face swipe orizontal pentru a vedea rapid rezultatele scanării, oferind o scurtătură către procesul de fuziune direct de pe ecranul principal.

- **Savings Card: Motivarea prin Rezultate.** Pentru a crește retenția utilizatorilor, vom implementa un widget care estimează beneficiile curățării agendei. Acesta va calcula: 1. Spațiul de stocare potențial eliberat (bazat pe dimensiunea medie a unui contact vCard). 2. Timpul economisit la căutare (estimat). Mesajul va fi de tipul "Poți elibera 2.4 MB și economisi 15 minute/lună". Acest element de "gamification" transformă o sarcină plictisitoare (curățarea agendei) într-o activitate cu beneficii clare și măsurabile.

- **Backup Status: Indicatorul de Siguranță.** Un element critic în Dashboard este cardul care indică starea ultimei copii de rezervă. Acesta va folosi o logică de culori semantică: `Verde` dacă există un backup mai recent de 7 zile, `Portocaliu` dacă backup-ul este vechi și `Roșu` dacă nu a fost făcut niciodată niciun backup. Cardul va include un buton rapid "Fă backup acum", asigurându-ne că utilizatorul este mereu protejat înainte de a efectua operațiuni masive de fuziune a contactelor.

- **Shimmer Dashboard: Experiența de Încărcare Fluidă.** Vom implementa efectul de "Skeleton Loading" pentru întregul Dashboard. În timpul primei citiri a contactelor native (care poate dura câteva secunde), în locul cardurilor reale vor fi afișate versiuni pulsante (gri deschis) care imită structura layout-ului final. Această tehnică reduce rata de abandon, confirmând utilizatorului că aplicația funcționează și că datele se încarcă, spre deosebire de un simplu indicator de progres circular care oferă mai puțin context.

- **Pull to Refresh: Sincronizarea Manuală.** Deși avem ascultători de schimbări native, vom implementa și gestul standard de "Pull to Refresh" (`RefreshIndicator`) pe Dashboard. La declanșare, aplicația va forța o re-citire completă a agendei de la nivel nativ și va rula o scanare de actualizare. Această funcționalitate oferă utilizatorului un sentiment de control, permițându-i să se asigure că vede cele mai recente informații dacă tocmai a făcut modificări manuale în aplicația nativă de contacte.

- **Widget Test: `DashboardScreen` - Validarea Interfeței.** Vom scrie o suită de teste de widget pentru a verifica dacă: 1. Cifrele din carduri se actualizează corect la schimbarea stării provider-ului. 2. Butonul de scanare devine inactiv în timpul unei scanări în desfășurare. 3. Mesajele de eroare sunt afișate dacă serviciul nativ eșuează. 4. Gestul de pull-to-refresh apelează metoda corectă din controller. Aceste teste asigură că inima vizuală a aplicației rămâne funcțională pe parcursul dezvoltării ulterioare.

- **Dashboard Gestures: Navigare Contextuală.** Vom implementa interacțiunile de atingere pe toate elementele din Dashboard. Apăsarea pe cardul "Total Contacte" va naviga către o listă completă a agendei (Read Only), iar apăsarea pe "Duplicate Găsite" va trimite utilizatorul direct la lista filtrată de duplicate. Acest sistem de navigare intuitiv minimizează numărul de click-uri necesare pentru a ajunge la informația dorită, îmbunătățind eficiența generală a aplicației.

- **Spotlight Tutorial: Ghidarea Utilizatorului Nou.** La prima deschidere a aplicației, vom folosi pachetul `showcaseview` pentru a evidenția secvențial elementele cheie ale Dashboard-ului: 1. Butonul de scanare. 2. Scorul de sănătate. 3. Secțiunea de backup. Fiecare pas va avea un mic text explicativ. Acest tutorial interactiv (Onboarding Inside App) asigură că utilizatorul înțelege valoarea aplicației și știe cum să facă primul pas în procesul de organizare a agendei.

- <WORK_CODE>0614-0650 - **Buffer: Adaptare pentru Tablete și Ecrane Mari.** Acest interval este rezervat pentru optimizarea layout-ului pe ecrane cu lățime mare (tablete, ecrane pliabile). Vom folosi `GridView` în loc de `Column` pentru cardurile de statistici, permițând afișarea a 3 sau 4 carduri pe același rând. Vom asigura că imaginile și textele nu sunt întinse inestetic și că interfața profită de spațiul suplimentar disponibil pentru a afișa mai multe informații dintr-o singură privire, menținând aspectul "premium".

### Duplicate List & Filters (0651 - 0700)
- **`DuplicateListController`: Filtrarea și Sortarea Reactivă.** Vom crea un controller specializat pentru gestionarea listei de duplicate, situat în `lib/features/duplicates/controllers/duplicate_list_controller.dart`. Acesta va primi lista brută de grupuri din baza de date și va aplica filtre în memorie pe baza selecției utilizatorului (ex: doar duplicatele de tip "Telefon"). Controller-ul va gestiona și ordinea de afișare: implicit după scorul de încredere (cele mai sigure primele), dar oferind și opțiunea de sortare alfabetică. Folosirea unui stat separat pentru UI (`DuplicateListState`) permite o experiență rapidă de filtrare, fără a re-interoga baza de date la fiecare schimbare a tab-ului.

- **Duplicate List UI: Performanță cu `ListView.separated`.** Implementăm ecranul listei folosind `ListView.separated` pentru a asigura performanța maximă pe agende mari. Fiecare item va fi un card de grup, iar separatoarele vor fi obiecte `AppDivider` personalizate. Vom asigura utilizarea proprietății `itemExtent` sau `prototypeItem` dacă toate cardurile au aceeași înălțime, permițând Flutter-ului să optimizeze calculele de scroll. Layout-ul va respecta padding-ul global de 16dp și va include efectul de "overscroll" specific platformei (Android/iOS).

- **Filter Tabs: Segmentarea pe Nivele de Încredere.** Vom implementa un `TabBar` personalizat situat sub titlul paginii. Acesta va conține 3 secțiuni principale: 1. `Sigur` (scor > 95%). 2. `Probabil` (scor 80-94%). 3. `Manual` (scor sub 80%). Fiecare tab va afișa un badge mic cu numărul de grupuri conținute. Schimbarea tab-ului va declanșa o animație fluidă a listei. Această segmentare este crucială pentru UX, deoarece permite utilizatorului să se concentreze pe rezolvarea rapidă a cazurilor evidente înainte de a trece la cele care necesită analiză atentă.

- **Real-time Search: Căutare cu Debouncer.** În partea de sus a listei, vom integra un `AppTextField` configurat pentru căutare. Pentru a nu suprasolicita procesorul, vom implementa un `Debouncer` de 300ms: filtrarea listei va începe doar după ce utilizatorul s-a oprit din tastat. Căutarea va fi inteligentă, verificând numele tuturor contactelor din interiorul fiecărui grup de duplicate. Se va asigura că normalizarea (eliminarea diacriticelor) este aplicată și asupra termenului de căutare pentru rezultate precise.

- **Score Slider: Sensibilitate Ajustabilă (Avansat).** Vom adăuga un filtru opțional (ascuns într-un meniu drop-down) care permite utilizatorului să ajusteze manual pragul minim de scor afișat în listă folosind un `Slider`. Aceasta este o funcționalitate pentru utilizatorii tehnici care doresc să curețe agenda de potriviri foarte vagi (ex: scor 50%). Valorile slider-ului vor fi legate de `DuplicateListController`, actualizând vizibilitatea elementelor în timp real pe măsură ce utilizatorul glisează.

- **Type Chips: Filtrare rapidă pe Categorii.** Folosind widget-ul `FilterChip`, vom permite utilizatorului să filtreze rapid lista după natura duplicatului: "Doar Telefon", "Doar Email" sau "Doar Nume". Chip-urile vor fi poziționate într-un rând scrollabil orizontal deasupra listei. Această metodă de filtrare complementară tab-urilor de încredere oferă o flexibilitate extremă, permițând utilizatorului să curețe agenda într-o manieră sistematică, categorie cu categorie.

- **Bulk Mode: Acțiuni în Masă.** Vom implementa logica de selecție multiplă (Bulk Action). Aceasta se activează prin "long-press" pe orice card sau prin apăsarea unui buton "Editează". În acest mod, pe fiecare card va apărea un `Checkbox` circular în partea stângă, iar la baza ecranului va apărea o bară de acțiuni (Bottom Bar) cu opțiunile "Unește Selectate" și "Ignoră Selectate". Această funcționalitate economisește enorm de mult timp pentru utilizatorii cu sute de duplicate, permițându-le să proceseze grupuri mari printr-o singură confirmare.

- **Group Card UI: Vizualizarea Similarităților.** Cardul de grup trebuie să arate dintr-o privire de ce contactele au fost grupate. Vom implementa un layout cu 2 sau 3 mini-avatare suprapuse (Stacked Avatars) în stânga, numele principal în centru și motivul match-ului dedesubt (ex: "Același telefon: 0722..."). În partea dreaptă, vom afișa `StatusBadge`-ul corespunzător scorului. Design-ul va fi aerisit, folosind carduri cu margini rotunjite și o umbră foarte fină pentru a separa vizual grupurile între ele.

- **Quick Actions: Comenzi Rapide pe Card.** Pe fiecare card de grup, vom adăuga două butoane discrete de tip iconiță în colțul din dreapta: 1. `Check` (Merge) pentru a declanșa fuziunea automată pe baza sugestiei aplicației. 2. `X` (Ignore) pentru a scoate grupul din listă fără a face modificări. Aceste "Quick Actions" sunt destinate tab-ului de duplicate "Sigure", unde utilizatorul are încredere în decizia algoritmului și nu mai dorește să intre în ecranul de detalii pentru fiecare pereche.

- **Item Animation: Fluiditatea Listei cu `AnimatedList`.** Pentru a face procesul de curățare mai satisfăcător, vom folosi `AnimatedList` pentru a gestiona adăugarea și eliminarea grupurilor. Atunci când un utilizator apasă "Merge" sau "Ignore", cardul respectiv va dispărea cu un efect de glisare (Slide Transition) și micșorare a opacității (Fade Out). Timpul animației va fi de 300ms. Acest feedback vizual confirmă acțiunea și oferă o senzație de progres real, făcând aplicația să se simtă mult mai responsivă și modernă.

- **Search Empty State: Gestiunea Rezultatelor Vide.** Dacă utilizatorul caută un nume care nu există în lista de duplicate, vom afișa widget-ul `SearchEmptyState` creat anterior. Acesta va include ilustrația de căutare fără rezultat și un buton mare "Resetează căutarea" care golește câmpul text și readuce lista completă. Este o măsură de siguranță pentru a preveni confuzia utilizatorului ("Unde mi-au dispărut toate duplicatele?"), oferindu-i o cale clară de ieșire din starea de filtrare.

- **Widget Test: `DuplicateListScreen` - Validarea Logicii UI.** Vom scrie teste care verifică: 1. Dacă tab-urile filtrează corect grupurile pe baza scorului. 2. Dacă modul de selecție multiplă se activează corect. 3. Dacă search-ul reacționează la input. 4. Dacă butoanele de quick action apelează metodele corespunzătoare din controller. Testarea acestui ecran este vitală deoarece este locul unde utilizatorul își petrece majoritatea timpului activ în aplicație.

- **Performance Test: Scroll Stress Test.** Vom genera 1000 de grupuri de duplicate mock și vom rula un test de performanță automatizat. Vom măsura `FPS` (Frames Per Second) în timpul unui scroll rapid de sus până jos. Ținta tehnică este o medie de peste 55 FPS. Dacă apar scăderi de performanță (jank), vom optimiza widget-urile din listă prin folosirea constantelor, reducerea numărului de layere suprapuse și implementarea `RepaintBoundary` pe cardurile complexe.

- <WORK_CODE>0664-0700 - **Buffer: Sticky Headers și Gruparea după Dată.** Acest interval de commit-uri este rezervat pentru adăugarea "Sticky Headers" în listă. Grupurile vor fi separate prin antete care indică data la care au fost găsite (ex: "Azi", "Ieri", "Săptămâna trecută"). Această organizare cronologică ajută utilizatorul să identifice duplicatele apărute recent în urma unor importuri noi, menținând agenda mereu sub control. Se va folosi pachetul `sticky_headers` sau o implementare personalizată cu `SliverPersistentHeader`.

### Merge Detail Screen (0701 - 0750)
- **`MergeDetailController`: Gestiunea Fuziunii Interactive.** Vom implementa logica de business pentru procesul de unire a contactelor în `lib/features/duplicates/controllers/merge_detail_controller.dart`. Acest controller va menține un "Draft Contact" – obiectul rezultat în urma alegerilor utilizatorului. Starea va urmări fiecare câmp individual (nume, telefoane, email-uri) și sursa selectată pentru fiecare. Controller-ul va expune metode pentru schimbarea sursei unui câmp (`selectSourceForField`) și pentru editarea manuală a rezultatului final. Este componenta cea mai complexă din punct de vedere al stării, necesitând o logică riguroasă pentru a nu pierde nicio informație în timpul selecției.

- **Side-by-Side UI: Compararea Contactelor.** Vom implementa layout-ul principal de comparare. Pe ecrane mari (tablete), contactele vor fi afișate în coloane paralele. Pe telefoane, vom folosi un `PageView` sau un sistem de tab-uri orizontale pentru a glisa între "Sursa A" și "Sursa B". Sub fiecare sursă, vom afișa valorile corespunzătoare. Această vizualizare comparativă este esențială pentru a ajuta utilizatorul să identifice rapid diferențele (ex: un contact are adresa de acasă, celălalt are adresa de la serviciu) și să decidă ce să păstreze în rezultatul final.

- **Master Selection: Alegerea Sursei Principale.** Utilizatorul va putea selecta unul dintre contacte ca fiind "Master" (Sursă de Bază). Această acțiune va popula automat rezultatul final cu toate datele din acel contact. Vizual, cardul sursei selectate va fi evidențiat printr-o bordură groasă de culoare albastră (`AppColors.blue500`) și o iconiță de "Check". Această funcționalitate de tip "One-click Merge" simplifică procesul pentru cazurile unde un contact este clar mai actualizat decât celelalte duplicate din grup.

- **Field Selection: Selecția Granulară a Valorilor.** Fiecare rând de date (un număr de telefon, o adresă de email) va fi interactiv. Vom implementa un widget `SelectionListTile` care conține un buton radio în dreptul fiecărei valori. Utilizatorul poate face "mix-and-match": păstrează numele de la primul contact, dar numărul de telefon de la al doilea. Se va implementa și opțiunea de "Păstrează ambele" acolo unde sistemul permite (ex: adăugarea ambelor numere de telefon într-o listă), oferind o flexibilitate maximă în consolidarea informațiilor.

- **Conflict Highlighting: Evidențierea Diferențelor.** Pentru a reduce efortul cognitiv, aplicația va marca automat rândurile unde valorile diferă între contacte folosind culoarea `AppColors.warning` (galben/portocaliu). De exemplu, dacă numele sunt "Ion Popescu" și "Ionel Popescu", fundalul rândului de nume va fi colorat discret pentru a semnala un conflict care necesită atenție. Această ghidare vizuală permite utilizatorului să ignore rapid câmpurile identice și să se concentreze doar pe datele care produc ambiguitate.

- **Final Name Edit: Personalizarea Rezultatului.** În partea superioară a ecranului, sub zona de preview, vom include un `AppTextField` care permite editarea numelui final al contactului rezultat. Chiar dacă utilizatorul alege una dintre surse, el poate dori să corecteze manual numele (ex: să adauge un titlu sau o poreclă). Această libertate de editare finală asigură că agenda va arăta exact așa cum își dorește utilizatorul după finalizarea procesului de curățare.

- **Live Preview: Vizualizarea în Timp Real.** Vom implementa o secțiune fixă (floating) la baza ecranului (sau un header persistent) care arată cum va arăta contactul final în timp ce utilizatorul face selecțiile. Preview-ul va include `ContactAvatar`-ul generat și numele complet. Această confirmare vizuală continuă elimină frica de a greși, deoarece utilizatorul vede permanent impactul fiecărei alegeri (radio button apăsat) asupra rezultatului ce urmează a fi salvat în agendă.

- **Merge Button: Confirmarea Acțiunii.** Butonul principal de execuție, situat la baza ecranului, va afișa textul localizat "Confirmă îmbinarea celor [x] contacte". Acesta va folosi stilul `AppPrimaryButton`. La apăsare, aplicația va declanșa un dialog de confirmare finală (Double-check) care rezumă acțiunea: "Se vor șterge 2 contacte și se va crea unul nou îmbunătățit". Acest ultim pas de siguranță previne erorile accidentale de tip "fat-finger".

- **Validation Logic: Prevenirea Datelor Incomplete.** Vom implementa o logică de validare care dezactivează butonul de "Merge" dacă există conflicte nerezolvate (ex: utilizatorul nu a ales niciun nume pentru contactul final). De asemenea, vom avertiza utilizatorul dacă încearcă să unească contacte care nu au nicio valoare comună, sugerând că ar putea fi o eroare de selecție. Acest strat de inteligență artificială aplicată procesului de UX garantează calitatea agendei rezultate.

- **Widget Test: `MergeDetailScreen` - Testarea Interacțiunii.** Vom scrie teste unitare pentru UI care verifică: 1. Dacă selectarea unui radio button actualizează corect obiectul `DraftContact`. 2. Dacă editarea numelui în text field se reflectă în preview. 3. Dacă butonul de merge se activează/dezactivează conform regulilor de validare. 4. Dacă scroll-ul funcționează corect pe liste lungi de detalii (ex: contacte cu 10+ numere de telefon). Aceste teste sunt critice pentru stabilitatea celui mai complex ecran al aplicației.

- <WORK_CODE>0711-0750 - **Buffer: Fuziunea Grupurilor Mari (3+ Contacte).** Acest interval este dedicat gestionării cazurilor unde un grup de duplicate conține mai mult de 2 contacte (ex: același om salvat în Google, SIM și local). Vom optimiza interfața pentru a permite compararea a 3 sau mai multe coloane prin scroll orizontal, menținând în același timp preview-ul final vizibil. Vom asigura că logica de selectare a valorilor scalează corect indiferent de numărul de surse implicate în grup.

### Settings & Profile (0751 - 0800)
- **`SettingsController`: Gestiunea Persistenței Configurărilor.** Vom implementa controller-ul pentru ecranul de setări în `lib/features/settings/controllers/settings_controller.dart`. Acesta va orchestra interacțiunea cu `AppSettingsDao` pentru salvarea preferințelor în baza de date locală și va comunica cu `ThemeProvider` și `AppLanguageService` pentru a aplica schimbările de aspect și limbă în timp real. Starea controller-ului va fi un obiect `SettingsState` care include: `currentThemeMode`, `currentLocale`, `scanSensitivity` și `isAutoBackupEnabled`. Vom asigura utilizarea unui mecanism de "optimistic UI update", unde interfața se actualizează imediat, iar salvarea în DB se face în fundal.

- **Theme Toggle: Controlul Aspectului cu `AppSwitch`.** Vom implementa rândul de setare pentru temă folosind widget-urile `SettingsTile` și `AppSwitch` create anterior. Utilizatorul va putea comuta între modul Light și Dark. Vom adăuga și o a treia opțiune "Sistem" printr-un dialog de selecție sau un set de butoane segmentate. Schimbarea temei va declanșa o animație de tranziție fluidă a culorilor în întreaga aplicație, folosind `AnimatedTheme` pentru a evita schimbările bruște de contrast care pot fi deranjante pentru ochi.

- **Language Picker: Dialogul de Selecție a Limbii.** Acest task presupune legarea widget-ului `LanguageSelector` de ecranul de setări. La apăsarea pe tile-ul de limbă, se va deschide un `AppBottomSheet` care listează opțiunile (Română, Engleză) cu steagurile corespunzătoare. Vom implementa o logică de confirmare: după selectarea unei noi limbi, aplicația va afișa un mic mesaj (Toast sau SnackBar) care confirmă schimbarea. Toate textele din aplicație trebuie să se actualizeze instantaneu, fără a necesita repornirea sesiunii.

- **Sensitivity Settings: Ajustarea Pragului de Detecție.** Vom implementa un slider în setări care permite utilizatorului să definească ce înseamnă un contact "Sigur" pentru el. Slider-ul va varia între 60 și 100 (scor de încredere). Valoarea implicită va fi 95. Această setare va fi transmisă către `DuplicateDetectorService`, care va recalcula în timp real încadrarea grupurilor în tab-urile din lista de duplicate. Este o funcționalitate avansată care oferă utilizatorului control total asupra algoritmului de inteligență a datelor.

- **Whitelist Management: Administrarea Contactelor Ignorate.** Vom crea un sub-ecran "Contacte Ignorate" care listează toate perechile de contacte pe care utilizatorul a ales să le păstreze separate (prin acțiunea "Ignore"). Fiecare item din listă va arăta cele două contacte și va oferi un buton "Restore" (Elimină din Whitelist). Această funcționalitate este esențială pentru a permite utilizatorului să corecteze greșelile de ignorare anterioare, asigurându-se că acele duplicate pot fi din nou detectate la scanările viitoare.

- **Auto-backup Toggle: Siguranță prin Automatizare.** Vom implementa setarea pentru backup automat. Dacă este activată, aplicația va genera silențios un fișier vCard cu starea curentă a agendei înainte de a executa orice operațiune de "Merge". Vom adăuga și o setare pentru "Ciclul de viață al backup-urilor", permițând utilizatorului să aleagă câte copii de rezervă dorește să păstreze (ex: ultimele 10) înainte ca cele vechi să fie șterse automat pentru a economisi spațiu.

- **About Section: Informații Legale și Versiune.** Vom implementa o secțiune informativă care afișează logo-ul aplicației, versiunea curentă (extrasă din `package_info_plus`), link-uri către "Termeni și Condiții" și "Politica de Confidențialitate". De asemenea, vom include un buton "Licențe Open Source" care deschide ecranul standard Flutter de afișare a licențelor pentru toate bibliotecile folosite. Această transparență este obligatorie pentru publicarea în App Store și Play Store.

- **Clear History: Resetarea Activității.** Vom adăuga o acțiune distructivă controlată: "Șterge istoricul de fuziuni". Această funcție va goli tabelele de loguri și statistici din baza de date, dar va păstra fișierele fizice de backup vCard. Acțiunea va fi protejată de un `ConfirmationDialog` cu stilul "Danger", explicând clar utilizatorului că nu va mai putea vedea statisticile anterioare de economisire a spațiului după resetare.

- **Feedback Flow: Comunicarea cu Suportul.** Vom integra pachetul `share_plus` pentru a permite utilizatorului să trimită un email de feedback. Aplicația va atașa automat (doar cu acordul utilizatorului) un fișier log anonimizat care conține informații tehnice (model dispozitiv, versiune OS, erori de sistem recente). Acest flux de feedback este vital pentru identificarea bug-urilor specifice anumitor modele de telefoane pe care echipa de dezvoltare nu le poate testa direct.

- **Widget Test: `SettingsScreen` - Verificarea Integrării.** Vom scrie teste unitare pentru UI care verifică: 1. Dacă toggle-urile de temă și backup actualizează starea în provider. 2. Dacă navigarea către ecranele secundare (Whitelist, About) funcționează. 3. Dacă textele se schimbă corect la selecția unei noi limbi în dialog. 4. Dacă slider-ul de sensibilitate persistă valoarea după ce utilizatorul părăsește și revine pe ecran.

- <WORK_CODE>0761-0800 - **Buffer: Secțiunea de Întrebări Frecvente (FAQ).** Acest interval este rezervat pentru implementarea unui ecran de tip FAQ, unde utilizatorii pot găsi răspunsuri la probleme comune fără a contacta suportul (ex: "De ce nu îmi vede toate contactele?", "Unde sunt salvate backup-urile?"). Vom folosi un `ExpansionPanelList` pentru a prezenta întrebările și răspunsurile într-un mod compact și ușor de parcurs, îmbunătățind autonomia utilizatorului final.

---

## FAZA 5: Merge Engine, Backup și Istoric (Commit-uri 0801 - 0920)

### Backup Service (0801 - 0840)
- **`BackupService`: Arhitectură și Gestiunea Fișierelor.** Vom implementa motorul de siguranță a datelor în `lib/core/services/backup_service.dart`. Acest serviciu va orchestra crearea, listarea și restaurarea copiilor de rezervă. Vom folosi pachetul `path_provider` pentru a defini o locație sigură în `applicationDocumentsDirectory` subfolderul `/backups`. Serviciul va implementa o logică de rotație a fișierelor, asigurându-se că doar cele mai recente N copii sunt păstrate pe disc pentru a nu aglomera memoria telefonului. Fiecare operațiune de backup va returna un obiect `BackupMetadata` care va fi salvat automat în baza de date locală pentru o urmărire ușoară în UI.

- **vCard Export: Implementarea Standardului 3.0.** Vom scrie un generator de fișiere vCard compatibil cu specificațiile RFC 2426. Acest task presupune transformarea obiectelor `AppContact` în string-uri formatate (ex: `BEGIN:VCARD`, `VERSION:3.0`, `FN:...`, `TEL:...`). Vom acorda o atenție deosebită codării caracterelor speciale (UTF-8) pentru a asigura suportul diacriticelor românești în orice aplicație care va citi fișierul ulterior (Google Contacts, Apple Contacts, Outlook). Exportul vCard este esențial deoarece este un format universal recunoscut, oferind utilizatorului libertatea de a-și muta datele pe orice alt dispozitiv.

- **JSON Export: Salvarea Metadatelor Interne.** Pe lângă formatul vCard, vom implementa și un export secundar în format JSON criptat. Acest fișier va conține metadate specifice aplicației noastre (ex: ID-urile native ale surselor, scorurile de încredere anterioare, istoricul ignorărilor). Acest "Full State Backup" permite o restaurare 1:1 a stării aplicației în cazul în care utilizatorul își schimbă telefonul dar dorește să păstreze tot procesul de organizare a agendei intact, oferind un nivel de continuitate superior exportului standard.

- **Naming Convention: Standardizarea Fișierelor.** Vom implementa o funcție utilitară pentru generarea numelor de fișiere de tipul `contacts_backup_YYYYMMDD_HHMMSS.vcf`. Această nomenclatură cronologică permite utilizatorului să identifice ușor versiunea backup-ului fără a deschide aplicația. Vom adăuga și un prefix opțional care indică dacă backup-ul a fost creat automat (ex: `auto_...`) sau manual, facilitând sortarea și curățarea automată a fișierelor de către serviciul de management al stocării.

- **Storage Management: Optimizarea Spațiului pe Disc.** Vom implementa un serviciu de monitorizare a folderului de backup. Înainte de a crea o copie nouă, aplicația va verifica spațiul disponibil. Dacă memoria este sub un anumit prag (ex: 50 MB), aplicația va avertiza utilizatorul și va sugera ștergerea backup-urilor vechi. Se va implementa și funcția `cleanupOldBackups()` care caută fișiere mai vechi de 30 de zile și le șterge dacă utilizatorul a activat această opțiune în setări, menținând dispozitivul curat și eficient.

- **`BackupMetadata`: Verificarea Integrității cu Checksum.** Vom crea un model de date care stochează metadatele fiecărui fișier de backup, inclusiv un `checksum` generat prin algoritmul `SHA-256`. Acest hash va fi calculat la finalizarea exportului și salvat în baza de date. Înainte de a începe orice proces de restaurare, aplicația va re-calcula hash-ul fișierului de pe disc și îl va compara cu cel salvat, avertizând utilizatorul dacă fișierul a fost modificat extern sau a fost corupt, garantând siguranța totală a datelor restaurate.

- **Auto-cleanup: Gestiunea Automată a Vechimii Fișierelor.** Vom implementa o logică de fundal care rulează periodic și verifică numărul total de fișiere din folderul `/backups`. Dacă acesta depășește limita configurată de utilizator (implicit 10), aplicația va șterge automat cele mai vechi fișiere (First-In, First-Out). Această întreținere proactivă previne situația în care aplicația ar putea ocupa GB de spațiu în timp, o problemă comună pentru utilizatorii care fac fuziuni frecvente ale contactelor.

- **Space Check: Verificarea Resurselor Sistemului.** Vom folosi pachetul `storage_details` sau similar pentru a interoga sistemul despre spațiul liber real de pe partiția de date. Vom implementa o barieră tehnică: nicio operațiune de backup masiv nu va putea fi pornită dacă spațiul liber este sub 50 MB. Utilizatorul va primi un dialog explicativ cu opțiunea de a curăța cache-ul sistemului sau de a șterge aplicații inutile, evitând astfel erorile de tip `IOError` la nivel de kernel.

- **Backup UI List: Vizualizarea și Sortarea Istoricului.** Vom implementa ecranul principal de backup folosind un `ListView` performant. Fiecare rând va afișa data creării în format localizat (RO/EN), numărul de contacte și dimensiunea fișierului (ex: "4.2 MB"). Vom implementa și o logică de sortare: implicit cronologic (cele mai noi primele), dar oferind și opțiunea de sortare după dimensiune. Utilizatorul va avea un feedback vizual rapid asupra eforturilor de protejare a agendei sale.

- **Share Integration: Partajarea Externă a Fișierelor.** Vom integra pachetul `share_plus` pentru a permite utilizatorului să trimită fișierul vCard către alte aplicații (Email, WhatsApp, Google Drive). Aceasta este o funcționalitate vitală pentru "off-device backup". Vom implementa o logică de "temporary share file": fișierul este copiat într-un folder temporar securizat pentru partajare, fiind șters automat după finalizarea operațiunii de către sistemul de operare.

- **Restore Logic: Procesul de Reintegrare a Datelor.** Vom dezvolta nucleul motorului de restaurare. Acesta va citi fișierul VCF sau JSON selectat, va parsa conținutul în obiecte `AppContact` și va folosi `INativeContactsService` pentru a scrie datele în agenda telefonului. Se va implementa o logică de "safe write": restaurarea se face în tranzacții mici (batch-uri de 50 contacte) pentru a raporta progresul utilizatorului și pentru a putea face rollback în caz de eroare critică de sistem.

- **Integrity Check: Validarea Structurii Fișierelor.** Înainte de a procesa orice fișier de backup pentru restaurare, vom implementa un pas de validare structurală. Vom verifica prezența header-elor și footer-elor obligatorii (`BEGIN:VCARD` / `END:VCARD`). Dacă structura este invalidă, utilizatorul va primi un mesaj de eroare specific: "Fișierul selectat nu este un backup valid de contacte". Acest pas previne importul accidental de fișiere greșite care ar putea corupe baza de date a sistemului.

- **Unit Test: `BackupService` - Validarea Formatului.** Vom scrie teste unitare riguroase pentru a verifica dacă generatorul vCard produce fișiere 100% conforme cu standardul 3.0. Vom valida prezența tuturor câmpurilor obligatorii și corectitudinea formatării numerelor de telefon internaționale. De asemenea, vom testa dacă procesul de restaurare refuză corect fișierele cu checksum greșit, asigurându-ne că mecanismele noastre de siguranță funcționează conform specificațiilor tehnice.

- **Benchmark: Performanța Procesării Masive.** Vom rula un test de benchmark pentru a măsura timpul de salvare a 5000 de contacte. Ținta tehnică este ca întreg procesul (generare string + scriere pe disc) să dureze sub 5 secunde pe un dispozitiv mediu. Dacă rezultatele sunt peste acest prag, vom optimiza generatorul folosind `StringSink` în loc de concatenări multiple de string-uri sau vom muta întreaga logică într-un `Isolate` de fundal pentru a nu bloca UI thread-ul.

- <WORK_CODE>0815-0840 - **Buffer: Gestiunea Imaginilor și Notele Complexe.** Acest interval este rezervat pentru a adăuga suport pentru exportul imaginilor de contact în format `BASE64` în interiorul vCard-ului. Această opțiune va fi configurabilă din setări, deoarece crește semnificativ dimensiunea fișierului de backup. De asemenea, vom asigura că toate câmpurile de tip `Notes` cu caractere multi-linie sunt corect "escaped" conform standardului, prevenind ruperea structurii fișierului la import.

### Merge Engine (0841 - 0880)
- **`MergeEngineService`: Coordonatorul Fuziunii Securizate.** Vom implementa piesa centrală a aplicației în `lib/core/services/merge_engine_service.dart`. Acest serviciu va orchestra fluxul critic: crearea unui backup vCard, scrierea noului contact în agendă și ștergerea duplicatelor originale. Toate aceste acțiuni vor fi înfășurate într-o mașină de stări (`idle`, `backingUp`, `writing`, `cleaning`, `completed`, `failed`) pentru a oferi un feedback vizual precis în UI și pentru a permite reluarea în caz de eșec parțial.

- **Safe Guard: Verificarea Backup-ului Recent.** Pentru a preveni pierderea datelor, vom implementa un "Safe Guard" tehnic. Metoda `executeMerge` va verifica existența unui fișier vCard generat cu cel mult 5 minute înainte. Dacă acest backup lipsește sau este învechit, motorul va refuza execuția și va forța apelul `BackupService.createBackup()`. Această regulă strictă elimină posibilitatea ca un bug sau o eroare de sistem în timpul scrierii să lase utilizatorul fără datele originale.

- **Android Batch Write: Atomicitatea Scrierii.** Pe Android, vom implementa scrierea folosind `ArrayList<ContentProviderOperation>`. Aceasta ne permite să trimitem atât crearea noului contact, cât și ștergerea duplicatelor într-un singur apel `applyBatch`. Sistemul de operare garantează că aceste operațiuni sunt executate atomic: fie reușesc toate, fie niciuna. Această tehnică elimină riscul de a rămâne cu contacte orfane sau duplicate parțial șterse.

- **Android Delete: Ștergerea Securizată a Surselor.** Vom adăuga operațiunea de tip `newDelete` în batch-ul de `ContentProvider`. Înainte de ștergere, vom verifica dacă contactul nu este marcat ca "ReadOnly" (ex: din WhatsApp). Dacă un contact din grup este read-only, motorul va sări peste ștergerea lui și va informa utilizatorul că duplicatul va rămâne vizibil în agendă din cauza restricțiilor sistemului, dar informația a fost consolidată în contactul nou.

- **iOS Batch: Utilizarea `CNSaveRequest`.** Pentru iOS, vom folosi `CNSaveRequest` din framework-ul `Contacts`. Această clasă ne permite să grupăm multiple operațiuni de adăugare și ștergere într-o singură tranzacție către `CNContactStore`. Vom gestiona cu atenție excepțiile de tip `CNErrorCodePermissionDenied`, asigurându-ne că oferim utilizatorului instrucțiuni de rezolvare specifice Apple dacă operațiunea de scriere este blocată de sistem.

- **iOS Delete: Eliminarea Contactelor Native.** Vom apela `saveRequest.deleteContact()` pe obiectele `CNMutableContact` identificate ca duplicate. Vom implementa o logică de verificare a containerului (iCloud vs Local), deoarece ștergerea din containere externe poate fi supusă unor latențe de rețea. Rezultatul va fi monitorizat prin delegatul de salvare pentru a confirma că agenda nativă reflectă corect starea dorită.

- **Transactionality: Implementarea Rollback-ului.** Dacă scrierea noului contact sau ștergerea duplicatelor eșuează la nivel nativ, motorul va declanșa o procedură de rollback. Aceasta presupune anularea oricărei modificări parțiale (dacă e posibil) sau, în caz de eroare critică, oferirea unui buton rapid "Restaurează din Backup" care folosește fișierul vCard creat la pasul 0842, garantând integritatea agendei.

- **Exception Mapping: Traducerea Erorilor Native.** Vom crea un mapper care transformă erorile brute (`PlatformException`) în mesaje localizate pe înțelesul utilizatorului. De exemplu, o eroare de tip `AccessDeniedException` pe Android va fi afișată ca "Lipsă permisiune de scriere. Te rugăm să verifici setările". Această abordare reduce frustrarea utilizatorului în fața problemelor tehnice, oferindu-i o cale clară de rezolvare.

- **Conflict Resolver: Regulă Automată de Selecție.** Pentru fuziunile automate (Bulk Merge), vom implementa o regulă de a păstra valoarea cea mai lungă (ex: între "Ion Popescu" și "Ion", sistemul va alege prima variantă). Această euristică simplă dar eficientă crește calitatea datelor rezultate, asigurându-ne că nicio informație bogată nu este înlocuită de o variantă prescurtată sau incompletă.

- **Merge Report: Generarea Rezumatului Activității.** După finalizarea fiecărei fuziuni, motorul va returna un obiect `MergeReport`. Acesta va conține lista ID-urilor șterse, ID-ul noului contact creat și un rezumat al datelor salvate (ex: "S-au unit 3 numere de telefon"). Acest raport va fi salvat în baza de date locală pentru a fi afișat ulterior în ecranul de Istoric, oferind utilizatorului o dovadă clară a muncii depuse de aplicație.

- **Merge Progress UI: Dialogul de Așteptare.** Vom implementa un dialog de progres care apare în timpul fuziunii. Acesta va include un indicator de progres liniar (`LinearProgressIndicator`) și un text dinamic care indică pasul curent: "Se creează backup...", "Se scrie contactul [x]...", "Se curăță duplicatele...". Această transparență vizuală asigură utilizatorul că procesul este sub control și că datele sale sunt manipulate cu grijă.

- **Photo Merger: Alegerea Celei Mai Bune Poze.** În cadrul procesului de fuziune, vom implementa o logică de comparare a rezoluțiilor imaginilor de profil. Motorul va alege automat poza cu dimensiunea cea mai mare dintr-un grup de duplicate pentru a fi păstrată în contactul final. Acest detaliu tehnic asigură că utilizatorul beneficiază de cea mai bună calitate vizuală posibilă în agenda sa consolidată.

- **Post-Merge Verify: Re-scanarea Automată.** După ce un grup a fost unit, motorul va declanșa silențios o scanare a noului contact creat pentru a se asigura că acesta nu a generat noi duplicate (ex: prin potrivirea cu un al treilea contact care nu fusese inițial în grup). Rezultatul va fi o agendă perfect curată și un sentiment de siguranță pentru utilizator că fuziunea a fost 100% reușită.

- **Unit Test: `MergeEngine` - Simulare Succes/Eșec.** Vom scrie teste unitare folosind `mocktail` pentru a simula diverse răspunsuri ale serviciului nativ. Vom verifica: 1. Dacă backup-ul este apelat întotdeauna înainte de scriere. 2. Dacă motorul se oprește corect la prima eroare. 3. Dacă agregarea datelor (nume, telefoane) este corectă matematic. Aceste teste sunt plasa noastră de siguranță pentru funcționalitatea cea mai critică.

- **Integration Test: Execuție Merge pe Emulator.** Vom rula un test de integrare complex pe un emulator cu o agendă pre-populată de 50 de contacte duplicat. Vom declanșa un bulk-merge și vom verifica la final: 1. Numărul total de contacte rămase. 2. Prezența contactelor noi unite. 3. Dispariția surselor. Acest test "End-to-End" este validarea supremă a calității soluției noastre de management al contactelor.

- <WORK_CODE>0856-0880 - **Buffer: Stabilitatea Motorului de Scriere.** Acest interval este rezervat pentru rezolvarea cazurilor limită identificate în timpul testelor de integrare: tratarea contactelor blocate de sistem (System Locked), gestionarea întreruperilor de rețea în timpul sync-ului iCloud și optimizarea timpului de scriere pe dispozitivele cu baze de date SQL native lente. Obiectivul este ca procesul de fuziune să fie impecabil și ultra-rapid.

### History & Activity Log (0881 - 0920)
- **`HistoryService`: Jurnalizarea Activității Aplicației.** Vom crea serviciul responsabil cu evidența tuturor acțiunilor de management al contactelor în `lib/core/services/history_service.dart`. Acest serviciu va interacționa direct cu `HistoryDao` din baza de date locală Drift. Obiectivul este să oferim utilizatorului o urmă clară (Audit Trail) a ceea ce s-a întâmplat cu agenda sa: când a fost făcut ultimul backup, câte contacte au fost unite în fiecare sesiune și ce erori au apărut. Serviciul va expune fluxuri de date (Streams) pentru a permite actualizarea în timp real a interfeței de istoric.

- **`HistoryEntry` Model: Structura Detaliată a Logurilor.** Vom defini modelul de date pentru o intrare în istoric. Acesta va include: `id` (unic), `timestamp`, `type` (un enum: `Merge`, `Backup`, `Scan`, `Restore`), `status` (`Success`, `Failure`, `Partial`), și un câmp `summary` de tip JSON pentru detalii specifice (ex: numărul de contacte implicate). Această structură flexibilă ne permite să stocăm informații variate fără a complica schema bazei de date, facilitând în același timp afișarea unor rezumate bogate în interfața de utilizator.

- **Auto-logging Integration: Colectarea Automată a Datelor.** Vom integra apeluri către `HistoryService.add()` în punctele cheie ale aplicației: 1. La finalizarea unei scanări. 2. După un export vCard reușit. 3. Imediat după ce `MergeEngine` confirmă scrierea în agendă. Această automatizare asigură că utilizatorul nu trebuie să face nimic special pentru a beneficia de istoric, aplicația construind silențios "biografia" procesului de curățare. Se va asigura că nicio dată PII (nume sau telefoane) nu este stocată în aceste loguri, respectând politicile de securitate.

- **History UI: Vizualizarea Cronologică cu `ListView`.** Vom implementa ecranul de istoric folosind un `ListView` cu grupări pe zile. Fiecare intrare va folosi o iconiță colorată semantic (Albastru pentru Backup, Verde pentru Merge, Galben pentru Scan). Vom afișa ora exactă și un rezumat scurt (ex: "Fuziune reușită: 4 contacte unite"). Se va implementa o animație de tip "expand" pe fiecare item pentru a arăta detalii suplimentare la atingere, menținând în același timp lista principală curată și ușor de parcurs, folosind formatarea datei relative (ex: "Acum 5 min").

- **History Detail: Detalierea unei Acțiuni și Contactele Implicate.** La apăsarea unui item din istoric, se va deschide un ecran sau un `AppBottomSheet` care arată tot raportul acțiunii. Pentru un "Merge", vom lista numele contactelor care au fost șterse și numele noului contact creat ("Înainte și După"). Această granularitate a informației oferă utilizatorului siguranța că nimic nu se întâmplă "în spate" fără ca el să poată verifica ulterior detaliile tehnice ale transformării agendei sale.

- **Undo Action: Inversarea Ultimului Merge.** Vom implementa funcționalitatea de "Undo" pentru operațiunile de fuziune. La apăsarea butonului, aplicația va folosi backup-ul vCard asociat acelei intrări din istoric pentru a restaura contactele originale și a șterge contactul creat prin fuziune. Această funcție de "salvare de ultim moment" este vitală pentru încrederea utilizatorului, oferindu-i o cale de întoarcere în cazul în care constată că a unit din greșeală două persoane diferite.

- **Incomplete Status: Detecția Întreruperilor de Sistem.** Vom implementa o logică de marcare a intrărilor din jurnal dacă aplicația s-a închis brusc în timpul unei operațiuni (folosind un timestamp `lastSeenAt`). La repornire, dacă o acțiune este găsită în starea "In Progress", ea va fi marcată automat ca "Incompletă / Posibilă Eroare". Utilizatorul va fi invitat să verifice integritatea datelor sau să ruleze un restore, asigurând o stare de date predictibilă.

- **Widget Test: Verificarea Sortării și Afișării.** Vom scrie teste unitare pentru UI-ul de istoric. Vom verifica: 1. Dacă lista este sortată descrescător (cea mai recentă acțiune sus). 2. Dacă iconițele se schimbă corect în funcție de tipul acțiunii. 3. Dacă textele sunt localizate conform setărilor de limbă ale utilizatorului. 4. Dacă butonul de Undo apare doar pentru acțiunile de tip Merge care au un backup valid asociat în memorie.

- <WORK_CODE>0889-0920 - **Buffer: Export Istoric și Optimizări de Bază.** Acest interval este rezervat pentru funcționalități de export (PDF/CSV) ale jurnalului de activitate pentru power users și pentru optimizarea vitezei de interogare a bazei de date. Vom implementa un sistem de "Log Rotation" care șterge automat intrările mai vechi de 6 luni, menținând aplicația rapidă și ocupând un spațiu minim pe disc, respectând în același timp nevoile de audit ale utilizatorului.

---

## FAZA 6: Testare Finală, Optimizare și Store (Commit-uri 0921 - 1000)

### Testing & Bug Fixing (0921 - 0960)
- **Regression Android: Testare pe Dispozitive Reale.** Această etapă presupune validarea întregului flux de utilizare pe o gamă variată de dispozitive Android. Vom folosi un Samsung S23 (pentru OneUI customizări), un Pixel 7 (pentru Android curat) și un Huawei (pentru lipsa GMS). Vom verifica: 1. Randarea corectă a iconițelor adaptive. 2. Viteza de scanare a contactelor pe diverse versiuni de OS (11, 12, 13, 14). 3. Stabilitatea bridge-ului nativ în timpul operațiunilor de scriere masivă. Obiectivul este să identificăm și să rezolvăm bug-urile specifice producătorilor (vendor-specific) care nu apar în simulatoare, asigurând o experiență uniformă pentru 99% din utilizatorii Android.

- **Regression iOS: Testare pe Ecosistemul Apple.** Vom rula suita de teste de regresie pe iPhone 13, iPhone 15 Pro și un model de iPad. Vom acorda o atenție deosebită: 1. Suportului pentru Multi-tasking și Split View pe iPad. 2. Gestionării corecte a permisiunilor în noul sistem de securitate iOS 17+. 3. Performanței animațiilor de tip Glassmorphism sub sarcină mare a procesorului. 4. Comportamentului aplicației în timpul sincronizării active cu iCloud. Orice latență detectată în scroll-ul listelor de duplicate va fi adresată prin optimizări la nivel de Swift sau prin reducerea complexității widget-urilor din lista principală.

- **Stress Test 10k: Performanța la Volume Masive.** Vom scrie un script de testare care injectează 10.000 de contacte mock în agenda telefonului (cu cel puțin 1.000 de grupuri de duplicate reale). Vom măsura timpul total de scanare, consumul maxim de memorie RAM și stabilitatea bazei de date Drift sub stres. Ținta tehnică este ca aplicația să rămână responsivă (să permită navigarea în UI) în timp ce scanarea rulează în fundal. Rezultatele acestui test ne vor ajuta să decidem dacă este necesară o limitare a numărului de contacte procesate simultan sau dacă putem garanta performanța pe agende de dimensiuni industriale.

- **Interrupt Test: Reziliența la Crash-uri și Reporniri.** Vom simula scenarii critice: 1. Forțarea închiderii aplicației în mijlocul procesului de scriere a unui "Merge". 2. Restartarea telefonului în timpul unui backup vCard. 3. Pierderea conexiunii la internet în timpul sync-ului iCloud (pe iOS). După repornire, vom verifica integritatea bazei de date locale și a agendei native. Aplicația trebuie să detecteze automat starea incompletă și să ofere utilizatorului opțiunea de a finaliza acțiunea sau de a face rollback, prevenind pierderea oricărei date de contact.

- **Offline Test: Garantarea Confidențialității Totale.** Pentru a valida promisiunea de privacy "100% Local", vom rula aplicația pe un dispozitiv în "Mod Avion" (fără WiFi, fără date mobile, fără Bluetooth). Vom efectua toate operațiunile: scanare, backup, merge și setări. Vom folosi un instrument de monitorizare a traficului de rețea (ex: Charles Proxy sau Wireshark) pe un dispozitiv adiacent pentru a confirma că niciun pachet de date nu părăsește aplicația către servere externe. Acest test este fundamentul pe care se bazează încrederea utilizatorului și integritatea brandului nostru.
- **UI Polish: Rafinarea Detaliilor și Contrast.** Vom efectua un audit de design final, pixel cu pixel. Vom verifica: 1. Alinierea tuturor elementelor la grid-ul de 8dp. 2. Consistența umbrelor (`AppBoxShadows`) pe diverse luminozități de ecran. 3. Verificarea contrastului textelor față de fundal conform standardelor WCAG 2.1 AA (raport minim 4.5:1). Vom ajusta fin culorile în `app_colors.dart` pentru a asigura o lizibilitate optimă, în special în modul Dark. Se va asigura că stările de "hover" și "active" ale butoanelor sunt vizibile și oferă feedback tactil și vizual prompt, crescând calitatea percepută a aplicației la un nivel profesional.

- **Bundle Optimization: Minificarea și Obfuscarea.** Pregătim pachetul de distribuție prin rularea comenzii `flutter build appbundle --obfuscate --split-debug-info=./debug-info`. Acest task presupune configurarea fișierelor `keep` pentru ProGuard (Android) și verificarea dimensiunii finale a fișierului `.aab`. Ținta tehnică este o dimensiune sub 25 MB, pentru a permite descărcarea rapidă chiar și pe conexiuni mobile slabe. Vom analiza raportul de dimensiune generat de Flutter (`--analyze-size`) pentru a identifica și elimina orice asset inutil sau bibliotecă redundantă care umflă artificial pachetul aplicației.

- **Memory Leak Audit: Depanarea Resurselor.** Vom folosi tab-ul `Memory` din Flutter DevTools pentru a efectua un audit de memorie de 2 ore de utilizare intensă. Vom căuta: 1. Stream-uri neînchise în controller-ele de Dashboard și Duplicate. 2. Imaginile de profil care rămân în RAM după ce ecranele au fost închise. 3. Referințe ciclice în serviciile de baze de date. Obiectivul este menținerea consumului de memorie sub 150 MB în timpul scanărilor masive, asigurându-ne că sistemul de operare nu va închide forțat aplicația din cauza utilizării excesive a resurselor.

- **Multi-Source Test: Verificarea Fuziunii între Conturi Diferite.** Un caz critic este unirea unui contact de pe cartela SIM cu unul din Google Contacts sau iCloud. Vom rula teste de integrare care verifică: 1. Dacă ID-urile ambelor surse sunt corect procesate. 2. Dacă scrierea noului contact se face în contul selectat ca "Master". 3. Dacă duplicatul de pe SIM este corect marcat ca "ReadOnly" sau șters acolo unde permisiunile hardware permit. Acest test garantează că utilizatorii care au contacte împrăștiate în multiple servicii de sync nu vor experimenta pierderi de date sau comportamente incoerente.

- <WORK_CODE>0930-0960 - **Bug-Fixing: Ciclul de Testare Beta și Rezolvarea Problemelor.** Acest bloc masiv de commit-uri este rezervat pentru remedierea feedback-ului primit în urma fazei de testare internă (Dogfooding) și beta externă. Vom adresa tichetele de tip: 1. Overflow-uri de text pe dispozitive cu ecrane mici. 2. Întârzieri la prima scanare pe agende cu 2000+ contacte. 3. Discrepanțe de localizare (string-uri tăiate sau netraduse). Fiecare bug rezolvat va fi însoțit de un unit test de regresie pentru a asigura că problema nu va reapărea în versiunile viitoare. Finalizarea acestui bloc marchează trecerea de la faza de dezvoltare la cea de publicare.

### Final Prep & Store Compliance (0961 - 1000)
- **Play Store Assets: Pregătirea Materialelor Vizuale.** Vom genera toate resursele necesare pentru Google Play Console: 1. `Feature Graphic` (1024x500px) care să evidențieze beneficiul principal (Merge). 2. `App Icon` (512x512px) de înaltă rezoluție cu margini sigure. 3. O serie de cel puțin 4 screenshot-uri pentru telefoane de 7 inch și 10 inch, folosind dispozitive mock-up profesionale. Vom adăuga texte explicative scurte deasupra fiecărui screenshot (ex: "Scanare inteligentă", "Fuziune rapidă"), asigurându-ne că mesajul este clar chiar și fără a descărca aplicația.

- **App Store Assets: Screenshot-uri și Iconițe Apple.** Vom exporta setul de imagini cerut de App Store Connect: screenshot-uri pentru ecranele de 6.5 inch (iPhone 11-15 Pro Max) și 5.5 inch (iPhone 8 Plus). Vom genera și iconița oficială în format 1024x1024px fără transparență, conform cerințelor Apple. Toate imaginile vor respecta stilul minimalist și culorile brandului, punând accent pe procesul de backup vCard, un punct forte pentru utilizatorii de iPhone care migrează frecvent datele.

- **Store Metadata: Redactarea Textelor Promoționale.** Vom scrie descrierile pentru magazinele de aplicații în Română și Engleză. Titlul va fi optimizat pentru SEO (max 50 caractere): "Contacte Duplicate - Scan & Merge". Descrierea scurtă va puncta beneficiul principal: "Curăță agenda în 30 de secunde". Descrierea lungă va include cuvinte cheie relevante (contacte, duplicate, vCard, backup, privat) și o listă de funcționalități cheie. Se va asigura că tonul este unul utilitar și de încredere, subliniind faptul că nu se colectează date personale.

- **Apple Privacy Questionnaire: Transparența Datelor.** Vom completa chestionarul de confidențialitate în App Store Connect. Vom declara că aplicația NU colectează date (Data not collected) și că nicio informație nu părăsește dispozitivul utilizatorului. Această declarație este susținută tehnic de testul offline efectuat la pasul 0925. Corectitudinea acestui chestionar este vitală pentru a obține eticheta "Privacy" pozitivă în Store, oferind utilizatorilor un argument puternic pentru descărcare.

- **Google Data Safety: Declarația de Securitate.** Similar cu pasul anterior, vom completa secțiunea "Data Safety" din Play Console. Vom explica utilizarea permisiunilor `READ_CONTACTS` și `WRITE_CONTACTS` exclusiv pentru funcționalitatea de bază a aplicației (organizarea agendei). Vom declara că datele sunt procesate local și că utilizatorul are control total asupra ștergerii acestora. Această conformitate este obligatorie pentru a evita suspendarea aplicației sau limitarea vizibilității în magazinul Google.
- **Log Audit: Eliminarea Ultimelor Urme de Date.** Vom efectua o scanare recursivă a întregului proiect (`grep` / `search_project`) pentru a identifica orice apel de logging (`AppLogger`) care ar putea conține date sensibile scăpate accidental. Ne vom asigura că orice variabilă care stochează nume de contacte sau numere de telefon este tratată ca `PII` (Personally Identifiable Information) și nu este scrisă în log-urile de producție. Acest pas de "igienă tehnică" este obligatoriu înainte de a trimite aplicația la review, garantând securitatea utilizatorului la cele mai înalte standarde.

- **Compliance Check: Verificarea Regulilor de Ștergere.** Vom valida manual funcționalitatea de "Șterge toate datele aplicației" din setări. Vom verifica dacă folderul de backup-uri este golit complet, dacă baza de date Drift este ștearsă de pe disc și dacă `SecureStorage` nu mai conține cheia de criptare. Acest pas confirmă conformitatea cu cerințele "Right to be Forgotten" din GDPR și regulile stricte ale Apple privind datele utilizatorului. Testul se va face prin inspectarea sistemului de fișiere al dispozitivului după rularea comenzii de wipe.

- **Release Notes: Redactarea Jurnalului de Versiune.** Vom crea sau actualiza fișierul `CHANGELOG.md` cu detaliile versiunii `1.0.0 (Initial Release)`. Vom enumera funcționalitățile principale: Scanare inteligentă, Backup vCard securizat, Suport multilingv (RO/EN) și Tehnologie 100% Offline. Textele din Changelog vor fi folosite și în secțiunile "What's New" din magazinele de aplicații. Această documentație oferă o imagine clară asupra maturității proiectului și a valorii oferite utilizatorului final.

- **Versioning: Sincronizarea Versiunilor Tehice.** Vom actualiza `version: 1.0.0+1` în `pubspec.yaml` și ne vom asigura că acest număr de versiune este propagat corect în `build.gradle` (Android) și `Info.plist` (iOS). Vom verifica dacă `versionCode` și `buildNumber` sunt incrementate corespunzător față de versiunile de testare anterioare. Această rigoare tehnică previne erorile de tip "version mismatch" la încărcarea pachetelor în consolele Google și Apple.

- **Production Build: Generarea Pachetelor Finale Semnate.** Vom rula procesul de build final pentru ambele platforme: `flutter build appbundle --release` (Android) și `flutter build ipa --release` (iOS). Vom folosi cheile de semnare oficiale ale proiectului stocate în medii securizate. Vom verifica manual validitatea certificatelor de semnare. Aceste artefacte binare sunt rezultatul a 1000 de commit-uri de muncă asiduă și reprezintă varianta pe care utilizatorii o vor instala din magazinele oficiale.

- <WORK_CODE>0971-1000 - **Submission & Review: Lansarea Oficială.** Ultimul bloc de task-uri presupune încărcarea binarelor în Google Play Console și App Store Connect. Vom trece prin procesul de review, răspunzând prompt la orice clarificare cerută de echipele Apple și Google. Vom monitoriza erorile din consola de developer după lansarea graduală (Staged Rollout) la 10%, 50% și în final 100% din utilizatori. Succesul acestui pas marchează finalizarea planului de implementare și debutul oficial al aplicației "Contacte Duplicate" pe piață.
